# Pre-commit hooks configuration for local code quality checks
# Install with: npm run precommit:setup

repos:
  # Code quality checks
  - repo: local
    hooks:
      # TypeScript type checking
      - id: typescript-check
        name: TypeScript Type Check
        entry: npm run type-check
        language: system
        files: \.(ts|tsx)$
        pass_filenames: false
        stages: [pre-commit]

      # ESLint for code quality
      - id: eslint
        name: ESLint
        entry: npm run lint
        language: system
        files: \.(js|jsx|ts|tsx)$
        pass_filenames: false
        stages: [pre-commit]

      # Security audit (disabled for pre-push to avoid slowdown)
      # Run manually with: npm audit --audit-level=high
      # Or enable in CI/CD pipeline instead
      # - id: npm-audit
      #   name: NPM Security Audit
      #   entry: npm audit --audit-level=high
      #   language: system
      #   pass_filenames: false
      #   always_run: true
      #   stages: [pre-push]

      # Quick security check (only on manual run)
      - id: npm-audit-quick
        name: NPM Security Audit (Quick)
        entry: npm audit --audit-level=critical --production
        language: system
        pass_filenames: false
        always_run: true
        stages: [manual]

  # File checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
        name: Check for large files
        args: ['--maxkb=1000']

      - id: check-case-conflict
        name: Check for case conflicts

      - id: check-json
        name: Check JSON syntax

      - id: check-yaml
        name: Check YAML syntax
        args: ['--multi', '--unsafe']

      - id: detect-private-key
        name: Detect private keys

      - id: end-of-file-fixer
        name: Fix end of files
        exclude: |
          (?x)^(
            .*\.min\.js$|
            .*\.map$|
            node_modules/.*|
            \.git/.*|
            \.cache/.*|
            \.next/.*
          )$

      - id: trailing-whitespace
        name: Trim trailing whitespace
        exclude: |
          (?x)^(
            .*\.md$|
            .*\.min\.js$|
            .*\.map$|
            node_modules/.*|
            \.cache/.*|
            \.next/.*
          )$

# Global configuration
default_language_version:
  node: '20'
  python: python3

# Hook execution settings
fail_fast: false
minimum_pre_commit_version: '3.0.0'
