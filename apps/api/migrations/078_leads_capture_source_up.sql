-- Migration: Lead capture source
-- Version: 078
--
-- Контакт теперь берётся в трёх местах: шаг контакта в мастере, экран
-- результата (ветка feat/landing-conversion, ещё не влита) и разговор с
-- ботом (эта ветка). Без отметки источника сравнить их между собой нельзя, а
-- поле source уже занято — оно про то, откуда человек пришёл на сайт.
--
-- Номер 078 занят намеренно, а не 077: колонку с тем же именем и тем же
-- эффектом уже завела ветка feat/landing-conversion под номером 077, но она
-- не влита ни в dev, ни в эту ветку. IF NOT EXISTS ниже делает обе миграции
-- совместимыми при слиянии; при самом слиянии одна из двух миграций-дублей
-- (скорее всего эта) подлежит удалению.

ALTER TABLE leads ADD COLUMN IF NOT EXISTS capture_source TEXT;
COMMENT ON COLUMN leads.capture_source IS 'Где оставлен контакт: contact_step | result | bot';
