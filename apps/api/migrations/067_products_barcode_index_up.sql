-- Индекс по штрихкоду на живых средах.
--
-- Миграция 005 создаёт его вместе с таблицей, но на dev и проде она была
-- пустышкой: таблица уже существовала. Миграция 066 добавила сам столбец и
-- пропустила индекс — расхождение осталось ровно в одну строку, и его нашла
-- сверка со снимком схемы.
--
-- Отдельной миграцией, а не правкой 066: та уже применена на dev, а править
-- применённое — это и есть та дисциплина, нарушение которой породило всё
-- расхождение.
CREATE INDEX IF NOT EXISTS idx_products_barcode ON products(barcode);
