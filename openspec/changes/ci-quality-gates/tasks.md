## 1. Немедленные исправления

- [ ] 1.1 Удалить фиктивный шаг «Check coverage threshold» из `.github/workflows/ci.yml:37-39`. Проверка: сценарий «Отсутствие декоративных шагов».
- [ ] 1.2 Измерить фактическое покрытие бэкенда, зафиксировать его как стартовый порог. Проверка: значение приложено к PR.
- [ ] 1.3 Добавить в `apps/api/Makefile` цель проверки порога по профилю покрытия и вызвать её в CI. Проверка: сценарий «Снижение покрытия бэкенда» — искусственное снижение роняет джобу.
- [ ] 1.4 Включить `-race` в `apps/api/Makefile` для целей `test` и `test-coverage-ci`. Проверка: сценарий «Гонка при конкурентном доступе» на тесте-пробнике.
- [ ] 1.5 Разобрать срабатывания детектора гонок, если они появятся. Проверка: `make test` зелёные с `-race`.
- [ ] 1.6 Добавить шаг `go vet ./...` в джобу бэкенда. Проверка: CI падает на намеренной ошибке форматной строки.

## 2. Статический анализ

- [ ] 2.1 Создать `apps/api/.golangci.yml` с набором `govet`, `errcheck`, `staticcheck`, `ineffassign`, `bodyclose`, `rowserrcheck`, `sqlclosecheck`, `noctx`. Проверка: локальный прогон выдаёт отчёт.
- [ ] 2.2 Добавить джобу `golangci-lint` в информационном режиме, зафиксировать список срабатываний. Проверка: список приложен к PR.
- [ ] 2.3 Исправить срабатывания. Проверка: локальный прогон без замечаний.
- [ ] 2.4 Перевести джобу в блокирующий режим. Проверка: сценарий «Незакрытый результат запроса».

## 3. Интеграционные тесты и миграции

- [ ] 3.1 Добавить в CI джобу с сервис-контейнером PostgreSQL и тегом сборки `integration`. Проверка: джоба поднимается и выполняет пустой набор.
- [ ] 3.2 Написать интеграционный тест применения всех миграций вперёд и отката назад на чистой базе. Проверка: сценарий «Некорректная миграция отката» на намеренно испорченной миграции.
- [ ] 3.3 Написать интеграционный тест, проверяющий, что каждый зарегистрированный маршрут отвечает не `404` при корректной аутентификации. Проверка: тест обнаруживает маршрут, объявленный на фронтенде, но отсутствующий в роутере.
- [ ] 3.4 Перевести на реальную БД ключевые сценарии модулей `auth`, `food-tracker`, `curator`. Проверка: джоба зелёная, длительность в пределах пяти минут.

## 4. Сквозные тесты

- [ ] 4.1 Прогнать существующие 51 файл Playwright локально против поднятого окружения, разделить на рабочие и нестабильные. Проверка: таблица результатов приложена к PR.
- [ ] 4.2 Создать `.github/workflows/e2e.yml`: PostgreSQL, миграции, засев тестовых пользователей, поднятие обоих сервисов, прогон Playwright. Проверка: джоба зелёная на рабочем наборе.
- [ ] 4.3 Пометить нестабильные тесты пропускаемыми и завести на каждый задачу. Проверка: список задач приложен к PR.
- [ ] 4.4 Сохранять отчёт и артефакты неуспешных сценариев. Проверка: сценарий «Сохранение отчёта».
- [ ] 4.5 Добавить сквозной сценарий смены пароля, покрывающий связку «фронтенд вызывает зарегистрированный маршрут». Проверка: сценарий «Фронтенд обращается к несуществующему маршруту API» — тест падает на текущем коде до исправления в `enforce-password-policy`.
- [ ] 4.6 Сделать джобу блокирующей. Проверка: PR с падающим сквозным тестом не сливается.

## 5. Безопасность и целостность

- [ ] 5.1 Снять `continue-on-error` с `npm audit` и ESLint-правил безопасности. Проверка: искусственная уязвимость роняет джобу.
- [ ] 5.2 Ввести файл исключений с полями «находка, причина, срок пересмотра» по образцу `.trivyignore`. Проверка: сценарий «Оформленное исключение».
- [ ] 5.3 Подключить проверку целостности кодовой базы из изменения `remove-dead-code`. Проверка: джоба присутствует и блокирует.

## 6. Оптимизация сборки

- [ ] 6.1 Вынести сборку образов в отдельную джобу с сохранением артефакта. Проверка: сценарий «Проверка количества сборок».
- [ ] 6.2 Переключить `container-security-scan` и `e2e` на загрузку готового образа. Проверка: общее время запуска сократилось; значения до и после приложены к PR.

## 7. Развёртывание

- [ ] 7.1 Создать `.github/workflows/deploy.yml`: запуск развёртывания dev после успешных проверок на ветке разработки через API Dokploy. Проверка: сценарий «Развёртывание среды разработки».
- [ ] 7.2 Добавить опрос пробы готовности после развёртывания с таймаутом и пометкой неуспеха. Проверка: сценарий «Проба готовности не отвечает после развёртывания» на искусственно сломанном развёртывании dev.
- [ ] 7.3 Добавить развёртывание производственной среды по тегу версии. Проверка: сценарий «Развёртывание в производственную среду» на тестовом теге.
- [ ] 7.4 Добавить оповещение о результате развёртывания в канал команды. Проверка: сообщение приходит.

## 8. Защита веток

- [ ] 8.1 Включить обязательный набор проверок для `main` и `dev` в настройках репозитория. Проверка: сценарий «Неуспешная обязательная проверка» — PR с падающей проверкой не сливается; скриншот настроек приложен к PR.
- [ ] 8.2 Зафиксировать список обязательных проверок в `CLAUDE.md`. Проверка: ревью PR.
