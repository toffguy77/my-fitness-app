## 1. Немедленные исправления

- [x] 1.1 Удалить фиктивный шаг «Check coverage threshold» из `.github/workflows/ci.yml:37-39`. Проверка: сценарий «Отсутствие декоративных шагов».
- [x] 1.2 Измерить фактическое покрытие бэкенда, зафиксировать его как стартовый порог. Проверка: значение приложено к PR.
- [x] 1.3 Добавить в `apps/api/Makefile` цель проверки порога по профилю покрытия и вызвать её в CI. Проверка: сценарий «Снижение покрытия бэкенда» — искусственное снижение роняет джобу.
- [x] 1.4 Включить `-race` в `apps/api/Makefile` для целей `test` и `test-coverage-ci`. Проверка: сценарий «Гонка при конкурентном доступе» на тесте-пробнике.
- [x] 1.5 Разобрать срабатывания детектора гонок, если они появятся. Проверка: `make test` зелёные с `-race`.
- [x] 1.6 Добавить шаг `go vet ./...` в джобу бэкенда. Проверка: CI падает на намеренной ошибке форматной строки.

## 2. Статический анализ

- [x] 2.1 Создать `apps/api/.golangci.yml` с набором `govet`, `errcheck`, `staticcheck`, `ineffassign`, `bodyclose`, `rowserrcheck`, `sqlclosecheck`, `noctx`. Проверка: локальный прогон выдаёт отчёт.
- [x] 2.2 Добавить джобу `golangci-lint` в информационном режиме, зафиксировать список срабатываний. Проверка: список приложен к PR.
- [x] 2.3 Исправить срабатывания. Проверка: локальный прогон без замечаний.
- [x] 2.4 Перевести джобу в блокирующий режим. Проверка: сценарий «Незакрытый результат запроса».

## 3. Интеграционные тесты и миграции

- [x] 3.1 Добавить в CI джобу с сервис-контейнером PostgreSQL и тегом сборки `integration`. Проверка: джоба поднимается и выполняет пустой набор.
- [x] 3.2 Написать интеграционный тест применения всех миграций вперёд и отката назад на чистой базе. Проверка: сценарий «Некорректная миграция отката» на намеренно испорченной миграции.
- [ ] 3.3 Написать интеграционный тест, проверяющий, что каждый зарегистрированный маршрут отвечает не `404` при корректной аутентификации. Проверка: тест обнаруживает маршрут, объявленный на фронтенде, но отсутствующий в роутере.
- [ ] 3.4 Перевести на реальную БД ключевые сценарии модулей `auth`, `food-tracker`, `curator`. Проверка: джоба зелёная, длительность в пределах пяти минут.

## 4. Сквозные тесты

- [x] 4.1 Прогнать существующие сценарии Playwright против поднятого окружения, разделить на рабочие и нестабильные. Проверка: прогнано в CI, а не локально — первый настоящий прогон дал 131/17, каждое падение разобрано: пустой каталог продуктов и отсутствующий диалог (исправлено в засеве), отсутствие цели по воде (тоже засев), FAB под панелью навигации, карточки клиентов, форма входа. Тесты тренировок пропущены с указанием причины — экрана нет, изменение вне скоупа.
- [x] 4.2 Создать `.github/workflows/e2e.yml`: PostgreSQL, миграции, засев тестовых пользователей, поднятие обоих сервисов, прогон Playwright. Проверка: джоба зелёная на рабочем наборе.
- [x] 4.3 Пометить нестабильные тесты пропускаемыми. Проверка: нестабильных не оказалось — все падения были настоящими дефектами и исправлены; пропущены только тесты несуществующего экрана тренировок.
- [x] 4.4 Сохранять отчёт и артефакты неуспешных сценариев. Проверка: сценарий «Сохранение отчёта».
- [x] 4.5 Добавить сквозной сценарий смены пароля, покрывающий связку «фронтенд вызывает зарегистрированный маршрут». Проверка: сценарий «Фронтенд обращается к несуществующему маршруту API» — тест падает на текущем коде до исправления в `enforce-password-policy`.
- [x] 4.6 Сделать джобу блокирующей. Проверка: PR с падающим сквозным тестом не сливается.

## 5. Безопасность и целостность

- [x] 5.1 Снять `continue-on-error` с `npm audit` и ESLint-правил безопасности. Проверка: искусственная уязвимость роняет джобу.
- [x] 5.2 Ввести файл исключений с полями «находка, причина, срок пересмотра» по образцу `.trivyignore`. Проверка: сценарий «Оформленное исключение».
- [x] 5.3 Подключить проверку целостности кодовой базы из изменения `remove-dead-code`. Проверка: джоба присутствует и блокирует.

## 6. Оптимизация сборки

- [x] 6.1 Вынести сборку образов в отдельную джобу с сохранением артефакта. Проверка: сценарий «Проверка количества сборок».
- [x] 6.2 Переключить `container-security-scan` и `e2e` на загрузку готового образа. Проверка: общее время запуска сократилось; значения до и после приложены к PR.

## 7. Развёртывание

- [x] 7.1 Создать `.github/workflows/deploy.yml`: запуск развёртывания dev после успешных проверок на ветке разработки через API Dokploy. Проверка: сценарий «Развёртывание среды разработки».
- [x] 7.2 Добавить опрос пробы готовности после развёртывания с таймаутом и пометкой неуспеха. Проверка: сценарий «Проба готовности не отвечает после развёртывания» на искусственно сломанном развёртывании dev.
- [x] 7.3 Добавить развёртывание производственной среды по тегу версии. Проверка: сценарий «Развёртывание в производственную среду» на тестовом теге.
- [ ] 7.4 Добавить оповещение о результате развёртывания в канал команды. Проверка: сообщение приходит.

## 8. Защита веток

- [x] 8.1 Включить обязательный набор проверок для `main` и `dev`. Проверка: набор уже включён через ruleset репозитория (`CI Pipeline`, `Performance Analysis`, `Comprehensive Security Scan`, `Container Security Scan`), проверено через API 2026-09-01. Заодно выяснилось, что переименование джобы делает обязательную проверку невыполнимой — имя восстановлено, правило записано в `CLAUDE.md`.
- [x] 8.2 Зафиксировать список обязательных проверок в `CLAUDE.md`. Проверка: ревью PR.
