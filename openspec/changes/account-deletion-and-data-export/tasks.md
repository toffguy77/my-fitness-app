## 1. Подготовка

- [x] 1.1 Составить полный перечень таблиц со ссылкой на `users` и назначить каждой стратегию (`delete` / `anonymize` / `keep`) с обоснованием. Проверка: таблица соответствий приложена к PR и покрывает вывод запроса к `information_schema` по внешним ключам на `users`.
- [ ] 1.2 Проверить формат ключей объектов во всех четырёх бакетах S3 на наличие идентификатора пользователя. Проверка: для каждого бакета приведён пример ключа; при отсутствии идентификатора — заведена подзадача на его добавление.
- [ ] 1.3 Прочитать текущий текст `/legal/privacy` и выписать расхождения с планируемым поведением. Проверка: список расхождений приложен к PR.

## 2. Схема данных

- [x] 2.1 Создать миграцию `047_account_deletion_{up,down}.sql`: `users.deletion_requested_at`, `users.deleted_at`, `users.anonymized_at`, `users.is_system BOOLEAN NOT NULL DEFAULT false`; таблица `data_exports(id UUID PK, user_id BIGINT, status TEXT, s3_key TEXT, requested_at, completed_at, downloaded_at, expires_at)`. Проверка: `make db-migrate`, down откатывает чисто.
- [x] 2.2 Создать в той же миграции технического пользователя «Удалённый пользователь» с `is_system = true`. Проверка: строка присутствует, роль `user`, вход по ней невозможен.

## 3. Экспорт данных

- [x] 3.1 Создать модуль `apps/api/internal/modules/account/` с `export.go`: сбор данных по разделам в JSON. Проверка: unit-тесты на состав разделов.
- [x] 3.2 Реализовать упаковку в ZIP с файлами из четырёх бакетов и файлом описания на русском. Проверка: тест — архив содержит ожидаемые записи.
- [x] 3.3 Реализовать `POST /api/v1/users/me/export` с лимитами «одна активная» и «одна в сутки». Проверка: тесты сценариев `202`, `409`, `429`.
- [x] 3.4 Зарегистрировать фоновую задачу `account.build-exports` в реестре задач. Проверка: после запроса архив появляется в бакете, статус переходит в готовый.
- [x] 3.5 Реализовать `GET /api/v1/users/me/export/:id` с одноразовой выдачей и сроком 24 часа. Проверка: тесты сценариев выдачи, повторного скачивания `410`, чужой выгрузки `403`.
- [ ] 3.6 Создавать уведомление о готовности выгрузки. Проверка: тест сценария «Уведомление о готовности».
- [x] 3.7 Зарегистрировать задачу `cleanup.data-exports` — удаление архивов и записей с истёкшим сроком. Проверка: тест.

## 4. Запрос на удаление и деактивация

- [x] 4.1 Реализовать `POST /api/v1/users/me/deletion` с проверкой пароля, отзывом сессий и возвратом даты удаления. Проверка: тесты сценариев `202`, `401`, `409`.
- [x] 4.2 Реализовать `DELETE /api/v1/users/me/deletion` — отмена. Проверка: тест сценария «Отмена удаления».
- [x] 4.3 В `middleware/auth.go` отклонять запросы деактивированного пользователя ко всем эндпоинтам, кроме разрешённого списка. Проверка: тест сценария «Обращение к защищённому эндпоинту».
- [ ] 4.4 В ответе входа возвращать признак ожидания удаления и дату. Проверка: тест сценария «Вход в период ожидания».
- [ ] 4.5 Исключить деактивированных клиентов из выдач куратора (`curator/service.go`: `GetClients`, `GetAttentionList`, `GetAnalytics`). Проверка: тест сценария «Список клиентов куратора».
- [ ] 4.6 Не создавать уведомления и не отправлять письма деактивированным. Проверка: тест сценария «Уведомления не создаются».
- [ ] 4.7 Уведомлять куратора об уходе клиента. Проверка: тест — у куратора появляется уведомление.

## 5. Необратимое удаление

- [x] 5.1 Реализовать `erasure.go` с таблицей стратегий из задачи 1.1 и выполнением удаления и анонимизации в одной транзакции. Проверка: интеграционный тест на аккаунте с полным набором данных.
- [x] 5.2 Реализовать тест полноты стратегий: запрос к схеме БД, сверка со списком, падение на непокрытой таблице. Проверка: временно добавленная таблица со ссылкой на `users` роняет тест.
- [ ] 5.3 Реализовать удаление объектов пользователя из четырёх бакетов после фиксации транзакции, с повторным проходом при частичном сбое. Проверка: тест на моке S3, объекты удалены.
- [x] 5.4 Зарегистрировать задачу `account.execute-deletions` — обработка аккаунтов старше 30 дней с момента запроса. Проверка: интеграционный тест с подменённым временем.
- [ ] 5.5 Проверить целостность данных куратора после удаления клиента. Проверка: тест сценария «Переписка сохранена обезличенной».

## 6. Интерфейс

- [x] 6.1 Создать страницу `apps/web/src/app/settings/privacy/page.tsx` с разделами «Скачать мои данные» и «Удалить аккаунт», явным описанием последствий и сроков. Проверка: `npx jest settings` зелёные.
- [x] 6.2 Реализовать диалог удаления с вводом пароля и подтверждением фразой. Проверка: тесты компонента.
- [x] 6.3 Заменить заглушку в `SettingsLocality.tsx:114-118` переходом на `/settings/privacy`. Проверка: `grep -rn "Функция в разработке" apps/web/src` пусто.
- [ ] 6.4 Реализовать экран восстановления аккаунта при входе в период ожидания. Проверка: e2e-сценарий «запрос удаления → вход → восстановление».

## 7. Документы

- [ ] 7.1 Актуализировать `apps/web/src/app/legal/privacy/page.tsx`: сроки хранения, состав удаляемого, что сохраняется обезличенным, срок хранения резервных копий. Проверка: расхождения из задачи 1.3 закрыты.
- [ ] 7.2 Добавить в `docs/user-guide/` раздел об удалении аккаунта и экспорте данных. Проверка: ревью PR.

## 8. Выкатка

- [ ] 8.1 На dev создать тестовый аккаунт с полным набором данных, выполнить экспорт, затем полный цикл удаления с ускоренным периодом ожидания. Проверка: архив корректен, после удаления данные отсутствуют, чат куратора работает.
- [ ] 8.2 Задеплоить на prod. Проверка: экспорт и запрос на удаление работают на тестовом аккаунте прода.
