## 1. Подготовка

- [x] 1.1 Составить перечень существующих типов уведомлений с указанием срочности и канала по умолчанию. Проверка: таблица приложена к PR и покрывает все вызовы `CreateNotification`. *Таблица — `defaultChannels` в `internal/modules/notifications/delivery.go`; `TestEveryDeclaredTypeIsClassified` падает, если появился тип без строки в ней.*
- [x] 1.2 Проверить настройки SPF, DKIM и DMARC для домена отправителя. Проверка: результат проверки приложен к PR; при отсутствии — заведена блокирующая задача.

  Проверено 2026-09-05 (`dig TXT`):
  - SPF — есть: `v=spf1 redirect=_spf.yandex.net`
  - DKIM — есть: `mail._domainkey.burcev.team`, ключ RSA от Яндекс 360
  - **DMARC — отсутствует**: у `_dmarc.burcev.team` записи нет.

  **Блокирует пункт 8.3** (включение писем на проде). Без DMARC почтовые
  системы решают судьбу письма по своим эвристикам, а мы не видим отчётов о
  том, что с письмами происходит. DNS домена не управляется терраформом
  (`infra/` не содержит DNS-ресурсов), поэтому запись заводит владелец домена
  в панели, начиная с наблюдения:
  `_dmarc.burcev.team TXT "v=DMARC1; p=none; rua=mailto:dmarc@burcev.team"`,
  и после недели чистых отчётов — `p=quarantine`.
- [ ] 1.3 Сгенерировать ключи VAPID и внести их в окружения dev и prod. Проверка: ключи заданы, публичный доступен фронтенду.

  Генератор написан — `go run ./cmd/vapid-keys` в `apps/api`. Пару заводит
  владелец: приватный ключ — секрет, и он не должен проходить ни через чей
  терминал, кроме того, где его впишут в Dokploy. **Пока переменных нет,
  возможность `web_push` выключена**: `/health` показывает её в списке
  выключенных, эндпоинты отвечают 503, а планировщик доставки помечает
  push-задания `skipped`, а не копит их.

## 2. Схема данных

- [x] 2.1 Создать миграцию `056_notification_delivery_{up,down}.sql` (`push_subscriptions` — отдельной, `057`): `push_subscriptions`, `notification_deliveries`, расширение таблицы предпочтений до матрицы «тип события × канал», `users.quiet_hours_start`, `users.quiet_hours_end`. Проверка: `make db-migrate`, down откатывает чисто.
- [x] 2.2 Перенести существующие записи `content_notification_preferences` в новую модель без изменения смысла для пользователя. Проверка: сценарий «Сохранение ранее сделанного выбора» на копии данных прода. *Покатегорийные отписки матрица «тип × канал» выразить не может без потери смысла, поэтому старая таблица остаётся и по-прежнему решает, создавать ли уведомление о материале. Переносится глобальный mute: тот, кто выключил уведомления о контенте, не начнёт получать их письмом.*

## 3. Слой доставки

- [x] 3.1 Создать `internal/modules/notifications/delivery.go`: расчёт каналов по предпочтениям, создание заданий в одной транзакции с уведомлением. Проверка: тесты сценариев «Событие с разрешённым push» и «Событие с запрещёнными внешними каналами».
- [x] 3.2 Расширить `CreateNotification` (`service.go:266-320`) вызовом слоя доставки. Проверка: тест сценария «Отправка не задерживает запрос».
- [x] 3.3 Зарегистрировать фоновую задачу отправки заданий доставки. Проверка: задание переходит в конечный статус, запись в `notification_deliveries` создана.
- [x] 3.4 Реализовать учёт результата доставки. Проверка: тест сценария «Неуспешная доставка».

## 4. Web push

- [x] 4.1 Добавить ключи VAPID в конфигурацию как опциональную возможность в терминах `harden-config-and-secrets`. Проверка: сценарий «Push выключен в конфигурации».
- [x] 4.2 Реализовать эндпоинты подписки и отписки на push. Проверка: тесты, подписка сохраняется с адресом доставки и ключами.
- [x] 4.3 Реализовать отправку через протокол Web Push с удалением недействительных подписок. Проверка: тесты сценариев «Доставка на подписанное устройство» и «Недействительная подписка».
- [x] 4.4 Добавить в service worker обработчик события push и переход по уведомлению. *Обработчик в `apps/web/public/push-sw.js`, подключается через `importScripts` в `next.config.ts`: next-pwa пересобирает `sw.js` из workbox на каждой сборке и затёр бы всё, что записано прямо в него.* Проверка: push на dev открывает нужный экран — после 1.3.
- [x] 4.5 Реализовать запрос разрешения в момент очевидной пользы, с предварительным объяснением. Проверка: тесты компонента, разрешение не запрашивается при первом входе.
- [x] 4.6 Реализовать определение платформы и подсказку об установке на домашний экран. Проверка: сценарий «Открытие в браузере на платформе с ограничением».
- [x] 4.7 Зарегистрировать задачу чистки мёртвых подписок. Проверка: тест.

## 5. Почта

- [x] 5.1 Реализовать `digest.go`: отбор непрочитанных уведомлений с истёкшим временем ожидания, сборка одного письма. Проверка: тесты сценариев «Уведомление прочитано вовремя», «Уведомление не прочитано», «Объединение событий в одно письмо».
- [ ] 5.2 Создать шаблон сводного письма на русском со ссылкой отписки. Проверка: визуальная проверка письма в почтовом клиенте, скриншот приложен к PR. *Шаблон написан (`notificationDigestTemplate`); осталась визуальная проверка на dev — делается вместе с 8.2.*
- [x] 5.3 Убедиться, что транзакционные письма не проходят через слой доставки. Проверка: сценарий «Транзакционные письма не задерживаются».
- [x] 5.4 Реализовать подписанную ссылку отписки и страницу подтверждения без аутентификации. Проверка: тесты сценариев «Переход по ссылке отписки» и «Подделанная ссылка отписки».
- [x] 5.5 Зарегистрировать задачу сборки и отправки дайджестов. Проверка: интеграционный тест — письмо уходит один раз за период.

## 6. Предпочтения

- [x] 6.1 Реализовать чтение и запись матрицы предпочтений с запретом отключения канала приложения. Проверка: тесты трёх сценариев требования о матрице.
- [x] 6.2 Реализовать значения по умолчанию для новых пользователей. Проверка: сценарий «Предпочтения нового пользователя».
- [x] 6.3 Реализовать тихое время с учётом часового пояса и отложенной доставкой. Проверка: тесты сценариев «Событие в тихое время» и «Смена часового пояса».
- [x] 6.4 Переписать `apps/web/src/app/settings/notifications/page.tsx` под матрицу, тихое время и управление push. Проверка: `npx jest notifications settings` зелёные, ручная проверка на dev.

## 7. Интерфейс куратора

- [x] 7.1 Показывать куратору факт доставки уведомления клиенту. Проверка: сценарий «Куратор видит факт оповещения».

## 8. Выкатка

- [ ] 8.1 Задеплоить на dev, проверить push в Chrome, Firefox и в установленном PWA на iOS. Проверка: чек-лист по платформам пройден.
- [ ] 8.2 Проверить дайджест: накопление, одно письмо, отписка. Проверка: чек-лист пройден.
- [ ] 8.3 Включить на проде поэтапно: сначала push для сообщений чата, затем остальные срочные типы, затем письма. Проверка: на каждом этапе доля неуспешных доставок и жалоб на спам под наблюдением; результаты приложены к задаче.
