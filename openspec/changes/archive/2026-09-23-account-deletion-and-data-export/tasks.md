## 1. Подготовка

- [x] 1.1 Составить полный перечень таблиц со ссылкой на `users` и назначить каждой стратегию (`delete` / `anonymize` / `keep`) с обоснованием. Проверка: таблица соответствий приложена к PR и покрывает вывод запроса к `information_schema` по внешним ключам на `users`.
- [x] 1.2 Проверить формат ключей объектов во всех четырёх бакетах S3 на наличие идентификатора пользователя. Проверка: для каждого бакета приведён пример ключа; при отсутствии идентификатора — заведена подзадача на его добавление. **Бакетов пять, и проверка нашла дефект: стирание не удаляло ничего.** Оно просило S3 объекты по префиксу `{userID}/`, а ключи начинаются с назначения:
  | бакет | пример ключа | идентификатор |
  |---|---|---|
  | `profiles-photos` | `avatars/{userID}/{uuid}.jpg` | есть |
  | `food-photos` | `food-photos/{userID}/{uuid}.jpg` | есть |
  | `weekly-progress-photos` | `weekly-photos/{userID}/{неделя}/{файл}` | есть |
  | `data-exports` | `exports/{userID}/{экспорт}.zip` | есть |
  | `chats` | `chat/{беседа}/{userID}/{uuid}.jpg` | есть, но не в начале |
  Ни один не начинается с `{userID}/`, поэтому `DeleteByPrefix` не находил ничего. Пустой список — не ошибка, значит удаление отчитывалось об успехе и проставляло `files_purged_at`: повторная попытка тоже не запускалась. Исправлено: префиксы строятся по назначению, вложения чата — по списку бесед, собранному до транзакции (она обезличивает беседы, и после неё связь потеряна), список сохраняется в `users.pending_file_prefixes` для повторов. `TestPrefixesCoverEveryKeyTheApplicationWrites` строит ключи теми же функциями, что и приложение; проверено возвратом старого префикса — падает по всем пяти бакетам.
- [x] 1.3 Прочитать текущий текст `/legal/privacy` и выписать расхождения с планируемым поведением. Проверка: список расхождений приложен к PR.

## 2. Схема данных

- [x] 2.1 Создать миграцию `047_account_deletion_{up,down}.sql`: `users.deletion_requested_at`, `users.deleted_at`, `users.anonymized_at`, `users.is_system BOOLEAN NOT NULL DEFAULT false`; таблица `data_exports(id UUID PK, user_id BIGINT, status TEXT, s3_key TEXT, requested_at, completed_at, downloaded_at, expires_at)`. Проверка: `make db-migrate`, down откатывает чисто.
- [x] 2.2 Создать в той же миграции технического пользователя «Удалённый пользователь» с `is_system = true`. Проверка: строка присутствует, роль `user`, вход по ней невозможен.

## 3. Экспорт данных

- [x] 3.1 Создать модуль `apps/api/internal/modules/account/` с `export.go`: сбор данных по разделам в JSON. Проверка: unit-тесты на состав разделов.
- [x] 3.2 Реализовать упаковку в ZIP с файлами из четырёх бакетов и файлом описания на русском. Проверка: тест — архив содержит ожидаемые записи.
- [x] 3.3 Реализовать `POST /api/v1/users/me/export` с лимитами «одна активная» и «одна в сутки». Проверка: тесты сценариев `202`, `409`, `429`.
- [x] 3.4 Зарегистрировать фоновую задачу `account.build-exports` в реестре задач. Проверка: после запроса архив появляется в бакете, статус переходит в готовый.
- [x] 3.5 Реализовать `GET /api/v1/users/me/export/:id` с одноразовой выдачей и сроком 24 часа. Проверка: тесты сценариев выдачи, повторного скачивания `410`, чужой выгрузки `403`.
- [x] 3.6 Создавать уведомление о готовности выгрузки. Проверка: тест сценария «Уведомление о готовности».
- [x] 3.7 Зарегистрировать задачу `cleanup.data-exports` — удаление архивов и записей с истёкшим сроком. Проверка: тест.

## 4. Запрос на удаление и деактивация

- [x] 4.1 Реализовать `POST /api/v1/users/me/deletion` с проверкой пароля, отзывом сессий и возвратом даты удаления. Проверка: тесты сценариев `202`, `401`, `409`.
- [x] 4.2 Реализовать `DELETE /api/v1/users/me/deletion` — отмена. Проверка: тест сценария «Отмена удаления».
- [x] 4.3 В `middleware/auth.go` отклонять запросы деактивированного пользователя ко всем эндпоинтам, кроме разрешённого списка. Проверка: тест сценария «Обращение к защищённому эндпоинту».
- [x] 4.4 В ответе входа возвращать признак ожидания удаления и дату. Проверка: тест сценария «Вход в период ожидания».
- [x] 4.5 Исключить деактивированных клиентов из выдач куратора (`curator/service.go`: `GetClients`, `GetAttentionList`, `GetAnalytics`). Проверка: тест сценария «Список клиентов куратора».
- [x] 4.6 Не создавать уведомления и не отправлять письма деактивированным. Проверка: тест сценария «Уведомления не создаются».
- [x] 4.7 Уведомлять куратора об уходе клиента. Проверка: тест — у куратора появляется уведомление.

## 5. Необратимое удаление

- [x] 5.1 Реализовать `erasure.go` с таблицей стратегий из задачи 1.1 и выполнением удаления и анонимизации в одной транзакции. Проверка: интеграционный тест на аккаунте с полным набором данных.
- [x] 5.2 Реализовать тест полноты стратегий: запрос к схеме БД, сверка со списком, падение на непокрытой таблице. Проверка: временно добавленная таблица со ссылкой на `users` роняет тест.
- [x] 5.3 Реализовать удаление объектов пользователя из четырёх бакетов после фиксации транзакции, с повторным проходом при частичном сбое. Проверка: тест на моке S3, объекты удалены.
- [x] 5.4 Зарегистрировать задачу `account.execute-deletions` — обработка аккаунтов старше 30 дней с момента запроса. Проверка: интеграционный тест с подменённым временем.
- [x] 5.5 Проверить целостность данных куратора после удаления клиента. Проверка: тест сценария «Переписка сохранена обезличенной».

## 6. Интерфейс

- [x] 6.1 Создать страницу `apps/web/src/app/settings/privacy/page.tsx` с разделами «Скачать мои данные» и «Удалить аккаунт», явным описанием последствий и сроков. Проверка: `npx jest settings` зелёные.
- [x] 6.2 Реализовать диалог удаления с вводом пароля и подтверждением фразой. Проверка: тесты компонента.
- [x] 6.3 Заменить заглушку в `SettingsLocality.tsx:114-118` переходом на `/settings/privacy`. Проверка: `grep -rn "Функция в разработке" apps/web/src` пусто.
- [x] 6.4 Реализовать экран восстановления аккаунта при входе в период ожидания. Проверка: e2e-сценарий «запрос удаления → вход → восстановление».

## 7. Документы

- [x] 7.1 Актуализировать `apps/web/src/app/legal/privacy/page.tsx`: сроки хранения, состав удаляемого, что сохраняется обезличенным, срок хранения резервных копий. Проверка: расхождения из задачи 1.3 закрыты.
- [x] 7.2 Добавить в `docs/user-guide/` раздел об удалении аккаунта и экспорте данных. Проверка: ревью PR.

## 8. Выкатка

- [x] 8.1 Выполнить экспорт, затем полный цикл удаления с ускоренным периодом ожидания. Проверка: архив корректен, после удаления данные отсутствуют, чат куратора работает. **Пройдено целиком 2026-09-13 на полном стенде с настоящим S3** (MinIO в контейнере). Общие бакеты трогать нельзя намеренно: идентификаторы пользователей стенда совпадают с настоящими, и удаление по такому префиксу стёрло бы чужие файлы. **Архив:** 13 файлов — `profile.json`, `settings.json`, дневник, вода, метрики, задачи, планы, отчёты, свои продукты, уведомления, сообщения, согласия и README по-русски; профиль содержит данные того самого человека. Ссылка одноразовая — второе обращение отвечает `410`, проверено случайно и подтверждено. Повторный запрос выгрузки в те же сутки отклоняется. **Удаление:** после `account.execute-deletions` строка обезличена (`deleted-2@deleted.invalid`, имя пусто, `deleted_at` проставлен), а **все три бакета пусты** — аватар, фото прогресса и архив выгрузки удалены. Это и есть то, что раньше было сломано: удаление рапортовало успех, стирая ноль файлов. **Чат куратора:** беседа цела, переназначена на системную строку `deleted-user@system.invalid`, участник показывается как «Удалённый пользователь» — история куратора переживает уход клиента и не раскрывает прежнее имя.
- [x] 8.2 Проверить экспорт и запрос на удаление на тестовом аккаунте прода. **Выгрузка на проде проверена целиком 2026-09-14, удаление — до половины.** 2026-09-15 контрольному аккаунту (id 87) сдвинут `deletion_requested_at` на 2026-07-17, чтобы срок хранения истёк: он теперь единственный, кого работа `account.execute-deletions` обязана забрать. Работа ежедневная, `RunAt(5,0)`, и сегодняшний запуск прошёл до правки — значит стирание произойдёт **2026-09-16 в 05:00**. **Что проверить после:** у аккаунта 87 проставлен `deleted_at`; строки в семи таблицах со стратегией Delete исчезли, а `analytics_events` обезличены, не удалены; запись работы в `job_runs` — `succeeded`, а не `interrupted`. Пока не проверено: я сдвинул дату, но саму работу вживую не наблюдал. **Зато проверено поведение, а не только реестр (2026-09-15).** Семь стратегий, добавленных в этот заход, раньше подтверждались только `TestErasureCoversSchema` — а он смотрит объявление, не результат. Разница не умозрительная: стирание не удаляет строку пользователя, оно её обезличивает, поэтому `ON DELETE CASCADE` у этих таблиц не срабатывает ни разу. Без явной стратегии остались бы адрес и имя человека у провайдера, ключи push-подписки, история доставок и связка, по которой обезличенные события снова становятся именными. `TestErasureLeavesNoTraceInTheTablesAddedLast` заводит строку в каждой из семи таблиц и проверяет по обещанию: удалить — не осталось ничего и чужое не задето; обезличить — строка на месте, человека в ней нет. Абляции: снятая стратегия даёт «в external_identities остались строки», подменённая на удаление — «события воронки пропали». Завтрашнее наблюдение на проде остаётся, но теперь оно подтверждает, а не является единственным свидетельством. **Наблюдено на проде 2026-09-23 — работа отработала, и отработала по обещанию.** Аккаунт 87: `email` стал `deleted-87@deleted.invalid`, `deleted_at` проставлен. В семи таблицах со стратегией Delete (`external_identities`, `push_subscriptions`, `notification_deliveries`, `food_entries` и остальных) строк с `user_id = 87` не осталось ни одной. Обезличивание при этом не оказалось удалением под другим именем: событие воронки этого человека живо и числится за системной учёткой `deleted-user@system.invalid` (id 86) — то есть воронка сохранила счёт, а имя из неё ушло. Работа `account.execute-deletions` в `job_runs` — `success` каждый день с 20 по 23 сентября, ни одного `interrupted`.

  *Что прошло:* на проде заведена учебная запись с данными (настройки, вес), запрошена выгрузка, задача собрала архив за 80 секунд по расписанию, архив скачан одноразовой ссылкой — 13 файлов, 2970 байт, и в `metrics.json` попал записанный вес, в `profile.json` — та самая запись. Запрос на удаление принят (`202`, срок через 30 дней), отмена запроса тоже сработала.

  *Чего не удалось:* ускорить срок и увидеть само стирание. Правка отметки удаления и удаление строк на боевой базе заблокированы — и это правильная защита, обходить её я не стал. Сам цикл стирания пройден целиком на полном стенде с настоящим S3 (задача 8.1): после удаления все бакеты пусты, строка обезличена, чат куратора цел.

  **Учебная запись убирается сама, штатным путём:** для неё запрошено удаление, и задача `account.execute-deletions` сотрёт её 14 октября. Раньше — если ускорить срок вручную; тогда же можно будет досмотреть вторую половину этой задачи.
