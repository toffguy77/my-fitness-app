## 1. Разведка

- [ ] 1.1 Собрать по логам dev, файлы каких типов реально приходят на каждую из пяти точек загрузки за 30 дней. Проверка: таблица «точка → типы → доли» приложена к PR.
- [ ] 1.2 Зафиксировать белые списки типов по результатам 1.1, включая решение по HEIC. Проверка: списки согласованы в PR.

## 2. Пакет загрузок

- [x] 2.1 Создать `apps/api/internal/shared/upload/detect.go`: определение типа по первым 512 байтам, сверка с белым списком. Проверка: unit-тесты на JPEG, PNG, WebP, GIF, PDF, HTML с подменённым заголовком, пустой файл.
- [x] 2.2 Создать `validate.go`: проверка размеров через `image.DecodeConfig`, пределы 8000 пикселей по стороне и 50 мегапикселей. Проверка: тест сценариев «чрезмерные размеры» и «в допустимых пределах».
- [x] 2.3 Создать `sanitize.go`: перекодирование изображения в тот же формат с удалением метаданных. Проверка: тест — на входе изображение с геометкой, на выходе метаданных нет.
- [x] 2.4 Создать `key.go`: формирование ключа `<префикс>/<user_id>/<uuid>.<ext>`. Проверка: тест сценария «Имя файла с обходом пути».

## 3. Перевод точек загрузки

- [x] 3.1 Перевести аватар (`users/handler.go:230-250`) на пакет `upload`. Проверка: тесты точки, отклонение PDF.
- [x] 3.2 Перевести фото еды (`food-tracker/handler.go:375-420`). Проверка: тесты, распознавание продолжает работать.
- [x] 3.3 Перевести фото прогресса (`dashboard/handler.go:575-600`). Проверка: тесты, метаданные удаляются.
- [x] 3.4 Перевести вложения чата (`chat/handler.go:218-245`), добавить хранение исходного имени отдельным полем и белый список с PDF. Проверка: тесты сценариев «Недопустимый тип во вложении чата» и «Сохранение исходного имени».
- [x] 3.5 Перевести медиа контента (`content/handler.go`). Проверка: тесты.
- [x] 3.6 Изменить `internal/shared/storage/s3.go:98-140`: тип содержимого принимается только из определённого пакетом `upload`. Проверка: `grep -rn "header.Header.Get(\"Content-Type\")" apps/api/internal/modules` пусто.
- [ ] 3.7 Создать миграцию для поля исходного имени вложения, если его нет. Проверка: `make db-migrate`, down откатывает чисто.

## 4. Предел тела запроса

- [x] 4.1 Создать `internal/shared/middleware/bodylimit.go` с `http.MaxBytesReader` и пределом 12 МБ; подключить глобально. Проверка: тест сценария «Запрос с телом сверх общего предела».

## 5. Постраничная выдача

- [x] 5.1 Добавить в `internal/shared/response/` формат ответа коллекции и разбор параметров `limit`/`offset` с умолчанием 20 и пределом 100. Проверка: тесты четырёх сценариев спека.
- [x] 5.2 Переписать `admin/service.go:39-67`: постраничная выдача, вынос агрегации по `refresh_tokens` в отдельный запрос по идентификаторам страницы. Проверка: тест сценария «Список пользователей», план запроса не содержит полного сканирования `refresh_tokens`.
- [ ] 5.3 Перевести `admin.GetConversations` на постраничную выдачу. Проверка: тесты.
- [ ] 5.4 Проверить остальные обработчики коллекций на отсутствие запросов без ограничения; перевести найденные. Проверка: сценарий «Проверка отсутствия неограниченных запросов».
- [x] 5.5 Обновить экраны административного раздела под новый формат ответа и добавить подгрузку. Проверка: `npx jest admin` зелёные, ручная проверка на dev.

## 6. Сообщения пользователю

- [x] 6.1 Привести сообщения об отказе в загрузке к единому виду на русском с перечислением допустимых форматов и пределов. Проверка: тесты сообщений на всех пяти точках.
- [x] 6.2 Показывать понятную ошибку в интерфейсе при отклонении файла. Проверка: тесты компонентов загрузки.

## 7. Выкатка

- [ ] 7.1 Задеплоить на dev, проверить все пять точек загрузки реальными файлами, включая снимок с iPhone. Проверка: чек-лист пройден.
- [ ] 7.2 Задеплоить на prod, проверить административный список пользователей на реальном объёме. Проверка: время ответа зафиксировано до и после.
