## 1. Подготовка

- [x] 1.1 Получить токен доступа к API Яндекс.Метрики для счётчика `107159088` и завести его в Dokploy для dev и prod. Проверка: `GET /health` на dev показывает способность `ads_attribution` включённой; по опыту прошлых выкаток переменная не доезжает без новой сборки, поэтому проверять именно `/health`, а не успешность деплоя.
- [x] 1.2 Зафиксировать текущее состояние счётчика: пять автоцелей, ни одной своей, признак ретаргетинга снят у всех, сегментов ноль. Проверка: список приложен к PR — он же ожидаемый вход для скрипта из раздела 6.
- [x] 1.3 Согласовать белый список зеркалируемых событий и набор передаваемых конверсий. Проверка: оба перечня приложены к PR; каждое имя есть в `apps/api/internal/modules/analytics/dictionary.go`, и ни одно зеркалируемое имя не объявлено `ServerOnly`.

## 2. Зеркалирование событий в счётчик

- [x] 2.1 Объявить белый список зеркалируемых имён в `apps/web/src/shared/analytics/events.ts` рядом со словарём `EVENTS`. Проверка: тесты сценариев «Белый список соответствует словарю» и «Имя удалено из словаря».
- [x] 2.2 Проверить, что ни одно зеркалируемое имя не является серверным. Проверка: тест сценария «Серверные события не зеркалируются из браузера» сверяется с серверным словарём.
- [x] 2.3 Добавить зеркалирование в `track` (`apps/web/src/shared/analytics/client.ts:55`) — только имя цели, без свойств. Проверка: тесты сценариев «Событие в белом списке», «Событие вне белого списка», «Свойства события не передаются», «Запрет проверяется автоматически».
- [x] 2.4 Сделать зеркалирование безопасным при отсутствии счётчика. Проверка: тест сценария «Счётчик недоступен» — при отказе от согласия `track` не бросает и по-прежнему ставит событие в свою очередь.

## 3. Источник перехода в заявке

- [x] 3.1 Добавить миграцию `078_lead_attribution`: метки кампании, идентификатор браузера, идентификатор рекламного перехода в `leads`. Проверка: миграция применяется и откатывается на чистой базе; `go test ./internal/shared/database/` проходит.
- [x] 3.2 Расширить `Lead` и `CreateInput` в `apps/api/internal/modules/leads/types.go` полями источника и сохранять их в `Create`. Проверка: тесты сценариев «Заявка из рекламного перехода» и «Заявка из прямого захода».
- [x] 3.3 Реализовать чтение меток кампании при входе и хранение их в хранилище сеанса новым модулем в `apps/web/src/shared/analytics/`. Проверка: тесты сценариев «Метки прочитаны на входе и использованы позже», «Новый приход без меток», «Хранилище недоступно».
- [x] 3.4 Передавать метки вместо `document.referrer` в `GuestOnboarding.tsx:402` и обновить состав полей в `apps/web/src/features/onboarding/api/guest.ts:66`. Проверка: `npm run type-check` в `apps/web` проходит; тест создания заявки утверждает состав отправленных полей.
- [x] 3.5 Показать кампанию в административном списке заявок (`apps/web/src/features/curator/`). Проверка: тест сценария «Административный список группируется по кампании».

## 4. Дозапись идентификатора браузера

- [x] 4.1 Реализовать дозапись идентификатора по токену заявки в `apps/api/internal/modules/leads/service.go` рядом с `UpdateStep` (строка 109). Проверка: тесты сценариев «Идентификатор пришёл после создания заявки», «Дозапись по чужому токену», «Повторная дозапись».
- [x] 4.2 Зарегистрировать публичный маршрут дозаписи в `apps/api/internal/router/leads.go` рядом с `POST /public/leads/step` (строка 21) и перегенерировать `routes.golden`. Проверка: маршрут без идентификатора в пути, поэтому записи в `protectedRoutes` не требует — убедиться в этом, прочитав `internal/router/authorization_matrix_test.go`; затем `UPDATE_GOLDEN=1 go test ./internal/router/`, диффы просмотрены, `go test ./internal/router/` проходит.
- [x] 4.3 Запрашивать идентификатор у счётчика после создания заявки, не блокируя её сохранение. Проверка: тест сценария «Идентификатор не пришёл никогда» — заявка сохранена, обратный вызов не состоялся, ошибки нет.
- [x] 4.4 Добавить миграцию `079_user_attribution`: источник, привязанный к пользователю и переживающий удаление заявки. Проверка: миграция применяется и откатывается на чистой базе.
- [x] 4.4а Копировать источник в `user_attribution` при переносе заявки, до её удаления. Проверка: тесты сценариев «Заявка перенесена в профиль», «Заявка без идентификатора браузера», «Повторный перенос», «Регистрация без заявки».
- [x] 4.5 Убедиться, что новый путь API объявлен для проверки контракта. Проверка: `node scripts/check-api-contract.mjs` проходит.

## 5. Передача конверсий

- [x] 5.1 Добавить миграцию `080_conversion_uploads`: очередь конверсий с отметкой об отправке и уникальностью по событию и заявке. Проверка: миграция применяется и откатывается; повторная вставка той же конверсии отклоняется базой.
- [x] 5.2 Создать модуль `apps/api/internal/modules/metrika/` с постановкой конверсии в очередь и загрузкой в счётчик. Проверка: тесты сценариев «Конверсия поставлена в очередь», «Идентификатор браузера отсутствует».
- [x] 5.3 Ставить конверсию в очередь при событиях-фактах из согласованного набора. Проверка: тест сценария «Событие-факт не ждёт отправки» — обработчик не обращается к внешнему сервису.
- [x] 5.4 Зарегистрировать задачу `metrika.upload-conversions` в `apps/api/internal/jobsetup/setup.go` рядом с `leads.send-reminders` (строка 246), интервал час, с обязательным таймаутом. Проверка: `go test ./internal/shared/jobs/` проходит; задача видна в реестре.
- [x] 5.5 Отмечать отправленные конверсии. Проверка: тесты сценариев «Конверсия отправлена» и «Повторный запуск не задваивает».
- [x] 5.6 Оставлять конверсии в очереди при ошибке внешнего сервиса. Проверка: тест сценария «Внешний сервис недоступен».
- [x] 5.7 Добавить способность `ads_attribution` в `Features`, `Disabled` и `Map` (`apps/api/internal/config/config.go:32-51` и `410-440`) и не регистрировать задачу без доступа. Проверка: тесты сценариев «Доступ не настроен» и «Доступ настроен»; `go test ./internal/config/` проходит.
- [x] 5.8 Объявить переменные токена и номера счётчика в `apps/api/.env.example` как необязательные. Проверка: `go test ./internal/config/` — `env_example_test.go` не падает.
- [x] 5.9 Закрыть состав передаваемых наружу данных тестом. Проверка: тесты сценариев «Запрещённое поле в передаче» и «Состав объявлен»; список запрещённых берётся из `analytics.IsForbidden`, а не дублируется.

## 6. Конфигурация целей счётчика

- [x] 6.1 Написать `scripts/metrika-goals.mjs`, сверяющий цели счётчика с белым списком. Проверка: тест сценария «Запуск без изменений» — цели счётчика не изменились, расхождение выведено.
- [x] 6.2 Реализовать применение изменений по явному указанию, с признаком ретаргетинга. Проверка: тесты сценариев «Применение изменений» и «Повторное применение».
- [x] 6.3 Отказывать внятно без токена. Проверка: тест сценария «Доступ не настроен» для скрипта — обращения к сети не происходит.
- [x] 6.4 Применить скрипт к счётчику и сверить результат. Проверка: `list_goals` показывает пять своих целей с включённым признаком ретаргетинга вместо пяти автоцелей без него.

## 7. Проверка целиком

- [x] 7.1 Прогнать фронтенд и бэкенд полностью. Проверка: `cd apps/web && npx jest` и `cd apps/api && go test ./...` проходят; пороги покрытия из `apps/web/jest.config.js` не снижены.
- [x] 7.2 Пройти путь гостя в браузере через `dev-proxy` на 3070 с метками кампании в адресе. Проверка: заявка в базе содержит метки и идентификатор браузера; в счётчике достигнуты цели начала мастера, показа результата и сохранения заявки.
- [x] 7.3 Проверить путь с включённым блокировщиком рекламы. Проверка: заявка сохранена без идентификатора браузера, события в своей базе на месте, ошибок в консоли нет.
- [ ] 7.4 Проверить передачу конверсии регистрации от начала до конца на dev. Проверка: через два часа после загрузки конверсия видна в отчётах счётчика — по документации данные появляются с этой задержкой, поэтому проверять не раньше.
- [x] 7.5 Зафиксировать, что ретаргетинг не включается этим изменением. Проверка: в PR записано, что сегмент в Яндекс.Аудиториях требует не менее ста записей, а за последние 30 дней счётчик записал 349 посетителей, из которых 364 визита из 368 — прямые; создание сегментов и кампаний остаётся отдельным решением с бюджетом.
- [x] 7.6 Прогнать проверки целостности. Проверка: `node scripts/check-codebase-integrity.mjs` проходит — новых `NEXT_PUBLIC_*` не добавлено, токен доступа объявлен без публичного префикса.
