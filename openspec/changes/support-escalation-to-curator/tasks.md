## 1. Хранение и стирание

- [x] 1.1 Миграция: `telegram_links(user_id PK → users, chat_id BIGINT NOT NULL UNIQUE, username TEXT, linked_at)`, `telegram_link_tickets(token_hash PK, user_id → users, expires_at, used_at, created_at)`, `support_topics(client_id PK → users, thread_id BIGINT NOT NULL, created_at)`, `support_conversations.answered_at`. Проверка: накатывается и откатывается на чистой базе.
- [x] 1.2 Стратегии стирания для трёх новых таблиц (`StrategyDelete`). Проверка: убедиться, что без стратегии `TestErasureCoversSchema` падает — иначе защита вакуумная.
- [x] 1.3 Дополнить `TestErasureLeavesNoTraceInTheTablesAddedLast` новыми таблицами. Проверка: абляция снятой стратегии даёт «остались строки стёртого человека».
- [x] 1.4 Обновить `testdata/schema.golden`. Проверка: `UPDATE_GOLDEN=1 go test ./internal/shared/database/`, в диффе только новое.

## 2. Билет привязки

- [x] 2.1 Выпуск: 32 случайных байта, base64url, в базе хэш. Проверка: тест на формат — умещается в 64 символа и алфавит `A-Za-z0-9_-`.
- [x] 2.2 Погашение одной операцией по образцу `RedeemWSTicket`. Проверка: тест на блокировке строки — погашение ждёт, а не проходит мимо. **Ограничение записано в тесте:** отличить от разнесённых проверки и пометки не удалось, абляция проходит; форма выбрана по образцу, а не по тесту.
- [x] 2.3 Неизвестный, просроченный и использованный отвечают одинаково, через `apperrors.ErrTokenInvalid`. Проверка: `errors.Is`, не сравнение текста.
- [ ] 2.4 `/start <билет>` перехватывается **до** логики поддержки. Проверка: обращение не создаётся, модель не вызывается, дневной лимит не тратится — тест на каждое.
- [x] 2.5 Повторная привязка того же `chat_id` к другой учётной записи отклоняется. Проверка: интеграционный тест на `UNIQUE(chat_id)`.

## 3. Профиль: подключение и отвязка

- [x] 3.1 Ручка выдачи ссылки привязки. Проверка: маршрут в `protectedRoutes`, `go test ./internal/router/`.
- [x] 3.2 Ручка отвязки. Проверка: после неё привязки нет, остальные каналы работают.
- [x] 3.3 Состояние привязки — по наличию `chat_id`, **не** по `telegram_username`. Проверка: тест с заполненным username и без привязки показывает «не подключено».
- [x] 3.4 Экран профиля: кнопка, состояние, отвязка. Проверка: Jest на три состояния.
- [x] 3.5 `node scripts/check-api-contract.mjs`.

## 4. Группа и темы

- [x] 4.1 Идентификатор группы в конфигурации; без него мост выключен, а не сломан. Проверка: `GET /health` показывает возможность выключенной, старт пишет `WARN`.
- [x] 4.2 Создание темы на клиента при первом сообщении, запись в `support_topics`. Проверка: повторное сообщение темы не плодит.
- [ ] 4.3 Проверка прав бота в группе в ежечасном опросе возможностей. Проверка: отняв право писать, увидеть возможность сломанной — иначе отказ молчит, как молчал вебхук.
- [ ] 4.4 Закрытие темы при стирании аккаунта; отказ Telegram не отменяет стирания. Проверка: интеграционный тест на обе ветки.
- [x] 4.5 Имя темы позволяет узнать клиента, но не несёт показателей. Проверка: тест на содержимое имени.

## 5. Мост: сообщения в обе стороны

- [ ] 5.1 Сообщение клиента из бота → в тему с пометкой `[telegram]`. Проверка: интеграционный тест.
- [ ] 5.2 Сообщение клиента из чата платформы → в ту же тему с пометкой `[приложение]`. Проверка: оба канала в одной теме.
- [ ] 5.3 Ответ реплаем уходит в канал исходного сообщения. Проверка: два теста — реплай на сообщение из приложения и на сообщение из Telegram.
- [ ] 5.4 Ответ без реплая уходит в канал последнего сообщения клиента. Проверка: тест на смену канала между сообщениями.
- [ ] 5.5 Ответ не дублируется в оба канала. Проверка: тест считает отправки.
- [ ] 5.6 Ответ из Telegram сохраняется в переписке платформы с куратором как автором. Проверка: строка в `messages`, видна в приложении.
- [ ] 5.7 Показатели в тему не уезжают — только ссылка на карточку. Проверка: тест на отсутствие значений в тексте.

## 6. Медиа

- [ ] 6.1 Приём фотографий и файлов от клиента: `getFile`, загрузка, тип **по байтам**, S3 через `chat_attachments`. Проверка: годный PNG принимается, HTML под видом картинки отклоняется.
- [ ] 6.2 Вложение появляется и в теме, и в переписке платформы (`type = 'image'`/`'file'`). Проверка: интеграционный тест.
- [ ] 6.3 Отправка фотографии куратором из темы клиенту. Проверка: уходит в канал, которому адресован ответ.
- [ ] 6.4 Хранилище выключено → внятный отказ клиенту, не молчание. Проверка: тест на ответ при отсутствии S3.
- [ ] 6.5 Снимок с iPhone (HEIC) получает объяснение, а не «неподдерживаемый тип». Проверка: как в наборе загрузок.

## 7. Маршрутизация и поднятие

- [ ] 7.1 Получатель эскалации — активный куратор клиента. Проверка: интеграционный тест, у суперадминов уведомления нет.
- [ ] 7.2 Запасной путь на суперадминов: обращение без `user_id`, клиент без куратора, куратора нет в группе. Проверка: три теста.
- [ ] 7.3 Выбор в момент эскалации, а не при создании обращения. Проверка: тест со сменой куратора между первым вопросом и эскалацией.
- [ ] 7.4 `answered_at` ставится по отправленному ответу, не по просмотру. Проверка: просмотр очереди поля не меняет.
- [ ] 7.5 Работа поднимает обращение без ответа на суперадминов **один раз**, порог настраиваемый, по умолчанию **30 минут**. Проверка: тест на однократность и на то, что клиенту ничего не пишется.
- [ ] 7.6 Явный вызов человека: команда и кнопка. Проверка: срабатывает, бот не пытается ответить сам.
- [ ] 7.7 Сузить `wantsHuman`: «поддержка форматов» больше не зовёт оператора. Проверка: тест на этот вопрос — сейчас он падает.

## 8. Уведомления

- [ ] 8.1 Telegram поверх обычных каналов при наличии привязки. Проверка: уведомление создано и сообщение отправлено.
- [ ] 8.2 Отказ Telegram не отменяет уведомление. Проверка: подставить отказ, строка уведомления всё равно есть, в журнале запись.
- [ ] 8.3 Не добавлять `'telegram'` в `CHECK (channel IN ...)`. Проверка: миграций, меняющих ограничение, нет.

## 9. Документация

- [ ] 9.1 `docs/user-guide/`: подключение Telegram, что приходит, как отвязать, почему бот не пишет первым. Проверка: вопросы покрыты, бот сможет по ним отвечать.
- [ ] 9.2 **`docs/user-guide/` про удаление: описать то, что происходит.** Удаление чистит платформу и не затрагивает переписку в Telegram. То же — в тексте согласия на обработку данных. Проверка: утверждение совпадает с поведением работы стирания.
- [ ] 9.3 `make sync-knowledge`, `TestKnowledgeMatchesUserGuide` зелёный.
- [ ] 9.4 `docs/curator-guide/11-бот-поддержки.md`: новая маршрутизация, работа в группе, ответ реплаем, медиа. Сейчас там написано, что очередь разбирает администратор.
- [ ] 9.5 `docs/operations/`: создание группы, права бота, идентификатор в окружении, порог поднятия, что проверять при молчании моста.
- [ ] 9.6 Сверить главы с кодом: ни одна не обещает того, чего нет.

## 10. Проверка целиком

- [ ] 10.1 `scripts/ci-local.sh` зелёный.
- [ ] 10.2 Абляции: снятая стратегия стирания, снятый перехват `/start`, возвращённый запрос всех суперадминов, снятая проверка прав бота. Каждая обязана уронить свой тест.
- [ ] 10.3 Проверка на dev живым обращением: клиент с куратором → тема и уведомление куратору; ответ реплаем → доходит в нужный канал; фото в обе стороны; обращение без ответа → поднялось.
- [ ] 10.4 Выкатка на прод и проверка там же. `APP_VERSION` задать **до** слияния.
