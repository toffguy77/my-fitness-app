## 1. Хранение и стирание

- [ ] 1.1 Миграция: `telegram_links(user_id PK → users, chat_id BIGINT NOT NULL UNIQUE, username TEXT, linked_at)` и `telegram_link_tickets(token_hash PK, user_id → users, expires_at, used_at, created_at)`. Проверка: миграция накатывается и откатывается на чистой базе.
- [ ] 1.2 Стратегии стирания для обеих таблиц (`StrategyDelete`). Проверка: `TestErasureCoversSchema` зелёный — без стратегии он обязан падать, убедиться, что падает.
- [ ] 1.3 Дополнить `TestErasureLeavesNoTraceInTheTablesAddedLast` двумя новыми таблицами. Проверка: абляция снятой стратегии даёт «остались строки стёртого человека».
- [ ] 1.4 Обновить `testdata/schema.golden`. Проверка: `UPDATE_GOLDEN=1 go test ./internal/shared/database/`, диff содержит только две новые таблицы.

## 2. Билет привязки

- [ ] 2.1 Выпуск билета: 32 случайных байта, base64url, в базе хэш. Проверка: значение умещается в 64 символа и проходит по алфавиту Telegram (`A-Za-z0-9_-`) — тест на формат.
- [ ] 2.2 Погашение одной операцией: `UPDATE ... SET used_at = NOW() WHERE token_hash = $1 AND used_at IS NULL AND expires_at > NOW() RETURNING user_id`, по образцу `RedeemWSTicket`. Проверка: интеграционный тест на живой базе — два одновременных погашения, успешное ровно одно.
- [ ] 2.3 Неизвестный, просроченный и использованный билеты отвечают одинаково, через `apperrors.ErrTokenInvalid`. Проверка: `errors.Is`, а не сравнение текста.
- [ ] 2.4 Удаление просроченных билетов по мере накопления, как у `ws_tickets`. Проверка: таблица не растёт после погашений.

## 3. Привязка в боте

- [ ] 3.1 Перехват `/start <билет>` в вебхуке **до** логики поддержки. Проверка: обращение в поддержку не создаётся, модель не вызывается, дневной лимит не тратится — тест на каждое из трёх.
- [ ] 3.2 Сохранение `chat_id` и `username` в `telegram_links`; повторная привязка того же `chat_id` к другой учётной записи отклоняется. Проверка: интеграционный тест на нарушение `UNIQUE(chat_id)`.
- [ ] 3.3 Подтверждение привязки сообщением; отдельный ответ на недействительную ссылку. Проверка: оба текста в словаре сообщений бота, не в коде.
- [ ] 3.4 `/start` без параметра остаётся обычным приветствием. Проверка: поведение не изменилось — существующий тест.

## 4. Профиль: подключение и отвязка

- [ ] 4.1 Ручка выдачи ссылки привязки для текущего пользователя. Проверка: маршрут зарегистрирован и внесён в `protectedRoutes`, `go test ./internal/router/`.
- [ ] 4.2 Ручка отвязки. Проверка: после неё `telegram_links` пуста для этого человека, остальные каналы работают.
- [ ] 4.3 Состояние привязки в ответе профиля — по наличию `chat_id`, **не** по `telegram_username`. Проверка: тест с заполненным username и без привязки показывает «не подключено».
- [ ] 4.4 Экран профиля: кнопка подключения, состояние, отвязка. Проверка: Jest на три состояния.
- [ ] 4.5 Проверить контракт фронт↔бэк. Проверка: `node scripts/check-api-contract.mjs`.

## 5. Доставка в Telegram

- [ ] 5.1 Отправка сообщения в Telegram поверх обычных каналов при создании уведомления, если привязка есть. Проверка: интеграционный тест — уведомление создано и сообщение отправлено.
- [ ] 5.2 Отказ Telegram не отменяет уведомление. Проверка: подставить отказ отправки, убедиться, что строка уведомления всё равно создана и в журнале есть запись.
- [ ] 5.3 Не добавлять `'telegram'` в `CHECK (channel IN ...)`. Проверка: миграций, меняющих это ограничение, нет.
- [ ] 5.4 Заблокированный бот не приводит к повторам. Проверка: одна попытка, запись в журнал.

## 6. Выбор получателя эскалации

- [ ] 6.1 Заменить запрос всех суперадминов на: активный куратор клиента по `curator_client_relationships.status = 'active'`. Проверка: интеграционный тест — уведомление у куратора, у суперадминов нет.
- [ ] 6.2 Запасной путь на действующих суперадминов: обращение без `user_id` и клиент без активной связи. Проверка: два теста, по одному на случай.
- [ ] 6.3 Выбор происходит в момент эскалации, а не при создании обращения. Проверка: тест со сменой куратора между первым вопросом и эскалацией.
- [ ] 6.4 Некого уведомить — предупреждение в журнал с идентификатором обращения. Проверка: существующее поведение сохранено, тест на отсутствие получателей.
- [ ] 6.5 Повторные вопросы в уже переданном обращении не поднимают новое уведомление. Проверка: существующий тест на однократность не сломан.

## 7. Документация

- [ ] 7.1 `docs/user-guide/`: как подключить Telegram в профиле и что после этого приходит. Проверка: описан и путь подключения, и отвязка; текст отвечает на вопрос «почему бот не пишет мне сам».
- [ ] 7.2 `make sync-knowledge` и проверка `TestKnowledgeMatchesUserGuide`. Проверка: встроенная копия совпадает с руководством.
- [ ] 7.3 `docs/curator-guide/11-бот-поддержки.md`: сейчас там написано, что очередь разбирает администратор. Переписать на новую маршрутизацию и добавить подключение Telegram. Проверка: утверждения главы совпадают с кодом.
- [ ] 7.4 `docs/operations/руководство-администратора.md`: суперадмин теперь запасной адресат, а не основной; как это проверить и что делать, если куратор не отвечает.
- [ ] 7.5 Проверить, что ни одна глава не обещает того, чего нет: очередь `/admin/support` остаётся за суперадмином, куратор получает уведомление, а не новый экран.

## 8. Проверка целиком

- [ ] 8.1 `scripts/ci-local.sh` зелёный.
- [ ] 8.2 Абляции на трёх главных утверждениях: снятая стратегия стирания, снятый перехват `/start`, возвращённый запрос всех суперадминов. Каждая обязана уронить свой тест.
- [ ] 8.3 Проверить на dev живым обращением: клиент с куратором → уведомление куратору; обращение до регистрации → суперадминам.
- [ ] 8.4 Выкатка на прод и проверка там же. Проверка: `/ready` отвечает, `APP_VERSION` обновлена.
