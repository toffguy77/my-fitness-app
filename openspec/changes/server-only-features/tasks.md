## 1. Избранное

- [ ] 1.1 Добавить в слой API поиска еды добавление и снятие избранного (`POST` и `DELETE /api/v1/food-tracker/favorites/:foodId`). Проверка: `node scripts/check-api-contract.mjs` видит оба пути.
- [ ] 1.2 Научить `useFoodSearch` менять избранное и обновлять список после ответа сервера. Проверка: тесты сценариев «Продукт добавлен» и «Продукт снят».
- [ ] 1.3 Обработать отказы, не оставляя отметку обещанием. Проверка: тесты сценариев «Добавить не удалось» и «Снять не удалось».
- [ ] 1.4 Добавить отметку в интерфейс поиска. Проверка: отметка доступна с клавиатуры и названа для чтения с экрана.
- [ ] 1.5 Объяснить пустой раздел. Проверка: тесты сценариев «Избранного ещё нет» и «Избранное есть».
- [ ] 1.6 Ключи текстов — через словарь. Проверка: `node scripts/check-i18n.mjs` проходит.

## 2. Загрузка статьи файлом

- [ ] 2.1 **Блокирует остальное в этом разделе.** Принять решение: возможность появляется в редакторе или маршрут снимается. Проверка: решение записано в предложении вместо раздела «Открытое решение».
- [ ] 2.2 **Если появляется:** форма загрузки в редакторе, проверка типа и размера файла, разбор ответа. Проверка: статья, загруженная файлом, открывается в редакторе; отказ по типу и размеру объяснён.
- [ ] 2.3 **Если снимается:** убрать маршрут, обработчик, запись в `protectedRoutes` и перегенерировать `routes.golden`. Проверка: `go test ./internal/router/` проходит, golden-диff просмотрен.

## 3. Реестр

- [ ] 3.1 Снять из `scripts/uncalled-routes.json` записи о маршрутах, которые начали звать. Проверка: `node scripts/check-api-contract.mjs` проходит и не сообщает об устаревших записях.

## 4. Проверка целиком

- [ ] 4.1 Прогнать всё. Проверка: `npx jest`, `npm run type-check`, `npm run lint`, `go test ./...` и скрипты целостности проходят.
- [ ] 4.2 Добавить продукт в избранное в браузере через `dev-proxy` на 3070 и увидеть его в разделе. Проверка: раздел, который был всегда пуст, перестал быть пустым.
