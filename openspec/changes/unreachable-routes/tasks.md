## 1. Подготовка

- [x] 1.1 Зафиксировать разбор всех восьми маршрутов без вызова: для каждого — законен он или дефект, и почему. Проверка: таблица приложена к PR; пять дефектов совпадают с тем, что закрывает это изменение, три законных совпадают с записями будущего реестра.
- [x] 1.2 Снять настоящий ответ `GET /api/v1/food-tracker/recommendations` с базы dev. Проверка: снят вызовом сервиса напрямую для пользователя с 47 записями о еде — ответ оказался пустым по всем категориям, потому что справочник нутриентов не заполнен ни в одной среде. Тест преобразования строится на нём и на составленном вручную непустом образце; про второй в тесте сказано, что он придуман.

## 2. Признак отслеживания в ответе сервера

- [x] 2.1 Добавить признак отслеживания в `NutrientRecommendationWithProgress` (`types.go:795`) и перестать выбрасывать невыключенные нутриенты из ответа: `if !isTracked { continue }` в дневном разборе и фильтр в SQL недельного запроса. Отбор для показа переходит на клиент. Проверка: тесты сценариев «Нутриент выключен из отслеживания» и «Настройки не менялись» на настоящей базе.
- [x] 2.2 Исправить `UpdateNutrientPreferences`: сохранять состояние по всем нутриентам справочника, а не только по тем, у кого уже есть строка. Сейчас первая снятая галочка не сохраняется — сбрасывать нечего, а умолчание считает нутриент отслеживаемым. Проверка: тест сценария «Галочка снята впервые» — падает до исправления.

## 3. Слой API рекомендаций

- [x] 3.1 Завести `apps/web/src/features/food-tracker/api/recommendationsApi.ts` с четырьмя вызовами. Проверка: `node scripts/check-api-contract.mjs` видит все четыре пути.
- [x] 3.2 Реализовать преобразование ответа сервера в состав пропсов вкладки: развернуть группировку, согласовать имена полей. Проверка: тест сценария «Группировка разворачивается» на образце из задачи 1.2.
- [x] 3.3 Сделать `currentIntake` необязательным у своей рекомендации и не подставлять ноль. Проверка: тест сценария «Потребление неизвестно» — в преобразованных данных поля нет, а не ноль.
- [x] 3.4 Завести тест, падающий при расхождении с ответом сервера. Проверка: тест сценария «Ответ сервера изменился» — удаление поля из образца роняет тест.

## 4. Хук данных и подключение вкладки

- [x] 4.1 Завести хук загрузки рекомендаций с состояниями загрузки и ошибки. Проверка: тесты сценариев «Данные ещё загружаются» и «Запрос не удался».
- [x] 4.2 Передать данные во вкладку в `FoodTrackerPage.tsx:115` вместо пустого монтирования. Проверка: тест сценария «Данные получены» — вкладка отрисовывает полученные нутриенты; существующие тесты `RecommendationsTab` продолжают проходить без правок.
- [x] 4.3 Показать осмысленное состояние, когда показывать нечего, и различить два случая: справочник не заполнен и записей о еде нет. Проверка: тесты сценариев «Справочник не заполнен» и «Записей о еде нет» — первое объясняет, что нормы ещё не заведены, второе что день пуст.
- [x] 4.4 Отрисовать своё рекомендацию без прогресса, когда потребление неизвестно. Проверка: тест сценария «Потребление неизвестно» на уровне компонента.

- [x] 4.5 Показать подробности по нутриенту: панель поверх вкладки, вызов `GET /recommendations/:id`. Экрана для этого в проекте нет — есть только тип, генератор и обратный вызов по клику. Проверка: тесты сценариев «Подробности открыты», «Справочник не заполнил описание» и «Подробности не загрузились».

## 5. Настройки и добавление своих

- [x] 5.1 Подключить экран настройки отслеживаемых нутриентов к признаку из задачи 2.1. Проверка: тест сценария «Текущий выбор виден».
- [x] 5.2 Сохранять изменённый набор через `PUT /recommendations/preferences`. Проверка: тесты сценариев «Выбор изменён» и «Сохранение не удалось».
- [x] 5.3 Подключить `AddCustomRecommendationForm` к `POST /recommendations/custom`. Проверка: тесты сценариев «Рекомендация добавлена» и «Данные не прошли проверку».
- [x] 5.4 Убедиться, что ключи текстов идут через словарь. Проверка: `node scripts/check-i18n.mjs` проходит — `features/food-tracker` входит в переведённые разделы.

## 6. Ссылка в рабочую группу

- [x] 6.1 Показать ссылку в профиле тем, у кого Telegram не привязан. Проверка: тесты сценариев «Telegram не привязан» и «Telegram привязан».
- [x] 6.2 Не ломать профиль, когда группа не настроена в окружении — обработчик отвечает `503` по признаку способности. Проверка: тест сценария «Способность выключена».

## 7. Реестр маршрутов

- [x] 7.1 Научить `check-api-contract.mjs` разбирать базовые константы вида `const BASE = '/api/v1/<домен>'`. Проверка: тесты сценариев «Путь собран из базовой константы», «Путь записан без префикса версии», «Параметр в пути»; без разбора констант ложных срабатываний 14 из 22, с разбором — ни одного.
- [x] 7.2 Завести реестр исключений с обязательной причиной у каждой записи. Проверка: тесты сценариев «Маршрут объявлен исключением» и «Запись без причины».
- [x] 7.3 Заполнить реестр тремя законными маршрутами: проверка живости, вебхук Telegram, сводка журналов для `super_admin`. Проверка: у каждого записана причина, по которой из браузера его не зовут.
- [x] 7.4 Ронять проверку на маршруте без вызова и без записи. Проверка: тест сценария «Маршрут не вызывается и не объявлен» — добавление такого маршрута роняет проверку и называет его.
- [x] 7.5 Сообщать об устаревших и лишних записях. Проверка: тесты сценариев «Устаревшая запись» и «Запись о несуществующем маршруте».
- [x] 7.6 Снять узкую проверку маршрутов аналитики из `check-codebase-integrity.mjs` — её заменяет общее правило. Проверка: снятие вызова сшивки по-прежнему роняет сборку, теперь через новый реестр.
- [x] 7.7 Убедиться, что CI зовёт ту же команду. Проверка: тест сценария «Запуск в CI» — `.github/workflows/ci.yml` вызывает существующий скрипт, а не свой вариант.

## 8. Проверка целиком

- [x] 8.1 Прогнать всё. Проверка: `npx jest` (4683), `npm run type-check`, `npm run lint`, `go test ./...`, интеграционные на настоящей базе и три скрипта целостности проходят. `node --test scripts/__tests__/*.test.mjs`: 74 из 77; три отказа — `e2e-db-snapshot-address-guard`, которому нужен `psql` в PATH, к изменению отношения не имеют.
- [x] 8.2 Пройти вкладку рекомендаций в браузере через `dev-proxy` на 3070. Снято в `verification.md`. Проверка **ограничена справочником**: пока `nutrient_recommendations` пуст, увидеть нутриенты негде. Проверяется, что запрос уходит, ответ разбирается без ошибок и вкладка объясняет пустоту. Полная проверка — после `nutrient-catalogue`.
- [x] 8.3 Проверить, что до изменения вкладка была пуста, а после — нет. Снято в `verification.md`: до — `<RecommendationsTab />` без пропсов и без запроса, после — `GET /recommendations -> 200` и названная причина пустоты. Проверка: скриншоты или описание до и после приложены к PR; иначе «соединили» останется утверждением без подтверждения.
- [x] 8.4 Сверить номера миграций с целевой базой, если понадобится миграция. Миграция не понадобилась: признак отслеживания уже читался запросом, а изменение настроек — правка SQL, а не схемы. Проверка: мигратор сверяет только номер, и занятый номер означает молча непримененную миграцию.
