## 1. Проверки перед удалением

- [ ] 1.1 Проверить логи прода за 30 дней на обращения к `/api/v1/nutrition/*`. Проверка: количество запросов равно нулю; результат приложен к PR.
- [x] 1.2 Проверить, какие миграции создавали таблицы для модуля `nutrition` и используются ли они модулем `food-tracker`. Проверка: список таблиц с пометкой «используется / не используется» приложен к PR.
- [x] 1.3 Проверить отсутствие обращений фронтенда: `grep -rn "api/v1/nutrition" apps/web/src` пусто. Проверка: команда возвращает пустой вывод.

## 2. Удаление модуля питания

- [x] 2.1 Удалить директорию `apps/api/internal/modules/nutrition/` целиком. Проверка: `go build ./...` проходит.
- [x] 2.2 Снять регистрацию группы `/nutrition` (`cmd/server/main.go:302-312` или соответствующий файл в `internal/router`). Проверка: golden-снимок маршрутов обновлён, `GET /api/v1/nutrition/entries` возвращает `404` в интеграционном тесте.
- [x] 2.3 Убрать модуль `nutrition` из списка в `CLAUDE.md`. Проверка: ревью PR.

## 3. Конфигурации сборки

- [x] 3.1 Включить `reactCompiler: true` в `apps/web/next.config.ts`, убрать устаревший комментарий. Проверка: `npm run build:web` проходит, `npx jest` в `apps/web` зелёные.
- [x] 3.2 ~~Если 3.1 не проходит~~ **Не применима:** задача 3.1 прошла — `reactCompiler: true` собирается и тесты зелёные, поэтому откатывать флаг и удалять `babel-plugin-react-compiler` не нужно. Если 3.1 не проходит — вернуть `false`, удалить `babel-plugin-react-compiler` из `devDependencies` и зафиксировать причину в `CLAUDE.md`. Проверка: сборка и тесты зелёные, документация соответствует.
- [x] 3.3 Удалить корневые `next.config.ts`, `jest.config.js`, `jest.setup.js`, `postcss.config.mjs`, `next-env.d.ts`; удалить `tsconfig.tsbuildinfo` и добавить его в `.gitignore`. Проверка: `npm run build:web`, `npm run type-check`, `npx jest` из корня и из `apps/web` проходят.
- [x] 3.4 Привести раздел «Key Technical Details» в `CLAUDE.md` в соответствие с фактическим значением флага React Compiler. Проверка: сценарий спека «Согласованность документации и конфигурации».

## 4. Сведение WebSocket-клиента

- [x] 4.1 Определить всех потребителей `useWebSocket` и `WebSocketProvider`. Проверка: список приложен к PR.
- [x] 4.2 Перевести потребителей `useWebSocket` на контекст `WebSocketProvider`; перенести в провайдер недостающую логику из хука. Проверка: `npx jest chat` зелёные.
- [x] 4.3 Удалить `apps/web/src/features/chat/hooks/useWebSocket.ts` и его тесты. Проверка: `grep -rn "new WebSocket(" apps/web/src` находит не более одного места.
- [ ] 4.4 Вручную проверить чат на dev: отправка, получение, индикатор «печатает», переподключение после разрыва. Проверка: чек-лист пройден.

## 5. Переменные окружения

- [x] 5.1 Удалить из `apps/web/.env.local` неиспользуемые `NEXT_PUBLIC_ENABLE_OCR`, `NEXT_PUBLIC_ENABLE_PREMIUM`, `NEXT_PUBLIC_ENABLE_CHAT`, `NEXT_PUBLIC_OPENROUTER_API_KEY`, `NEXT_PUBLIC_SUPABASE_*`, а также `RESEND_*`, если Resend не используется. Проверка: `npm run build:web` проходит.
- [x] 5.2 Оставить `NEXT_PUBLIC_SENTRY_DSN` и `NEXT_PUBLIC_GA_ID` только если они будут использованы изменениями `observability-baseline` и `product-analytics-events`; иначе удалить. Проверка: ревью PR со ссылкой на соответствующее изменение.

## 6. Проверка целостности в CI

- [x] 6.1 Написать `scripts/check-codebase-integrity.mjs` с тремя правилами: неиспользуемые `NEXT_PUBLIC_*`; секретоподобные имена с публичным префиксом; более одного `next.config.*`. Проверка: скрипт падает на намеренно добавленной неиспользуемой переменной.
- [x] 6.2 Добавить в скрипт правило про `TODO: Implement` в сервисах модулей с зарегистрированными маршрутами. Проверка: скрипт падает на намеренно добавленной заглушке.
- [x] 6.3 Подключить скрипт шагом в `.github/workflows/ci.yml` без `continue-on-error`. Проверка: CI падает на тестовой ветке с намеренным нарушением и проходит на чистой.
