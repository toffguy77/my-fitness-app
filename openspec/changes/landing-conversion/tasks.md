## 1. Подготовка

- [ ] 1.1 Снять базовые значения воронки до изменения: посещения посадочной, переходы в мастер, сохранённые заявки, регистрации. Проверка: значения и запрос к `analytics_events` приложены к PR.
- [ ] 1.2 Согласовать тексты четырёх утверждений посадочной страницы, текст согласия на экране результата и состав согласий в форме запроса ссылки входа. Проверка: тексты приложены к PR; согласие называет параметры тела и упоминает сведения о здоровье, как требует `consentWording.test.ts`.
- [ ] 1.3 Определить, откуда фронтенд узнаёт о включённых способностях для условного показа утверждения про фотографии еды. Проверка: источник назван, и это не константа в коде.

## 2. Вход по одноразовой ссылке — бэкенд

- [ ] 2.1 Добавить миграцию: таблица одноразовых ссылок входа (хэш токена, владелец или адрес, срок, отметка погашения) и допущение пользователя без хэша пароля. Проверка: `go test ./internal/shared/database/` проходит, миграция применяется и откатывается на чистой базе.
- [ ] 2.2 Реализовать выдачу ссылки с одинаковым ответом для существующего и несуществующего адреса. Проверка: тесты сценариев «Аккаунт существует», «Аккаунта не существует» — ответы побайтово совпадают.
- [ ] 2.3 Реализовать погашение ссылки: выдача сессии, одноразовость, срок пятнадцать минут. Проверка: тесты сценариев «Переход по действующей ссылке», «Повторный переход», «Истёкшая ссылка», «Подделанная ссылка».
- [ ] 2.4 Создавать аккаунт с подтверждённым адресом при переходе по ссылке для адреса без аккаунта. Проверка: тесты сценариев «Первый вход создаёт аккаунт» и что письмо подтверждения адреса не отправляется.
- [ ] 2.4а Собирать согласия при запросе ссылки и записывать их в `user_consents` при создании аккаунта тем же кодом, что и обычная регистрация. Проверка: тесты сценариев «Согласия записаны при создании аккаунта», «Обязательные согласия не даны», «Вход в существующий аккаунт не требует согласий заново», «Согласия из заявки не дублируются».
- [ ] 2.5 Переносить заявку при погашении ссылки через существующий `leads.ClaimInto`. Проверка: тест сценария «Перенос сохранённой заявки» — рост и вес оказываются в профиле.
- [ ] 2.6 Сделать вход по паролю в беспарольный аккаунт неотличимым от неверного пароля. Проверка: тест сценария «Вход по паролю в беспарольный аккаунт» сравнивает код, тело и наличие задержки.
- [ ] 2.7 Ограничить частоту запросов ссылки по адресу и по почтовому ящику. Проверка: тест сценария «Превышение частоты запросов».
- [ ] 2.8 Отвечать отказом о недоступности способности, когда почта выключена. Проверка: тест сценария «Отправка почты недоступна» ожидает `503`.
- [ ] 2.9 Зарегистрировать маршруты в `internal/router/auth.go` и перегенерировать `routes.golden`. Проверка: `UPDATE_GOLDEN=1 go test ./internal/router/`, диффы просмотрены, `go test ./internal/router/` проходит.

## 3. Вход по одноразовой ссылке — письмо и фронтенд

- [ ] 3.1 Добавить шаблон письма со ссылкой входа, различающий вход и создание аккаунта. Проверка: тест рендеринга обоих вариантов; ссылка отписки в письме входа отсутствует намеренно — это транзакционное письмо.
- [ ] 3.2 Сделать вход по ссылке основным способом на `AuthScreen`, оставив пароль вторым вариантом. Проверка: тесты сценариев «Оба действия в шапке» и тест, что форма пароля доступна без перезагрузки страницы.
- [ ] 3.3 Добавить страницу подтверждения перехода по ссылке с понятными состояниями истёкшей и использованной ссылки. Проверка: тесты сценариев «Повторный переход» и «Истёкшая ссылка» на уровне компонента.
- [ ] 3.4 Убедиться, что путь входа по ссылке работает через `dev-proxy` на 3070, а не только на 3069. Проверка: E2E-сценарий входа по ссылке проходит на `http://localhost:3070`.

## 4. Посадочная страница

- [ ] 4.1 Переписать `apps/web/src/app/page.tsx`: четыре утверждения вместо шести карточек возможностей. Проверка: тесты сценариев «Распознавание еды включено» и «Распознавание еды выключено».
- [ ] 4.2 Развести вход и регистрацию в шапке, герое и блоке перед подвалом. Проверка: тесты сценариев «Оба действия в шапке», «Отдельный блок призыва к действию», «Возврат вернувшегося пользователя».
- [ ] 4.3 Разметить блок социального доказательства так, чтобы он не отображался без подтверждённых данных. Проверка: тест сценария «Данных нет».
- [ ] 4.4 Сохранить перенаправление посетителя с действующей сессией. Проверка: тест сценария «Посетитель с действующей сессией».
- [ ] 4.5 Обновить метаданные и разметку `JsonLd` под новое описание. Проверка: `npm run lint` и `npm run type-check` в `apps/web` проходят.

## 5. Захват контакта на экране результата

- [ ] 5.1 Добавить на экран результата `GuestOnboarding` форму сохранения расчёта на почту с раздельными согласиями. Проверка: тесты сценариев «Гость сохраняет расчёт с экрана результата», «Согласие на обработку не дано», «Согласие на связь отдельно».
- [ ] 5.2 Обновлять существующую заявку, а не создавать вторую, если гость доходит до шага контакта. Проверка: тест сценария «Гость доходит до шага контакта после сохранения».
- [ ] 5.3 Сохранять точку захвата контакта в заявке. Проверка: тест сценария «Источник записан».

## 6. Аналитика

- [ ] 6.1 Добавить события в оба словаря: `apps/web/src/shared/analytics/events.ts` и `apps/api/internal/modules/analytics/dictionary.go`. Проверка: тест сценария «Имя объявлено только на клиенте» падает при рассогласовании.
- [ ] 6.2 Отправлять события запроса и погашения ссылки. Проверка: тесты сценариев «Запрос ссылки» и «Погашение ссылки».
- [ ] 6.3 Отправлять событие захвата контакта с источником. Проверка: тесты сценариев «Контакт оставлен на экране результата» и «Контакт оставлен на шаге контакта».

## 7. Проверки целостности и выкатка

- [ ] 7.1 Прогнать проверки контракта и целостности. Проверка: `node scripts/check-api-contract.mjs` и `node scripts/check-codebase-integrity.mjs` проходят.
- [ ] 7.2 Прогнать полный набор тестов. Проверка: `cd apps/api && go test ./...`, `cd apps/web && npx jest`, `npm run test:e2e` — вывод приложен к PR.
- [ ] 7.3 Выкатить на dev и проверить живьём: вход по ссылке новым адресом, вход по ссылке существующим аккаунтом, сохранение расчёта с экрана результата. Проверка: наблюдения приложены к PR, включая факт доставки письма.
- [ ] 7.4 Выкатить на прод и повторить проверку. Проверка: наблюдения приложены к PR.
- [ ] 7.5 Снять значения воронки через две недели после выкатки и сравнить с 1.1. Проверка: сравнение приложено; при малом трафике честно отметить, что выводов делать нельзя.
