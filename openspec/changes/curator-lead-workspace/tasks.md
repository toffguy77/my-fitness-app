## 1. Перенос маршрутов

- [x] 1.1 Перенести группу заявок с `/admin/leads` на `/curator/leads` с ролями `coordinator` и `super_admin`, не помещая её внутрь `/curator/clients/:id`. Проверка: тесты сценариев «Куратор получает доступ», «Супер-администратор сохраняет доступ», «Обычный пользователь», «Без аутентификации».
- [x] 1.2 Перенести группу разговоров с `/admin/support` на `/curator/support` тем же образом. Проверка: тесты сценариев «Куратор разбирает очередь разговоров» и «Обычный пользователь».
- [x] 1.3 Удалить прежние административные пути, не оставляя синонимов. Проверка: тест сценария «Прежний путь удалён».
- [x] 1.4 Обновить ключи в реестре защищаемых маршрутов, сохранив механизм «роль». Проверка: тесты сценариев «Механизм защиты объявлен явно» и «Маршрут отсутствует в реестре»; `go test ./internal/router/` проходит.
- [x] 1.5 Перегенерировать `routes.golden` и просмотреть диффы. Проверка: `UPDATE_GOLDEN=1 go test ./internal/router/`, затем `go test ./internal/router/`.

## 2. Очередь заявок — сервис

- [x] 2.1 Дополнить `leads.List` отбором необработанных и порядком от старых к новым. Проверка: тесты сценариев «Куратор открывает очередь» и «Обработанные заявки скрыты по умолчанию».
- [x] 2.2 Возвращать в записи очереди шаг остановки, возраст, факт напоминания и наличие согласия на связь. Проверка: тест сценария «Отображение записи».
- [x] 2.3 Возвращать признак привязанного разговора. Проверка: тесты сценариев «Связь с разговором» и «Разговора нет».
- [x] 2.4 Сделать повторную отметку об обработке безопасной. Проверка: тест сценария «Заявка уже отмечена» — первая отметка не перезаписана.
- [ ] 2.5 Убедиться, что изменение и удаление заявки недоступны. Проверка: тесты сценариев «Попытка изменения» и «Попытка удаления».

## 3. Очередь заявок — интерфейс

- [x] 3.1 Перенести страницу заявок из `apps/web/src/app/admin/leads/` в `apps/web/src/app/curator/leads/`. Проверка: `node scripts/check-api-contract.mjs` проходит.
- [x] 3.2 Перенести страницу разговоров из `apps/web/src/app/admin/support/` в `apps/web/src/app/curator/support/`. Проверка: `node scripts/check-api-contract.mjs` проходит.
- [ ] 3.3 Переделать список заявок в очередь: основание для разговора, а не таблица контактов. Проверка: тест сценария «Отображение записи» на уровне компонента.
- [ ] 3.4 Пометить заявки без согласия на связь запретом и убрать у них действие связи. Проверка: тесты сценариев «Согласия на связь нет» и «Согласие на связь есть».
- [ ] 3.5 Добавить переход к разговору, если он привязан. Проверка: тесты сценариев «Связь с разговором» и «Разговора нет».
- [x] 3.6 Добавить оба раздела в навигацию кураторского рабочего места. Проверка: `npm run lint` и `npm run type-check` в `apps/web` проходят.

## 4. Ответы в разговорах

- [ ] 4.1 Убедиться, что ответ куратора уходит в канал происхождения разговора. Проверка: тесты сценариев «Куратор отвечает в разговор из Telegram» и «Куратор отвечает в разговор из браузера».
- [ ] 4.2 Убедиться, что автор ответа и закрывший разговор сохраняются. Проверка: тесты сценариев «Автор ответа сохраняется» и «Закрытие разговора».

## 5. Документация куратора

- [x] 5.1 Переписать `docs/curator-guide/10-воронка-и-возвращение-клиентов.md`: что делает автоматика, что делает куратор, как читать очередь, когда писать и когда нельзя, что говорить человеку в зависимости от шага остановки, когда заявка считается обработанной. Проверка: текст приложен к PR; прежнее утверждение об отсутствии экрана заявок удалено — его наличие проверяется поиском по файлу.
- [x] 5.2 Дополнить `docs/curator-guide/11-бот-поддержки.md` тем, что очередь разговоров разбирает куратор и что каналов два. Проверка: текст приложен к PR.
- [x] 5.3 Убедиться, что руководство пользователя не затронуто и синхронизация базы знаний не требуется. Проверка: `git diff --stat docs/user-guide/` пуст; `go test ./internal/modules/support/` проходит.

## 6. Проверки и выкатка

- [x] 6.1 Прогнать проверки контракта и целостности. Проверка: `node scripts/check-api-contract.mjs` и `node scripts/check-codebase-integrity.mjs` проходят.
- [ ] 6.2 Прогнать полный набор тестов. Проверка: `cd apps/api && go test ./...`, `cd apps/web && npx jest`, `npm run test:e2e` — вывод приложен к PR.
- [ ] 6.3 Проверить на dev от лица куратора и от лица обычного пользователя. Проверка: куратор видит обе очереди, обычный пользователь получает отказ на обоих путях; наблюдения приложены к PR.
- [ ] 6.4 Выкатить на прод и повторить проверку обеими ролями. Проверка: наблюдения приложены к PR.
