## 1. Перенос маршрутов

- [x] 1.1 Перенести группу заявок с `/admin/leads` на `/curator/leads` с ролями `coordinator` и `super_admin`, не помещая её внутрь `/curator/clients/:id`. Проверка: тесты сценариев «Куратор получает доступ», «Супер-администратор сохраняет доступ», «Обычный пользователь», «Без аутентификации».
- [x] 1.2 Перенести группу разговоров с `/admin/support` на `/curator/support` тем же образом. Проверка: тесты сценариев «Куратор разбирает очередь разговоров» и «Обычный пользователь».
- [x] 1.3 Удалить прежние административные пути, не оставляя синонимов. Проверка: тест сценария «Прежний путь удалён».
- [x] 1.4 Обновить ключи в реестре защищаемых маршрутов, сохранив механизм «роль». Проверка: тесты сценариев «Механизм защиты объявлен явно» и «Маршрут отсутствует в реестре»; `go test ./internal/router/` проходит.
- [x] 1.5 Перегенерировать `routes.golden` и просмотреть диффы. Проверка: `UPDATE_GOLDEN=1 go test ./internal/router/`, затем `go test ./internal/router/`.

## 2. Очередь заявок — сервис

- [x] 2.1 Дополнить `leads.List` отбором необработанных и порядком от старых к новым. Проверка: тесты сценариев «Куратор открывает очередь» и «Обработанные заявки скрыты по умолчанию».
- [x] 2.2 Возвращать в записи очереди шаг остановки, возраст, факт напоминания и наличие согласия на связь. Проверка: тест сценария «Отображение записи».
- [x] 2.3 Возвращать признак привязанного разговора. Проверка: тесты сценариев «Связь с разговором» и «Разговора нет».
- [x] 2.4 Сделать повторную отметку об обработке безопасной. Проверка: тест сценария «Заявка уже отмечена» — первая отметка не перезаписана.
- [ ] 2.5 Убедиться, что изменение и удаление заявки недоступны. Проверка: тесты сценариев «Попытка изменения» и «Попытка удаления». В `internal/router/leads.go` `registerCuratorLeadRoutes` действительно не регистрирует ни `PUT`, ни `DELETE` на `/curator/leads/:id` — маршрутов для изменения и удаления нет. Но именно теста, подтверждающего отказ на такую попытку, не нашлось (`grep` по `leads` и `router` на «изменения»/«удаления» ничего не даёт) — отсутствие маршрута не то же самое, что проверенный отказ.

## 3. Очередь заявок — интерфейс

- [x] 3.1 Перенести страницу заявок из `apps/web/src/app/admin/leads/` в `apps/web/src/app/curator/leads/`. Проверка: `node scripts/check-api-contract.mjs` проходит.
- [x] 3.2 Перенести страницу разговоров из `apps/web/src/app/admin/support/` в `apps/web/src/app/curator/support/`. Проверка: `node scripts/check-api-contract.mjs` проходит.
- [x] 3.3 Переделать список заявок в очередь: основание для разговора, а не таблица контактов. Проверка: тест сценария «Отображение записи» на уровне компонента. Сделано и отмечено ошибочно как неготовое: `LeadList.tsx` показывает шаг остановки, возраст и параметры вместо таблицы контактов; тест `показывает основание для разговора, а не только контакт` (`LeadList.test.tsx`) зелёный (`cd apps/web && npx jest` — весь набор проходит, 4566 тестов).
- [x] 3.4 Пометить заявки без согласия на связь запретом и убрать у них действие связи. Проверка: тесты сценариев «Согласия на связь нет» и «Согласие на связь есть». Сделано: в `LeadList.tsx` ссылка «Написать» показывается только при `lead.contact_allowed`, иначе — текст-запрет; тесты `помечает запретом заявку без согласия на связь и не предлагает написать` и `предлагает написать заявке с согласием на связь` (`LeadList.test.tsx`) зелёные.
- [x] 3.5 Добавить переход к разговору, если он привязан. Проверка: тесты сценариев «Связь с разговором» и «Разговора нет». Сделано: `LeadList.tsx` показывает ссылку на `/curator/support?conversation=...` при `lead.conversation_id`; тесты `даёт перейти к разговору, если он был` и `не предлагает переход к разговору, когда его нет` (`LeadList.test.tsx`) зелёные.
- [x] 3.6 Добавить оба раздела в навигацию кураторского рабочего места. Проверка: `npm run lint` и `npm run type-check` в `apps/web` проходят.

## 4. Ответы в разговорах

- [x] 4.1 Убедиться, что ответ куратора уходит в канал происхождения разговора. Проверка: тесты сценариев «Куратор отвечает в разговор из Telegram» и «Куратор отвечает в разговор из браузера». Сделано (это же основание уже использовано в `public-support-widget` 2.2 для той же пары тестов): `TestAnswerAsOperatorInTelegramConversationStillSends` и `TestAnswerAsOperatorInWebConversationDoesNotSend` (`apps/api/internal/modules/support/web_test.go`) — `go test ./internal/modules/support/` зелёный.
- [ ] 4.2 Убедиться, что автор ответа и закрывший разговор сохраняются. Проверка: тесты сценариев «Автор ответа сохраняется» и «Закрытие разговора». Код это делает: `Service.answerAs` пишет `operatorID` через `recordMessage`, `Service.Close` пишет `closed_by`. Но теста, который бы прочитал эти поля обратно и сверил, не нашлось — ближайшие (`TestClose_MalformedIDIsRejectedBeforeTheDatabase`, `TestReplyFromAnUnlinkedCuratorIsStillRecorded`) проверяют другое.

## 5. Документация куратора

- [x] 5.1 Переписать `docs/curator-guide/10-воронка-и-возвращение-клиентов.md`: что делает автоматика, что делает куратор, как читать очередь, когда писать и когда нельзя, что говорить человеку в зависимости от шага остановки, когда заявка считается обработанной. Проверка: текст приложен к PR; прежнее утверждение об отсутствии экрана заявок удалено — его наличие проверяется поиском по файлу.
- [x] 5.2 Дополнить `docs/curator-guide/11-бот-поддержки.md` тем, что очередь разговоров разбирает куратор и что каналов два. Проверка: текст приложен к PR.
- [x] 5.3 Убедиться, что руководство пользователя не затронуто и синхронизация базы знаний не требуется. Проверка: `git diff --stat docs/user-guide/` пуст; `go test ./internal/modules/support/` проходит.

## 6. Проверки и выкатка

- [x] 6.1 Прогнать проверки контракта и целостности. Проверка: `node scripts/check-api-contract.mjs` и `node scripts/check-codebase-integrity.mjs` проходят.
- [x] 6.2 Прогнать полный набор тестов. Проверка: `cd apps/api && go test ./...`, `cd apps/web && npx jest`, `npm run test:e2e` — вывод приложен к PR. Перепрогнано заново для этой проверки: `go test ./...` — зелёный (все модули); `npx jest` — 344/345 наборов, 4566/4580 тестов, 14 намеренно пропущенных; `npm run test:e2e` реально прошёл целиком в релизном прогоне (`gh run view 35550770370`, PR #112, 2026-09-21, джоб `Playwright`) — `223 passed, 1 flaky` (flaky — `support-widget.spec.ts`, не относится к этой задаче), включая `curator-leads.spec.ts`.
- [ ] 6.3 Проверить на dev от лица куратора и от лица обычного пользователя. Проверка: куратор видит обе очереди, обычный пользователь получает отказ на обоих путях; наблюдения приложены к PR. Прямых наблюдений с dev не найдено — только автоматический E2E на собранном стенде (см. 6.2), это не то же самое, что ручная проверка на `new.burcev.team`.
- [ ] 6.4 Выкатить на прод и повторить проверку обеими ролями. Проверка: наблюдения приложены к PR. **Частично.** Перепроверено самостоятельно на проде (curl, 2026-09-21, `burcev.team`): `/admin/leads` → `308` на `/curator/leads`, `/api/v1/admin/leads` → `404`, `/api/v1/curator/leads` и `/api/v1/curator/support/conversations` без токена → `401`. Это подтверждает перенос маршрутов и отказ анонимному запросу, но не «куратор видит обе очереди» и не «обычный пользователь получает 403» — эти два требуют вошедших под ролью учётных записей, которых на проде ещё нет.
