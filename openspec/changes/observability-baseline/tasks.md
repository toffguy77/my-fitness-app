## 1. Пробы состояния

- [x] 1.1 Разделить `/health` и `/ready` в API: живость без обращения к зависимостям, готовность с проверкой БД и статусом `503` при недоступности. Проверка: тесты трёх сценариев спека `service-health`.
- [x] 1.2 Перевести маршрутизацию на проксирование `/ready` (фактический прокси — Traefik в Dokploy, а не nginx: маршрут добавлен в `docker-compose.yml`; до этого `/ready` уходил в Next.js и отвечал 404 в обеих средах). Проверка: остановка БД на dev выводит инстанс из-под трафика без перезапуска контейнера.
- [ ] 1.3 Перевести healthcheck в Dokploy: живость на `/health`, готовность на `/ready`. *Настройка в панели Dokploy, не в репозитории: делается руками при выкатке.*

## 2. Метрики

- [x] 2.1 Добавить `prometheus/client_golang`, создать `internal/shared/telemetry/metrics.go` и middleware сбора метрик HTTP с шаблоном маршрута. Проверка: тест сценария «Запросы к маршруту с параметром».
- [x] 2.2 Добавить метрики пула соединений и длительности запросов в `internal/shared/database/postgres.go`. Проверка: `/metrics` содержит серии пула.
- [x] 2.3 Добавить метрики фоновых задач в `internal/shared/jobs/`. Проверка: тест сценария «Провал фоновой задачи».
- [ ] 2.4 *(решение эксплуатации)* Выбрать место сбора метрик (Prometheus рядом в Dokploy либо managed-сборщик) и зафиксировать решение. Проверка: решение с оценкой стоимости приложено к PR.
- [x] 2.5 Закрыть `/metrics` снаружи в nginx. Проверка: тест сценария «Обращение к метрикам извне».
- [x] 2.6 Добавить счётчики доменных событий: регистрации, входы успешные и неуспешные, записи о еде, отправленные сообщения, запросы распознавания, отправленные письма. Проверка: тест сценария «Регистрация пользователя».

## 3. Трассировка и логи

*Частично.* Обязательный набор полей в логах есть, значения секретов маскируются (`secure-token-lifecycle`, задача 5.1). OpenTelemetry и проброс идентификатора трассировки на фронтенд не сделаны.

- [ ] 3.1 Добавить OpenTelemetry, инициализацию в `internal/shared/telemetry/tracing.go`, middleware трассировки с выводом идентификатора из `X-Request-Id`. Проверка: тесты сценариев «Клиент прислал идентификатор запроса» и «Связь записей лога».
- [x] 3.2 Привести `internal/shared/middleware/logger.go` к обязательному набору полей. Проверка: тест сценария «Запись об обработанном запросе».
- [ ] 3.3 Настроить выборку трассировок: все ошибочные запросы, доля успешных. Проверка: конфигурация задаётся переменной окружения, значение по умолчанию задокументировано.
- [ ] 3.4 Прокинуть идентификатор трассировки на фронтенд и добавлять его в записи собственного логгера. Проверка: `npx jest logger api-client` зелёные.

## 4. Отслеживание ошибок

*Не сделано:* требует аккаунта Sentry и DSN. Своя труба клиентских ошибок (`/api/v1/logs`) из `frontend-resilience` работает и не зависит от неё.

- [ ] 4.1 Подключить `getsentry/sentry-go` на бэкенде: паники, версия приложения, идентификатор трассировки. Проверка: тест сценария «Паника на бэкенде».
- [ ] 4.2 Подключить `@sentry/nextjs` на фронтенде с релизом из `NEXT_PUBLIC_APP_VERSION`. Проверка: искусственная ошибка на dev появляется в системе отслеживания.
- [ ] 4.3 Настроить фильтрацию отправляемого: без тел запросов, без содержимого сообщений чата, без значений хранилищ браузера, с маскированием персональных данных. Проверка: тест сценария «Проверка состава отправляемого события», ручная проверка события на dev.
- [ ] 4.4 Убедиться, что собственная труба клиентских логов из `frontend-resilience` продолжает работать независимо. Проверка: при заблокированном домене системы отслеживания запись всё равно доходит до `/api/v1/logs`.

## 5. Дашборд и оповещения

*Не сделано:* требует выбранного места сбора метрик (задача 2.4) и доступа к нему.

- [ ] 5.1 Собрать дашборд: доля `5xx`, задержка по процентилям, запросы по маршрутам, состояние пула БД, результаты фоновых задач, доменные счётчики. Проверка: ссылка на дашборд приложена к PR.
- [ ] 5.2 Настроить оповещения по симптомам из спека с каналом доставки. Проверка: тест сценария «Рост доли ошибок сервера» через искусственный всплеск на dev.
- [ ] 5.3 Задокументировать в `docs/` порядок разбора инцидента: где смотреть, как найти запрос по идентификатору. Проверка: ревью PR.

## 6. Выкатка

- [ ] 6.1 Задеплоить на dev, проверить остановкой БД, что готовность переходит в `503`, а живость остаётся `200`. Проверка: чек-лист пройден.
- [ ] 6.2 Задеплоить на prod, убедиться в поступлении метрик и трассировок. Проверка: дашборд наполняется данными прода.
