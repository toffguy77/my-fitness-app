## 1. Словарь и схема

- [x] 1.1 Составить словарь событий: имена, обязательные и необязательные свойства, источник (клиент или сервер). Покрыть путь от посадочной страницы до первой записи о еде и первого сообщения куратору. Проверка: словарь приложен к PR и согласован.
- [x] 1.2 Объявить словарь в коде в общем месте, доступном фронтенду и бэкенду. Проверка: `npm run type-check` и `go build ./...` проходят.
- [x] 1.3 Написать тест словаря, отклоняющий свойства из запрещённого перечня. Проверка: сценарий «Автоматическая проверка словаря».
- [x] 1.4 Создать миграцию `053_analytics_events_{up,down}.sql` (номер 049 занят) с индексами по времени, имени и идентификатору посетителя, а также таблицей связывания посетителя с пользователем. Проверка: `make db-migrate`, down откатывает чисто.

## 2. Приёмка

- [x] 2.1 Создать модуль `apps/api/internal/modules/analytics/` с `POST /api/v1/analytics/events`, принимающим пакет. Проверка: тесты сценариев корректного и некорректного события.
- [x] 2.2 Реализовать валидацию по словарю и отклонение запрещённых свойств. Проверка: тесты сценариев «Событие вне словаря», «Событие с недостающим обязательным свойством», «Событие с запрещённым свойством».
- [x] 2.3 Добавить ограничение частоты и предел размера пакета. Проверка: тесты сценариев `400` и `429`.
- [x] 2.4 Внести маршрут в реестр из `fix-authorization-gaps` как публичный. Проверка: тест-матрица проходит.

## 3. Клиентский модуль

- [x] 3.1 Создать `apps/web/src/shared/analytics/` с генерацией идентификатора посетителя, накоплением и пакетной отправкой. Проверка: тесты сценариев «Повторный визит с того же устройства» и «Уход со страницы с накопленными событиями».
- [x] 3.2 Инициализировать модуль в корневом layout. Проверка: события с посадочной страницы доходят до приёмки на dev.
- [x] 3.3 Инструментировать посадочную страницу и `features/auth`: просмотры, начало регистрации, шаги формы, ошибки. Проверка: события видны в таблице.
- [x] 3.4 Инструментировать `features/onboarding`: начало, каждый шаг, выход, завершение. Проверка: события покрывают все шаги.
- [~] 3.5 Инструментировать `features/food-tracker` и `features/chat`. *Запись о еде и первая запись — сделано (`store/entriesSlice.ts`). Распознавание и первое сообщение куратору — нет: события объявлены в словаре, точки вызова не расставлены.*

## 4. Серверные события

- [x] 4.1 Отправлять события успешной регистрации, подтверждения почты и входа из `modules/auth`. Проверка: сценарий «Регистрация при потере соединения клиентом».
- [ ] 4.2 Отправлять события назначения куратора и отправки недельного отчёта. *Событие объявлено, отправка из `curator`/`dashboard` не подключена.*
- [x] 4.3 Реализовать связывание идентификатора посетителя с пользователем при успешной аутентификации. Проверка: сценарий «Связывание после регистрации».

## 5. Отчёты

- [x] 5.1 Написать и сохранить в репозитории запрос воронки регистрации. Проверка: сценарий «Построение воронки регистрации».
- [x] 5.2 Написать запросы воронки онбординга, активации за первые сутки и удержания по когортам. Проверка: запросы возвращают осмысленные результаты на данных dev.
- [ ] 5.3 Зафиксировать базовые значения показателей до изменений входа в продукт. Проверка: сценарий «Фиксация базовых значений», значения приложены к задаче.
- [x] 5.4 Зарегистрировать фоновую задачу чистки событий старше срока хранения. Проверка: тест.

## 6. Выкатка

- [ ] 6.1 Задеплоить на dev, пройти путь от посадочной страницы до первой записи о еде, убедиться в полноте воронки. Проверка: все шаги воронки непусты.
- [ ] 6.2 Задеплоить на prod, через неделю снять базовые значения. Проверка: значения зафиксированы.
