## 1. Подготовка

- [ ] 1.1 Снять текущую разбивку предупреждений по правилам и по тому, тесты это или боевой код. Проверка: таблица приложена к PR и совпадает с `npx eslint src -f json` на ветке.
- [ ] 1.2 Выделить случаи, где `any` — следствие устройства самого теста, а не лени. Проверка: список файлов приложен к PR; эти файлы в уборку не входят и выносятся отдельным предложением.

## 2. Неиспользуемое (`no-unused-vars`, 203 случая)

- [ ] 2.1 Удалить неиспользуемые импорты и переменные в тестах. Проверка: `npx jest` проходит полностью; ни одно удаление не задело то, что используется через побочный эффект импорта.
- [ ] 2.2 То же в боевом коде. Проверка: `npm run type-check` и `npx jest` проходят.
- [ ] 2.3 Там, где аргумент обязан остаться по сигнатуре, переименовать в `_`. Проверка: правило считает такие имена намеренными; предупреждений по правилу не остаётся.

## 3. `any` (322 случая)

- [ ] 3.1 Заменить `any` настоящим типом там, где тип известен. Проверка: `npm run type-check` проходит; число предупреждений по правилу уменьшилось на заявленное.
- [ ] 3.2 Где имени типа нет — `unknown` с сужением на месте использования. Проверка: тесты проходят; ни одно сужение не сведено к приведению обратно к `any`.
- [ ] 3.3 Убедиться, что ни один тест не стал слабее. Проверка: для каждого изменённого теста ответ API разобран по форме, а не приведён целиком.

## 4. Зависимости эффектов (`exhaustive-deps`, 18 случаев в боевом коде)

- [ ] 4.1 Разобрать каждый случай отдельно: понять, что эффект синхронизирует. Проверка: для каждого в PR записано, чем он оказался — недостающей зависимостью, лишней или осознанным исключением.
- [ ] 4.2 Исправить недостающие зависимости. Проверка: тест на поведение эффекта для каждого исправленного; `npx jest` проходит.
- [ ] 4.3 Осознанные исключения подавить точечно с объяснением. Проверка: тест сценария «Подавление без объяснения» — ни одного подавления без пояснения рядом.
- [ ] 4.4 Отдельным коммитом. Проверка: `git log` — коммит с `exhaustive-deps` один и откатывается независимо; это единственный коммит уборки, меняющий поведение.

## 5. Остальные правила (41 случай)

- [ ] 5.1 `no-img-element` — перейти на `next/image` там, где размеры известны. Проверка: страницы отрисовываются, `npx jest` проходит.
- [ ] 5.2 `jsx-a11y/*` — добавить недостающие подписи и атрибуты ролей. Проверка: тесты доступности проходят; `alt` не пустой там, где изображение несёт смысл.
- [ ] 5.3 `no-alert` — заменить на существующий механизм уведомлений. Проверка: поведение сохранено, `npx jest` проходит.
- [ ] 5.4 `no-require-imports`, `react/display-name`, `import/no-anonymous-default-export` — привести к принятому в проекте виду. Проверка: предупреждений по этим правилам не остаётся.
- [ ] 5.5 Снять мёртвые директивы подавления. Проверка: тест сценария «Мёртвое подавление» — линтер о них больше не сообщает.

## 6. Порог

- [ ] 6.1 Убедиться, что предупреждений ноль. Проверка: `npx eslint src` сообщает `0 problems`.
- [ ] 6.2 Добавить `--max-warnings 0` в скрипт `lint` в `apps/web/package.json`. Проверка: тесты сценариев «Предупреждений нет» и «Появилось новое предупреждение» — второй проверяется временным нарушением, которое затем снимается.
- [ ] 6.3 Убедиться, что CI использует ту же команду. Проверка: тест сценария «Проверка в CI» — `.github/workflows/ci.yml` вызывает `npm run lint`, а не свой вариант.
- [ ] 6.4 Проверить, что конфигурация правил не менялась. Проверка: тест сценария «Правило выключено в конфигурации» — `git diff` по `eslint.config.mjs` и `packages/config/` пуст.

## 7. Проверка целиком

- [ ] 7.1 Прогнать всё. Проверка: `npx jest`, `npm run type-check`, `npm run lint`, `go test ./...` и три скрипта целостности проходят.
- [ ] 7.2 Убедиться, что покрытие не снизилось. Проверка: пороги из `apps/web/jest.config.js` соблюдены.
