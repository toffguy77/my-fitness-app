## 1. Подготовка

- [x] 1.1 Снять базовые значения счётчика за 30 дней до изменения. Проверка: `baseline.md` — визиты, посетители, отказы, устройства, источники, пути, цели.
- [x] 1.2 Зафиксировать, что уходит вместе с записью сессий и чем закрывается. Проверка: `tradeoff-webvisor.md` — картe скроллинга назван раздел 3, аналитике форм — существующее `onboarding_step_completed` и `last_step` заявки.
- [x] 1.3 Развести идентификатор счётчика по средам в `deploy/env/*.example`. Проверка: `grep METRIKA deploy/env/*.example` показывает обе записи.
- [x] 1.4 Снять `NEXT_PUBLIC_YANDEX_METRIKA_ID` в окружении dev в Dokploy и выкатить dev полной сборкой. Проверка: в чанках `new.burcev.team` идентификатора нет — выполнено, см. `verification.md`.

## 2. Состав сбора счётчика

- [x] 2.1 Выключить `webvisor` в опциях инициализации, сохранив `clickmap`, `trackLinks` и `accurateTrackBounce`. Проверка: тесты сценариев «Состав опций инициализации».
- [x] 2.2 Удалить `noscript`-пиксель. Проверка: тест сценария «Сбор без JavaScript отсутствует».
- [x] 2.3 Не сломать существующую защиту согласием. Проверка: тест сценария «Отказ по-прежнему выключает счётчик».

## 3. Просмотры при клиентской навигации

- [x] 3.1 Завести `shared/analytics/counter.ts` — идентификатор, проверка согласия через `CookieConsent`, безопасный вызов. Проверка: тесты сценариев «Счётчика нет» и «Счётчик не загрузился».
- [x] 3.2 Реализовать компонент отправки просмотров при смене пути и смонтировать его в `apps/web/src/app/layout.tsx`. Проверка: тест сценария «Переход между экранами» — ровно один просмотр на переход.
- [x] 3.3 Пропустить первый путь. Проверка: тест сценария «Первый просмотр не задваивается» — при монтировании вызовов не происходит.
- [x] 3.4 Передавать путь вместе со строкой запроса, читая её из `window.location`. Проверка: тест сценария «Строка запроса сохраняется»; `npm run type-check` проходит без границы `Suspense` в макете.
- [x] 3.5 Не отправлять просмотр при переходе на тот же путь и без согласия. Проверка: тесты сценариев «Повторный переход на тот же путь» и «Согласия нет».

## 4. Глубина прокрутки посадочной страницы

- [x] 4.1 Добавить событие в оба словаря с порогом как обязательным свойством и объявленным набором значений. Проверка: `go test ./internal/modules/analytics/` проходит.
- [x] 4.2 Реализовать наблюдение за прокруткой и смонтировать на посадочной. Проверка: тесты сценариев «Порог достигнут», «Повторное достижение порога», «Один переход через несколько порогов», «Страница короче экрана».
- [x] 4.3 Реализовать проверку значения по набору на приёме. Проверка: тесты сценариев «Значение вне набора», «Число и строка — один ответ», «Свойство без объявленного набора».

## 5. Проверка целиком

- [x] 5.1 Прогнать фронтенд и бэкенд полностью. Проверка: `npx jest` — 4611 тестов, `go test ./...` — 38 наборов, `npm run type-check` и `npx eslint src` без ошибок.
- [x] 5.2 Проверить путь в браузере через `dev-proxy` на 3070. Проверка: `verification.md` — до ответа 0 обращений, после отказа 0, после согласия один просмотр `/` и один новый при клиентском переходе.
- [x] 5.3 Прогнать проверки целостности. Проверка: `check-codebase-integrity`, `check-api-contract`, `check-i18n` проходят.
- [ ] 5.4 Через сутки после выкатки на прод сверить счётчик: появились ли просмотры внутренних переходов и прекратились ли записи сессий. Проверка: в отчёте по путям видны переходы, не являющиеся прямыми заходами; раздел записей сессий не пополняется.
