## 1. Хранилище

- [x] 1.1 Создать миграцию `046_job_runs_{up,down}.sql`: таблица `job_runs(id UUID PK, job_name TEXT NOT NULL, started_at TIMESTAMPTZ NOT NULL, finished_at TIMESTAMPTZ, status TEXT NOT NULL, error TEXT, items_processed INTEGER NOT NULL DEFAULT 0)` с индексом `(job_name, started_at DESC)`. Проверка: `make db-migrate`, down откатывает чисто.

## 2. Пакет jobs

- [x] 2.1 Создать `apps/api/internal/shared/jobs/registry.go`: тип `Job{Name, Interval, RunAt, Timeout, Run}`, реестр с регистрацией и перечислением. Проверка: `go build ./...`.
- [x] 2.2 Создать `lock.go`: захват и освобождение `pg_try_advisory_lock` по хешу имени задачи. Проверка: тест — два параллельных захвата, второй получает отказ.
- [x] 2.3 Создать `store.go`: запись запуска, чтение последнего запуска по задаче, чтение истории. Проверка: тесты на sqlmock.
- [x] 2.4 Создать `scheduler.go`: один тикер, отбор задач по интервалу и по времени суток в `Europe/Moscow`, выполнение под блокировкой с таймаутом, запись результата. Проверка: тесты сценариев «два инстанса», «превышен таймаут», «время суток», «падение инстанса освобождает блокировку».
- [x] 2.5 Реализовать ожидание завершения текущей задачи при остановке. Проверка: тест — сигнал остановки во время выполнения дожидается завершения.

## 3. Перенос существующей задачи

- [x] 3.1 Зарегистрировать задачу `content.publish-scheduled` с интервалом одна минута и таймаутом 30 секунд, вызывающую `PublishScheduledArticles`. Проверка: интеграционный тест — статья со `scheduled_at` в прошлом публикуется.
- [x] 3.2 Удалить `apps/api/internal/modules/content/scheduler.go` и его запуск из `main.go:506-509`. Проверка: `go build ./...`, `grep -rn "RunScheduler" apps/api` пусто.
- [x] 3.3 Подключить планировщик реестра в `main.go` и завести ожидание его остановки в graceful shutdown. Проверка: остановка контейнера не оставляет запись `job_runs` без `finished_at`.

## 4. Задачи уборки

- [x] 4.1 Зарегистрировать `cleanup.password-reset-attempts` (раз в сутки), вызывающую `rate_limiter.CleanupOldAttempts`. Проверка: тест сценария «Уборка попыток сброса пароля», `items_processed` заполнен.
- [ ] 4.2 Зарегистрировать `cleanup.ws-tickets` (раз в час) — удаление использованных и просроченных тикетов. Проверка: тест на удаление просроченных строк.
- [ ] 4.3 Зарегистрировать `cleanup.refresh-tokens` (раз в сутки) — удаление отозванных токенов с истёкшим сроком. Проверка: тест.
- [x] 4.4 Зарегистрировать `cleanup.job-runs` (раз в сутки) — удаление истории старше 30 дней. Проверка: тест.

## 5. Аналитика куратора

- [x] 5.1 Зарегистрировать `curator.daily-snapshot` с запуском в 03:00 по московскому времени: обход активных кураторов, вызов `CollectDailySnapshot`, таймаут 5 минут. Проверка: тесты сценариев «Сбор снапшота за день» и «Повторный сбор за ту же дату».
- [x] 5.2 Реализовать `CollectWeeklySnapshot` в `curator/snapshots.go`: недельные метрики куратора с upsert по `(curator_id, week_start)`. Проверка: unit-тест на sqlmock.
- [x] 5.3 Реализовать `CollectPlatformBenchmark`: агрегат по всем кураторам за завершившуюся неделю с upsert по `week_start`. Проверка: unit-тест, поле `curator_count` заполнено.
- [x] 5.4 Зарегистрировать `curator.weekly-snapshot` с запуском по понедельникам в 04:00: последовательно недельные снапшоты, затем бенчмарк. Проверка: интеграционный тест — после запуска обе таблицы непусты.
- [ ] 5.5 Проверить, что `GET /curator/analytics/history` и `/benchmark` возвращают накопленные данные. Проверка: тесты сценариев спека.

## 6. Админские эндпоинты

- [x] 6.1 Реализовать `GET /api/v1/admin/jobs`: список задач с расписанием и последним запуском. Проверка: тесты «супер-администратор → 200», «обычная роль → 403».
- [x] 6.2 Реализовать `POST /api/v1/admin/jobs/:name/run` с той же блокировкой и ограничением частоты. Проверка: тесты сценариев «запуск», «запуск во время выполнения → 409», «неизвестное имя → 404».
- [x] 6.3 Внести оба маршрута в реестр защищаемых маршрутов из изменения `fix-authorization-gaps`. Проверка: тест-матрица авторизации проходит.

## 7. Фронтенд

- [ ] 7.1 Проверить состояния «данных пока нет» в разделах истории и бенчмарка куратора; при отсутствии — добавить. Проверка: `npx jest curator` зелёные, скриншот пустого состояния приложен к PR.

## 8. Выкатка

- [ ] 8.1 Задеплоить на dev, запустить вручную `curator.daily-snapshot` и `curator.weekly-snapshot`, проверить наполнение таблиц и разделов куратора. Проверка: разделы показывают данные.
- [ ] 8.2 Задеплоить на prod, выполнить ручной запуск для первичного наполнения. Проверка: `GET /api/v1/admin/jobs` показывает `success` для всех задач.
