## 1. Единое оформление ошибок

- [x] 1.1 Создать `apps/web/src/shared/components/ErrorState.tsx` — состояние ошибки с заголовком, описанием, кнопкой повтора, ссылкой на главную и идентификатором ошибки. Проверка: тесты компонента, snapshot приложен к PR.
- [x] 1.2 Создать утилиту генерации идентификатора ошибки (короткая читаемая строка). Проверка: unit-тест на формат и уникальность.

## 2. Страницы ошибок App Router

- [x] 2.1 Создать `app/global-error.tsx` на русском с перезагрузкой. Проверка: временный бросок ошибки в корневом layout показывает экран.
- [x] 2.2 Создать `app/error.tsx` и `app/not-found.tsx`. Проверка: несуществующий URL отдаёт `404` и собственную страницу.
- [x] 2.3 Создать посегментные `error.tsx` для `dashboard`, `food-tracker`, `chat`, `curator`, `admin`, `settings`. Проверка: ошибка в сегменте не роняет остальное приложение.
- [x] 2.4 Создать `loading.tsx` для сегментов с заметной задержкой загрузки. Проверка: визуальная проверка на dev с эмуляцией медленной сети. *`loading.tsx` добавлен девяти сегментам, которые перед первым кадром идут в сеть: дашборд, трекер, чат, куратор, админ, уведомления, профиль, материалы, настройки. Состояние объявляется через `role="status"` и `aria-live` — иначе для того, кто не смотрит на спиннер, экран просто замолкает.*

## 3. Границы ошибок

- [x] 3.1 Смонтировать `ErrorBoundary` в `app/layout.tsx` вокруг содержимого. Проверка: `grep -rn "ErrorBoundary" apps/web/src/app` находит монтирование.
- [x] 3.2 Добавить в `ErrorBoundary` генерацию идентификатора ошибки и отправку в логгер. Проверка: тест — при падении дочернего компонента вызван отправитель с идентификатором.
- [x] 3.3 Обернуть границами виджеты дашборда, ленту контента и список задач. Проверка: тест сценария «Падение виджета на дашборде».

## 4. Глобальный перехват

- [x] 4.1 Добавить в `shared/utils/logger.ts` метод отправки необработанной ошибки с идентификатором, стеком, путём и версией приложения. Проверка: тесты.
- [x] 4.2 Зарегистрировать `window.onerror` и `unhandledrejection` при инициализации приложения. Проверка: тесты сценариев «Необработанная ошибка» и «Отклонённый промис».
- [x] 4.3 Добавить ограничение потока: не более установленного числа одинаковых ошибок за интервал. Проверка: тест сценария «Ограничение потока ошибок».
- [x] 4.4 Добавить в `apps/api/internal/modules/logs/` приём поля идентификатора ошибки и ограничение частоты приёма клиентских логов по IP. Проверка: тесты, превышение лимита возвращает `429`.

## 5. Ошибки сети

- [x] 5.1 В `shared/utils/api-client.ts` различать исключение `fetch` (нет сети) и ответ с ошибочным статусом; возвращать разные типы ошибок. Проверка: тесты обоих случаев.
- [x] 5.2 Показывать соответствующие сообщения в интерфейсе. Проверка: тесты сценариев «Отсутствие сети» и «Ошибка сервера».

## 6. Проверка

- [x] 6.1 Добавить e2e-сценарий: несуществующий URL, падение виджета, потеря сети. Проверка: `e2e/tests/error-screens.spec.ts`, прогон в CI зелёный.
- [ ] 6.2 Проверить на dev вручную все экраны ошибок. Проверка: чек-лист со скриншотами приложен к PR.
