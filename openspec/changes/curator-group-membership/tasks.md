## 1. Хранение

- [x] 1.1 Миграция `curator_group_invites(user_id PK → users, invite_link, issued_at, joined_at)`. Проверка: накатывается и откатывается на чистой базе.
- [x] 1.2 Стратегия стирания `Delete`. Проверка: убедиться, что без неё `TestErasureCoversSchema` падает.
- [x] 1.3 Обновить `schema.golden`, в диффе только новое.

## 2. Клиент Telegram

- [x] 2.1 `CreateInviteLink(chatID, name)` с `creates_join_request: true` и пределом в одно вступление. Проверка: тест на подставном сервере, отказ приходит с кодом 200 — на это уже есть образец.
- [x] 2.2 `ApproveJoinRequest` и `DeclineJoinRequest`. Проверка: отказ Telegram не принимается за успех.
- [x] 2.3 `RemoveMember`: `banChatMember`, следом `unbanChatMember`. Проверка: тест считает оба вызова — без второго вернуть человека будет нельзя.
- [x] 2.4 `ChatMembers` для сверки. Проверка: недоступность отличается от пустого состава.

## 3. Кому место в группе

- [x] 3.1 Решение по ролям: `coordinator` и `super_admin`, не удалён, не ожидает удаления. Проверка: интеграционный тест на каждую роль.
- [x] 3.2 Заявка одобряется по `telegram_links`, а не по имени. Проверка: тест с совпадающим именем и без привязки — заявка отклоняется.
- [x] 3.3 Отклонённая заявка пишется в журнал с тем, кто просился. Проверка: запись есть.

## 4. Реакция на смену роли

- [x] 4.1 Роль присвоена → выдаётся персональная ссылка, сохраняется в `curator_group_invites`. Проверка: двум людям — две разные ссылки.
- [x] 4.2 Привязан Telegram → приглашение уходит в личный чат. Проверка: интеграционный тест.
- [ ] 4.3 Не привязан → приглашение видно в профиле. Проверка: ручка отдаёт ссылку, Jest на состояние.
- [x] 4.4 Роль снята или аккаунт удалён → удаление из группы **сразу**. Проверка: тест на оба случая.
- [x] 4.5 Удаление не закрывает дорогу назад. Проверка: `unbanChatMember` вызван — абляция его снимает и тест падает.

## 5. Обработка заявки

- [ ] 5.1 Обновление `chat_join_request` разбирается в вебхуке. Проверка: не создаёт обращения, не зовёт модель.
- [x] 5.2 Заявка куратора одобряется, `joined_at` проставляется. Проверка: интеграционный тест.
- [x] 5.3 Заявка постороннего отклоняется. Проверка: тест, и в журнале причина.

## 6. Сверка

- [x] 6.1 Работа по расписанию сверяет состав с ролями. Проверка: лишний удаляется, недостающий даёт предупреждение.
- [x] 6.2 Недоступный состав → ничего не удаляется, возможность сломана. Проверка: **абляция обязательна** — иначе сетевой отказ однажды вычистит группу целиком.
- [x] 6.3 Неудача удаления → предупреждение с именем и причиной. Проверка: тест.
- [x] 6.4 Владельца группы удалить нельзя — это норма, а не ошибка. Проверка: отказ не поднимает тревогу.

## 7. Среды

- [x] 7.1 Группа берётся из переменной среды; без неё ничего не происходит. Проверка: `/health` показывает возможность выключенной.
- [ ] 7.2 Проверить, что среды не пересекаются: dev-бот не трогает боевую группу. Проверка: тест на то, что идентификатор берётся из настроек, а не из константы.

## 8. Документация

- [ ] 8.1 `docs/operations/`: включить одобрение новых участников, иначе заявок не будет; что делать, когда сверка жалуется.
- [ ] 8.2 `docs/curator-guide/`: куратор получает приглашение сам; привязать Telegram надо раньше, иначе ссылку придётся брать в профиле.
- [ ] 8.3 Сверить утверждения глав с кодом.

## 9. Проверка целиком

- [ ] 9.1 `scripts/ci-local.sh` зелёный.
- [ ] 9.2 Абляции: снятый `unbanChatMember`, снятая защита сверки от недоступного состава, одобрение по имени вместо привязки.
- [ ] 9.3 Живая проверка на dev: роль дана → приглашение пришло → заявка одобрена; роль снята → удалён.
- [ ] 9.4 Создать боевую группу, задать переменную, выкатить, проверить там же.
