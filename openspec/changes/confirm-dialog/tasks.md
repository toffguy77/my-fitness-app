# Tasks

## 1. Ловушка фокуса становится общей

- [ ] 1.1 Перенести `useFocusTrap` из `features/dashboard/hooks/useKeyboardNavigation.ts` в `shared/hooks/useFocusTrap.ts`, оставив реэкспорт из дашборда. Проверка: `npx jest src/features/dashboard/hooks` и `src/features/dashboard/__tests__/keyboard-navigation.property.test.tsx` проходят без правок самих проверок.
- [ ] 1.2 Перенести относящиеся к хуку тесты вместе с ним. Проверка: `npx jest src/shared/hooks` содержит проверки переноса фокуса вперёд и назад; `npm run type-check` проходит.

## 2. Компонент и хук

- [ ] 2.1 Написать `shared/components/ui/ConfirmDialog.tsx`: заголовок, объяснение, кнопка действия (обычная и разрушающая), кнопка отказа. Проверка: тест сценария «Удаление с перечислением последствий» — в разметке есть заголовок, объяснение и подпись, называющая действие.
- [ ] 2.2 Добавить роль диалога, связь с заголовком и ловушку фокуса. Проверка: тесты сценариев «Фокус при открытии», «Фокус не уходит за пределы диалога», «Возврат фокуса» — фокус встаёт на кнопку отказа, Tab по кругу, после закрытия возвращается на вызвавший элемент.
- [ ] 2.3 Закрытие по Escape и по клику на подложку — как отказ. Проверка: тест сценария «Закрытие клавишей Escape» и тест на подложку: `onConfirm` не вызывался.
- [ ] 2.4 Написать `shared/components/ui/useConfirm.ts`, возвращающий `{ confirm, dialog }`. Проверка: тест — вызов `confirm(...)` показывает диалог, нажатие подтверждения выполняет переданное действие ровно один раз.
- [ ] 2.5 Держать диалог открытым, пока обещание `onConfirm` не разрешилось; кнопка на это время отключена и показывает выполнение. Проверка: тест сценария «Двойное нажатие подтверждения» — два нажатия подряд дают один вызов; Escape и подложка в это время не закрывают диалог.
- [ ] 2.6 Отклонённое обещание закрывает диалог и пробрасывает исключение наружу. Проверка: тест сценария «Отказ сервера» — вызывающий код получает исключение, диалог закрыт.
- [ ] 2.7 Вынести подписи в словари `ru`/`en` и экспортировать компонент из `shared/components/ui/index.ts`. Проверка: `node scripts/check-i18n.mjs` проходит; тест сценария «Язык кнопок» — при выбранном языке подписи из соответствующего словаря.

## 3. Переход четырёх мест

- [ ] 3.1 `admin/components/UserDetail.tsx:65` — смена роли. Проверка: существующие тесты экрана проходят после замены подмены `window.confirm` на нажатие кнопки в диалоге; отказ не отправляет запрос.
- [ ] 3.2 `content/components/ArticleList.tsx:84` — удаление статьи; литеральная строка `'Удалить статью?'` уезжает в словарь. Проверка: тесты экрана проходят; `check-i18n` не находит литералов.
- [ ] 3.3 `curator/components/PlanTab.tsx:123` — удаление активного плана. Проверка: тесты экрана проходят; повторное нажатие подтверждения не отправляет второй запрос.
- [ ] 3.4 `dashboard/components/UnsavedDataNotification.tsx:93` — отказ от несохранённых данных. Проверка: тесты экрана проходят; при отказе данные остаются.

## 4. Закрыть путь назад

- [ ] 4.1 Перевести `no-alert` в конфигурации линтера из предупреждения в ошибку и убрать точечные подавления в четырёх местах вместе с комментариями про долг. Проверка: тест сценария «Системное окно не используется» — `npm run lint` проходит, а добавленный временно `confirm()` его роняет.
- [ ] 4.2 Убрать из конфигурации комментарий, объясняющий, почему правило было предупреждением. Проверка: `git diff` по `eslint.config.mjs` показывает только смену уровня и удаление устаревшего объяснения.

## 5. Проверка целиком

- [ ] 5.1 Прогнать всё. Проверка: `npx jest`, `npm run type-check`, `npm run lint`, `npm run build`, `node scripts/check-i18n.mjs`, `node scripts/check-codebase-integrity.mjs` проходят.
- [ ] 5.2 Убедиться, что покрытие не опустилось ниже порогов из `apps/web/jest.config.js`. Проверка: `npx jest --coverage` завершается успешно.
