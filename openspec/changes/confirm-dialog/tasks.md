# Tasks

## 1. Ловушка фокуса становится общей

- [x] 1.1 Перенести `useFocusTrap` из `features/dashboard/hooks/useKeyboardNavigation.ts` в `shared/hooks/useFocusTrap.ts`, оставив реэкспорт из дашборда. Проверка: `npx jest src/features/dashboard/hooks` и `src/features/dashboard/__tests__/keyboard-navigation.property.test.tsx` проходят без правок самих проверок.
- [x] 1.2 Перенести относящиеся к хуку тесты вместе с ним. Проверка: `npx jest src/shared/hooks` содержит проверки переноса фокуса вперёд и назад; `npm run type-check` проходит.

## 2. Компонент и хук

- [x] 2.1 Написать `shared/components/ui/ConfirmDialog.tsx`: заголовок, объяснение, кнопка действия (обычная и разрушающая), кнопка отказа. Проверка: тест сценария «Удаление с перечислением последствий» — в разметке есть заголовок, объяснение и подпись, называющая действие.
- [x] 2.2 Добавить роль диалога, связь с заголовком и ловушку фокуса. Проверка: тесты сценариев «Фокус при открытии», «Фокус не уходит за пределы диалога», «Возврат фокуса» — фокус встаёт на кнопку отказа, Tab по кругу, после закрытия возвращается на вызвавший элемент.
- [x] 2.3 Закрытие по Escape и по клику на подложку — как отказ. Проверка: тест сценария «Закрытие клавишей Escape» и тест на подложку: `onConfirm` не вызывался.
- [x] 2.4 Написать `shared/components/ui/useConfirm.ts`, возвращающий `{ confirm, dialog }`. Проверка: тест — вызов `confirm(...)` показывает диалог, нажатие подтверждения выполняет переданное действие ровно один раз.
- [x] 2.5 Держать диалог открытым, пока обещание `onConfirm` не разрешилось; кнопка на это время отключена и показывает выполнение. Проверка: тест сценария «Двойное нажатие подтверждения» — два нажатия подряд дают один вызов; Escape и подложка в это время не закрывают диалог.
- [x] 2.6 Отклонённое обещание закрывает диалог и пробрасывает исключение наружу. Проверка: тест сценария «Отказ сервера» — вызывающий код получает исключение, диалог закрыт.
- [x] 2.7 Вынести подписи в словари `ru`/`en` и экспортировать компонент из `shared/components/ui/index.ts`. Проверка: `node scripts/check-i18n.mjs` проходит; тест сценария «Язык кнопок» — при выбранном языке подписи из соответствующего словаря.

## 3. Переход четырёх мест

- [x] 3.1 `admin/components/UserDetail.tsx:65` — смена роли. Проверка: существующие тесты экрана проходят после замены подмены `window.confirm` на нажатие кнопки в диалоге; отказ не отправляет запрос.
- [x] 3.2 `content/components/ArticleList.tsx:84` — удаление статьи. Проверка: тесты экрана проходят. Строки остались литеральными: раздел «Контент» не входит в `TRANSLATED` (`scripts/check-i18n.mjs`), и в этом файле литеральны все остальные тексты тоже. Вынести в словарь один — значит сделать вид, что раздел переведён; извлечение раздела целиком — отдельная работа.
- [x] 3.3 `curator/components/PlanTab.tsx:123` — удаление активного плана. Проверка: тесты экрана проходят; повторное нажатие подтверждения не отправляет второй запрос.
- [x] 3.4 `dashboard/components/UnsavedDataNotification.tsx:93` — отказ от несохранённых данных. Проверка: тесты экрана проходят; при отказе данные остаются.

## 4. Закрыть путь назад

- [x] 4.1 Перевести `no-alert` в конфигурации линтера из предупреждения в ошибку и убрать точечные подавления в четырёх местах вместе с комментариями про долг. Проверка: тест сценария «Системное окно не используется» — `npm run lint` проходит, а добавленный временно `confirm()` его роняет.
- [x] 4.2 Убрать из конфигурации комментарий, объясняющий, почему правило было предупреждением. Проверка: `git diff` по `eslint.config.mjs` показывает только смену уровня и удаление устаревшего объяснения.

## 5. Проверка целиком

- [x] 5.1 Прогнать всё. Проверка: `npx jest`, `npm run type-check`, `npm run lint`, `npm run build`, `node scripts/check-i18n.mjs`, `node scripts/check-codebase-integrity.mjs` проходят.
- [x] 5.2 Убедиться, что покрытие не опустилось ниже порогов из `apps/web/jest.config.js`. Проверка: `npx jest --coverage` завершается успешно.

## 6. Отклонения от предложения

- Строки в `ArticleList` остались литеральными (см. 3.2). Предложение обещало
  вынести `'Удалить статью?'` в словарь; раздел «Контент» ещё не извлечён, и
  один ключ среди пятнадцати литералов вводил бы в заблуждение.
- Английский словарь не тронут: он пуст намеренно, каждый ключ падает на
  русский (`dictionaries/en.ts`). Новая подпись добавлена только в `ru`.
