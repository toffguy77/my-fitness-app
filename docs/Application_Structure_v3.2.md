# Структура приложения My Fitness App v3.2

## Обзор

My Fitness App — это Freemium SaaS платформа для управления питанием и отслеживания прогресса. Документация v3.2 отражает **текущую реализацию** в коде (по состоянию на 17 декабря 2025) с добавлением **Feedback Loop** (петли обратной связи): Daily Check-in, Coach Feedback, улучшенный Coach Dashboard и пересчет целей.

---

## Ключевые изменения v3.2

1. **Daily Check-in:** Явное действие пользователя по завершению дня с блокировкой редактирования
2. **Coach Feedback:** Асинхронная связь тренера с клиентом через заметки на конкретную дату
3. **Coach Dashboard v2:** Улучшенная система индикаторов (Red/Yellow/Green/Grey) с учетом статуса завершения дня
4. **Target Recalculation:** Возможность пересчитать цели питания на основе текущего веса без повторного прохождения Onboarding

---

## Роли пользователей

*Без изменений по сравнению с v3.1: Client (Free/Premium), Coach, Super Admin*

---

## Публичные страницы (не требуют авторизации)

*Без изменений по сравнению с v3.1: `/`, `/login`, `/register`*

---

## Защищенные страницы для клиентов

### `/onboarding` — Мастер настройки

**Файл:** `src/app/onboarding/page.tsx`

**Функциональность:**
- *Без изменений в v3.2*
- 3 шага: Биометрия, Активность, Цель
- Расчет BMR/TDEE и создание целей

**Переходы:**
- → `/app/dashboard` (после завершения)

---

### `/app/dashboard` — Дашборд клиента

**Файл:** `src/app/app/dashboard/page.tsx`

**Изменения в v3.2:**

#### 1. Виджет заметки тренера (NEW)
- **Расположение:** После секции "Сводка за выбранную дату"
- **Условие отображения:** Только для Premium пользователей с назначенным тренером
- **Функциональность:**
  - Загружает заметку из таблицы `coach_notes` за выбранную дату
  - Отображается в синем блоке с иконкой сообщения
  - Показывает дату заметки
  - Автоматически обновляется при смене даты

#### 2. Кнопка "Завершить день" (NEW)
- **Расположение:** Отдельная секция внизу страницы
- **Условие отображения:** Только для выбранной даты (не будущей)
- **Валидация:**
  - Проверка наличия веса (`weight !== null`)
  - Проверка наличия данных (хотя бы один прием пищи ИЛИ `actual_calories > 0`)
- **Функциональность:**
  - При нажатии устанавливает `is_completed = true` и `completed_at = NOW()` в `daily_logs`
  - Блокирует редактирование дня (все кнопки редактирования становятся неактивными)
  - **Free пользователи:** Показывает модальное окно с сообщением и подсчетом стрика (дней подряд)
  - **Premium пользователи:** Показывает сообщение "Тренер получит уведомление"
- **Состояние после завершения:**
  - Кнопка заменяется на зеленый индикатор "День завершен" с временем завершения
  - Все кнопки редактирования скрыты или неактивны

#### 3. Блокировка редактирования завершенных дней
- **Кнопка "Редактировать":** Скрыта, если `is_completed === true`
- **Кнопки редактирования приемов пищи:** Неактивны для завершенных дней
- **Кнопка "Добавить прием пищи":** Скрыта для завершенных дней
- **Модальное окно AddMealModal:** Не открывается для завершенных дней

**Остальная функциональность:**
- Навигация по датам
- Сводка КБЖУ за выбранную дату
- Просмотр и управление приемами пищи
- Статистика за неделю

**Переходы:**
- → `/app/settings` (кнопка "Настройки")
- → `/app/nutrition?date={selectedDate}` (ввод/редактирование питания, только если день не завершен)
- → `/app/reports` (отчеты — только Premium)

**Особенности:**
- Завершенные дни нельзя редактировать (защита от читерства)
- Заметка тренера загружается автоматически при смене даты
- Стрик (дни подряд) рассчитывается для Free пользователей

---

### `/app/nutrition` — Ввод данных о питании

**Файл:** `src/app/app/nutrition/page.tsx`

**Изменения в v3.2:**

#### 1. Блокировка редактирования завершенных дней (NEW)
- **Проверка при загрузке:**
  - Если `logResult.data.is_completed === true`, показывается предупреждение
  - Автоматический редирект на `/app/dashboard?date={selectedDate}`
  - Редактирование невозможно

**Остальная функциональность:**
- Поддержка параметра `date` из URL
- Поддержка параметра `edit` для редактирования конкретного приема
- Динамическое переключение типа дня
- Ввод веса тела
- Множественные приемы пищи

**Переходы:**
- → `/app/dashboard?date={selectedDate}` (после сохранения или если день завершен)
- → `/login` (если пользователь не авторизован)

**Особенности:**
- Завершенные дни нельзя редактировать (проверка при загрузке)

---

### `/app/reports` — Отчеты и аналитика (только Premium)

**Файл:** `src/app/app/reports/page.tsx`

**Функциональность:**
- *Без изменений в v3.2*
- Группировка логов по неделям
- Отображение КБЖУ, веса, заметок

**Переходы:**
- → `/app/dashboard` (кнопка "← Назад")

---

### `/app/settings` — Настройки профиля

**Файл:** `src/app/app/settings/page.tsx`

**Изменения в v3.2:**

#### 1. Секция "Мои цели" (NEW)
- **Расположение:** После секции "Подписка", перед секцией "Мой тренер"
- **Функциональность:**
  - Отображает текущие цели для дня отдыха и тренировочного дня
  - Показывает: Калории, Белки, Жиры, Углеводы для каждого типа дня
  - Кнопка **"Пересчитать по текущему весу"**
- **Пересчет целей:**
  - **Валидация:** Проверяет наличие биометрических данных (`height`, `birth_date`, `gender`, `activity_level`)
  - Если данных нет → редирект на `/onboarding`
  - Получает последний вес из `daily_logs`
  - Использует ту же логику расчета, что и в Onboarding:
    - BMR по формуле Харриса-Бенедикта
    - TDEE = BMR × коэффициент активности
    - Сохраняет текущее соотношение калорий (определяет цель из текущих целей)
    - Макронутриенты: 30% белки, 25% жиры, 45% углеводы
  - Обновляет обе записи в `nutrition_targets` (rest и training, +200 ккал для training)
  - Обновляет локальное состояние для отображения

**Остальная функциональность:**
- Редактирование профиля (имя, телефон)
- Смена пароля
- Просмотр статуса подписки
- Информация о тренере (для Premium)
- Выход из системы

**Переходы:**
- → `/app/dashboard` (кнопка "Назад")
- → `/onboarding` (если нет биометрических данных при пересчете)
- → `/login` (после выхода)

**Особенности:**
- Пересчет использует последний введенный вес из дневника
- Сохраняет текущее соотношение калорий (не сбрасывает цель)

---

## Защищенные страницы для тренеров

### `/app/coach` — Кабинет тренера

**Файл:** `src/app/app/coach/page.tsx`

**Изменения в v3.2:**

#### 1. Улучшенная система индикаторов (Traffic Light System v2)
- **Новые статусы:**
  - 🔴 **Red (Требует внимания):**
    - День не закрыт И отклонение калорий > 15%
    - Нет отчета > 48 часов
  - 🟡 **Yellow (В процессе):**
    - День закрыт, но отклонение калорий > 15%
    - День не закрыт, но в пределах нормы
    - Нет отчета > 24 часов (но < 48)
  - 🟢 **Green (В норме):**
    - День закрыт И попадание в цели (отклонение ≤ 15%)
  - ⚪ **Grey (Нет данных):**
    - Новый клиент / нет данных
    - Нет отчета < 24 часов

#### 2. Отображение статуса завершения дня
- В списке клиентов показывается индикатор ✅, если день завершен
- Текст "(День завершен)" рядом со статусом

#### 3. Фильтр по статусу
- Добавлен фильтр "🟡 В процессе" для yellow статуса
- Фильтры: Все / 🔴 Требуют внимания / 🟡 В процессе / 🟢 В норме / ⚪ Нет данных

#### 4. Сортировка
- Приоритетная сортировка: Red (1) > Yellow (2) > Grey (3) > Green (4)
- По умолчанию сортировка по статусу (Red сверху)

**Остальная функциональность:**
- Список клиентов с информацией о последнем чекине
- Отображение калорий за сегодня vs цель
- Переход к просмотру дашборда клиента

**Переходы:**
- → `/app/coach/[clientId]` (клик по клиенту)
- → `/login` (кнопка выхода)

**Особенности:**
- Учитывается статус `is_completed` при определении индикатора
- Приоритет отдается клиентам, требующим внимания

---

### `/app/coach/[clientId]` — Просмотр дашборда клиента

**Файл:** `src/app/app/coach/[clientId]/page.tsx`

**Изменения в v3.2:**

#### 1. Date Picker для заметок (NEW)
- **Расположение:** Вверху страницы, перед секцией "Заметка для клиента"
- **Функциональность:**
  - Позволяет выбрать дату для заметки
  - При смене даты загружается существующая заметка (если есть)
  - Ограничение: нельзя выбрать будущую дату

#### 2. Секция "Заметка для клиента" (NEW)
- **Расположение:** После Date Picker, перед дашбордом клиента
- **Функциональность:**
  - Текстовое поле для ввода заметки
  - Показывает существующую заметку за выбранную дату (если есть)
  - Кнопка "Сохранить заметку"
  - Использует `upsert` для создания/обновления заметки
  - Сохраняет в таблицу `coach_notes` с полями:
    - `client_id` (ID клиента)
    - `coach_id` (ID тренера)
    - `date` (выбранная дата)
    - `content` (текст заметки)
    - `updated_at` (время обновления)

**Остальная функциональность:**
- Read-only просмотр дашборда клиента
- Редактирование целей по питанию

**Переходы:**
- → `/app/coach` (кнопка "Назад")

**Особенности:**
- Заметка сохраняется с уникальным ключом `(client_id, coach_id, date)`
- При смене даты форма очищается, если заметки нет

---

## Защищенные страницы для супер-администраторов

### `/admin` — Панель супер-администратора

**Файл:** `src/app/admin/page.tsx`

**Функциональность:**
- *Без изменений в v3.2*
- Управление пользователями
- Редактирование ролей, подписок, назначение тренеров

**Переходы:**
- → `/login` (кнопка выхода)

---

## Система навигации и защиты маршрутов

### Middleware (`src/middleware.ts`)

**Функциональность:**
- *Без изменений в v3.2*
- Защита `/onboarding` (только авторизованные)
- Защита `/app/reports` (только Premium)
- Защита `/app/coach` (только тренеры)
- Защита `/admin` (только super_admin)

---

## Особенности реализации v3.2

### Daily Check-in Flow

1. **Валидация:**
   - Проверка наличия веса
   - Проверка наличия данных о питании (хотя бы один прием или калории > 0)

2. **Сохранение:**
   - Устанавливает `is_completed = true`
   - Устанавливает `completed_at = NOW()`
   - Блокирует все действия редактирования

3. **Feedback:**
   - Free: Показывает стрик (дни подряд)
   - Premium: Сообщение о уведомлении тренера

4. **Защита:**
   - Завершенные дни нельзя редактировать
   - Проверка в `/app/nutrition` при загрузке
   - Все кнопки редактирования неактивны на дашборде

### Coach Feedback Flow

1. **Создание заметки:**
   - Тренер выбирает дату
   - Вводит текст заметки
   - Сохраняет в `coach_notes`

2. **Отображение для клиента:**
   - Загружается автоматически при смене даты на дашборде
   - Показывается только для Premium пользователей с назначенным тренером
   - Отображается в синем блоке с иконкой сообщения

3. **Хранение:**
   - Таблица `coach_notes` с уникальным ключом `(client_id, coach_id, date)`
   - RLS политики для безопасности

### Coach Dashboard v2

1. **Определение статуса:**
   - Учитывается `is_completed` из `daily_logs`
   - Учитывается отклонение калорий от цели
   - Учитывается время последнего чекина

2. **Приоритетная сортировка:**
   - Red (требуют внимания) → Yellow (в процессе) → Grey (нет данных) → Green (в норме)

3. **Фильтрация:**
   - По статусу (All / Red / Yellow / Green / Grey)
   - По имени
   - По дате последнего чекина

### Target Recalculation

1. **Валидация:**
   - Проверка наличия биометрических данных
   - Получение последнего веса из `daily_logs`

2. **Расчет:**
   - Использует ту же логику, что и Onboarding
   - Сохраняет текущее соотношение калорий (определяет цель)

3. **Обновление:**
   - Обновляет обе записи в `nutrition_targets` (rest и training)
   - Training получает +200 ккал к rest

---

## Технические детали

### Используемые технологии

- **Frontend:** Next.js 16 (App Router), React 19, Tailwind CSS v4
- **Backend:** Supabase (Authentication, Database, RLS)
- **State Management:** React Hooks (useState, useEffect, useMemo)
- **Routing:** Next.js App Router с middleware для защиты маршрутов
- **Icons:** Lucide React

### База данных

#### Новые поля в таблице `daily_logs` (v3.2):
- `completed_at`: TIMESTAMPTZ — время завершения дня (Check-in)

#### Новая таблица `coach_notes` (v3.2):
- `id`: UUID (PRIMARY KEY)
- `client_id`: UUID (REFERENCES profiles)
- `coach_id`: UUID (REFERENCES profiles)
- `date`: DATE
- `content`: TEXT
- `created_at`: TIMESTAMPTZ
- `updated_at`: TIMESTAMPTZ
- **Уникальный ключ:** `(client_id, coach_id, date)`

#### Новые поля в типе `UserProfile` (v3.2):
- `gender`: 'male' | 'female' | 'other' | null
- `birth_date`: string | null
- `height`: number | null
- `activity_level`: 'sedentary' | 'light' | 'moderate' | 'active' | 'very_active' | null

#### Миграция:
- Файл: `docs/migrations/v3.2_add_feedback_loop.sql`
- Добавляет таблицу `coach_notes`, поле `completed_at` и RLS политики

### Безопасность

- **Row Level Security (RLS)** в Supabase для `coach_notes`:
  - Тренер может читать/писать заметки для своих клиентов
  - Клиент может читать заметки от своего тренера
  - Super Admin имеет полный доступ
- **Защита завершенных дней:** Блокировка редактирования через проверку `is_completed`
- **Валидация Check-in:** Проверка наличия обязательных данных перед завершением

### Адаптивность

- **Mobile First подход:** Все новые компоненты адаптивны
- Виджет заметки тренера адаптивен
- Кнопка завершения дня адаптивна
- Date Picker для тренера адаптивен

---

## Компоненты

### `DayToggle` (`src/components/DayToggle.tsx`)
- *Без изменений в v3.2*

### `Paywall` (`src/components/Paywall.tsx`)
- *Без изменений в v3.2*

### `ClientDashboardView` (`src/components/ClientDashboardView.tsx`)
- *Без изменений в v3.2*

### `AddMealModal` (встроенный компонент в `src/app/app/dashboard/page.tsx`)
- *Без изменений в v3.2*
- Блокируется для завершенных дней

---

## Примечания

1. **Check-in обязателен для фиксации данных:** Завершенные дни нельзя редактировать, что защищает от читерства

2. **Coach Feedback асинхронный:** Тренер может оставить заметку на любую дату, клиент увидит ее при просмотре этой даты

3. **Traffic Light System v2:** Улучшенная система индикаторов учитывает статус завершения дня и отклонение от целей

4. **Пересчет целей:** Позволяет обновить цели без повторного прохождения Onboarding, используя текущий вес

5. **Миграция БД:** Необходимо выполнить `docs/migrations/v3.2_add_feedback_loop.sql` перед использованием новых функций

6. **Стрик для Free пользователей:** Подсчитывается при завершении дня для мотивации

---

*Документ обновлен: 17 декабря 2025 (на основе текущей реализации в коде v3.2)*


