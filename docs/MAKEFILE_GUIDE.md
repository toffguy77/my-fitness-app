# Makefile Guide - BURCEV Fitness App

Централизованное управление проектом через Makefile в корне репозитория.

## Быстрый старт

```bash
# Показать все доступные команды
make help

# Установить зависимости
make install

# Запустить dev серверы (frontend + backend)
make dev
```

## Категории команд

### 📦 Installation & Setup

```bash
make install          # Установить все зависимости (frontend + backend)
make setup-env        # Создать .env.local из env.example
```

### 🚀 Development

```bash
make dev              # Запустить оба сервера (frontend + backend)
make dev-web          # Запустить только frontend (localhost:3069)
make dev-api          # Запустить только backend (localhost:4000)
```

**Примечание:** `make dev` запускает оба сервера параллельно. Для остановки используйте `Ctrl+C`.

### 🔨 Building

```bash
make build            # Собрать frontend и backend
make build-web        # Собрать только frontend
make build-api        # Собрать только backend
```

### ✅ Testing

```bash
make test             # Запустить все тесты
make test-web         # Тесты frontend
make test-api         # Тесты backend
make test-coverage    # Тесты с покрытием (frontend + backend)
make test-watch-web   # Тесты frontend в watch режиме
```

**Покрытие тестами:**
- Frontend: ~77.5% (цель: 77%)
- Backend: ~57% (цель: 57%)

### 🔍 Code Quality

```bash
make lint             # Линтинг frontend + форматирование backend
make lint-web         # Линтинг frontend (ESLint)
make lint-api         # Форматирование backend (go fmt)
make type-check       # TypeScript type checking
```

### 🐳 Docker

```bash
make docker-build     # Собрать Docker образы
make docker-up        # Запустить контейнеры
make docker-down      # Остановить контейнеры
make docker-logs      # Просмотр логов
make docker-restart   # Перезапустить контейнеры
make docker-clean     # Очистить Docker ресурсы
```

### 🚢 Deployment

```bash
make deploy-dev       # Деплой на dev.burcev.team (порт 3071)
make deploy-staging   # Деплой на beta.burcev.team (порт 3070)
make deploy-prod      # Деплой на burcev.team (порт 3069)
```

**Процесс деплоя:**
1. Команда делает `git push` в соответствующую ветку
2. GitHub Actions автоматически запускает CI/CD
3. Приложение деплоится на соответствующий сервер

**Окружения:**
- `dev` → dev.burcev.team:3071
- `staging` → beta.burcev.team:3070
- `main` → burcev.team:3069

### 🗄️ Database

```bash
make db-migrate       # Запустить миграции
make db-reset         # Сбросить базу данных (с подтверждением)
```

**Примечание:** Миграции находятся в папке `migrations/` и выполняются через Supabase Dashboard.

### 🧹 Maintenance

```bash
make clean            # Очистить артефакты сборки
make clean-install    # Очистить и переустановить зависимости
make update-deps      # Обновить зависимости
```

### 🔧 Utilities

```bash
make logs-web         # Просмотр логов frontend
make logs-api         # Просмотр логов backend
make status           # Показать статус проекта
make version          # Показать версии инструментов
```

### 🤖 CI/CD Helpers

```bash
make ci-test          # Запустить тесты в CI режиме
make ci-build         # Собрать для CI
make ci-lint          # Запустить линтеры в CI режиме
```

## Типичные сценарии использования

### Начало работы над проектом

```bash
# 1. Клонировать репозиторий
git clone <repo-url>
cd my-fitness-app

# 2. Установить зависимости
make install

# 3. Настроить окружение
make setup-env
# Отредактировать .env.local с вашими значениями

# 4. Запустить dev серверы
make dev
```

### Ежедневная разработка

```bash
# Запустить dev серверы
make dev

# В другом терминале - запустить тесты в watch режиме
make test-watch-web

# Проверить код перед коммитом
make lint
make type-check
make test
```

### Подготовка к деплою

```bash
# 1. Проверить что все тесты проходят
make test-coverage

# 2. Проверить линтинг
make lint

# 3. Собрать проект
make build

# 4. Задеплоить на dev для проверки
make deploy-dev

# 5. После проверки - задеплоить на staging
make deploy-staging

# 6. После финальной проверки - задеплоить на prod
make deploy-prod
```

### Работа с Docker

```bash
# Собрать и запустить в Docker
make docker-build
make docker-up

# Просмотр логов
make docker-logs

# Остановить
make docker-down

# Полная очистка
make docker-clean
```

### Обновление зависимостей

```bash
# Обновить все зависимости
make update-deps

# Проверить что все работает
make test
make build
```

### Очистка проекта

```bash
# Очистить артефакты сборки
make clean

# Полная переустановка
make clean-install
```

## Структура Makefile

Makefile организован по категориям:

1. **Installation & Setup** - установка и настройка
2. **Development** - разработка
3. **Building** - сборка
4. **Testing** - тестирование
5. **Code Quality** - качество кода
6. **Docker** - работа с Docker
7. **Deployment** - деплой
8. **Database** - работа с БД
9. **Maintenance** - обслуживание
10. **Utilities** - утилиты
11. **CI/CD Helpers** - помощники для CI/CD

## Переменные окружения

Makefile автоматически настраивает окружение для Go:

```makefile
export GOROOT := /opt/homebrew/opt/go/libexec
export GOPATH := $(HOME)/src/go
export PATH := /opt/homebrew/bin:$(GOROOT)/bin:$(GOPATH)/bin:$(PATH)
```

Это решает проблему с Homebrew на macOS.

## Цветной вывод

Makefile использует цветной вывод для лучшей читаемости:

- 🔵 Синий - информационные сообщения
- 🟢 Зеленый - успешные операции
- 🟡 Желтый - предупреждения
- 🔴 Красный - ошибки и важные предупреждения

## Параллельное выполнение

Некоторые команды запускают процессы параллельно:

```bash
# make dev запускает frontend и backend одновременно
make dev

# Для остановки используйте Ctrl+C
# Это остановит оба процесса
```

## Безопасность

Команды с потенциально опасными операциями требуют подтверждения:

```bash
# Деплой на production
make deploy-prod
# → Запросит подтверждение: "Are you sure? (yes/no):"

# Сброс базы данных
make db-reset
# → Запросит подтверждение
```

## Troubleshooting

### Go не найден

Если видите ошибку "go: command not found":

```bash
# Проверьте установку Go
make version

# Если Go не установлен
brew install go

# Или используйте setup-env.sh
source scripts/setup-env.sh
```

### npm не найден

```bash
# Установите Node.js и npm
brew install node

# Проверьте версию
make version
```

### Docker не запускается

```bash
# Проверьте что Docker Desktop запущен
docker ps

# Если не запущен - запустите Docker Desktop
```

## Интеграция с CI/CD

Makefile интегрирован с GitHub Actions:

```yaml
# .github/workflows/ci.yml
- name: Run tests
  run: make ci-test

- name: Build
  run: make ci-build

- name: Lint
  run: make ci-lint
```

## Дополнительные ресурсы

- [README.md](../README.md) - Общая информация о проекте
- [TESTING.md](./TESTING.md) - Руководство по тестированию
- [LOGGING.md](./LOGGING.md) - Система логирования
- [GOLANG_BACKEND.md](./GOLANG_BACKEND.md) - Backend на Go

## Поддержка

Если у вас возникли проблемы с Makefile:

1. Запустите `make help` для списка команд
2. Запустите `make version` для проверки инструментов
3. Запустите `make status` для проверки статуса проекта
4. Проверьте документацию в `docs/`
