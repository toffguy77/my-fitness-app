# Структура приложения My Fitness App

## Обзор

My Fitness App — это Freemium SaaS платформа для управления питанием и отслеживания прогресса. Приложение поддерживает три роли пользователей: **клиенты**, **тренеры** и **супер-администраторы**.

---

## Роли пользователей

### 1. Клиент (Client)
- **Роль по умолчанию** при регистрации
- Может быть **Free** (бесплатный) или **Premium** (платный)
- Free клиенты имеют доступ к базовому функционалу дневника питания
- Premium клиенты получают доступ к отчетам, аналитике и работе с тренером

### 2. Тренер (Coach)
- Может просматривать данные своих клиентов
- Видит статус выполнения планов питания
- Может редактировать цели по питанию для клиентов

### 3. Супер-администратор (Super Admin)
- Полный доступ ко всем функциям
- Управление пользователями, ролями и подписками
- Назначение тренеров клиентам

---

## Публичные страницы (не требуют авторизации)

### `/` — Landing Page (Главная страница)

**Функциональность:**
- Маркетинговая страница с описанием приложения
- Секции: Hero, The Problem, The Solution, The Hook (сравнение Free vs Pro)
- Призыв к действию: регистрация

**Переходы:**
- → `/register` (кнопка "Начать бесплатно")
- → `/register` (кнопка "Регистрация" в футере)
- Автоматический редирект на `/app/dashboard` (для авторизованных пользователей)
- Автоматический редирект на `/admin` (для super_admin)
- Автоматический редирект на `/app/coach` (для тренеров)

---

### `/login` — Страница входа

**Функциональность:**
- Авторизация существующих пользователей (email + password)
- Проверка существующей сессии (автоматический редирект, если уже авторизован)
- Определение роли пользователя и редирект на соответствующую страницу

**Переходы:**
- → `/app/dashboard` (после успешного входа для клиентов)
- → `/app/coach` (после успешного входа для тренеров)
- → `/admin` (после успешного входа для super_admin)
- → `/register` (ссылка "Нет аккаунта? Зарегистрируйтесь")

---

### `/register` — Страница регистрации

**Функциональность:**
- Регистрация новых пользователей
- Автоматическое создание профиля с ролью `client` и статусом `free`
- Ввод данных: email, password, full_name (опционально)

**Переходы:**
- → `/app/dashboard` (после успешной регистрации)
- → `/login` (ссылка "Уже есть аккаунт? Войдите")

---

## Защищенные страницы для клиентов

### `/app/dashboard` — Дашборд клиента

**Функциональность:**
- **Сводка за сегодня:**
  - Отображение потребленных КБЖУ (Калории, Белки, Жиры, Углеводы)
  - Для Premium пользователей: отображение целевых значений (например, "140 / 100", где 140 — потреблено, 100 — норма)
  - Вес тела (последний введенный вес)
  - Возможность редактирования веса (клик по блоку веса)
  - Список всех приемов пищи за сегодня
  - Кнопки для добавления, редактирования и удаления приемов пищи

- **Быстрые действия:**
  - Добавление нового приема пищи
  - Редактирование веса

- **Статистика за неделю:**
  - Средние значения КБЖУ за неделю
  - Количество дней с записями
  - График прогресса (если есть данные)

- **Навигация:**
  - Переход на ввод питания
  - Переход на отчеты (только для Premium)
  - Выход из системы

**Переходы:**
- → `/app/nutrition` (кнопка "Ввести данные", добавление/редактирование приема пищи)
- → `/app/nutrition?edit={mealId}` (редактирование конкретного приема пищи)
- → `/app/reports` (кнопка "Отчеты" — только для Premium)
- → `/login` (кнопка выхода)

**Особенности:**
- Для Premium пользователей отображаются целевые значения КБЖУ
- Тип дня (тренировочный/отдых) определяется автоматически на основе сохраненных данных
- Вес можно редактировать прямо на дашборде

---

### `/app/nutrition` — Ввод данных о питании

**Функциональность:**
- **Ввод данных о приемах пищи:**
  - Добавление нескольких приемов пищи за день
  - Для каждого приема: название (автоматически по времени суток), вес, калории, белки, жиры, углеводы
  - Возможность удаления приемов пищи (минимум 1 прием должен остаться)
  - Выбор даты приема пищи (для поправок, если забыл внести ранее)

- **Выбор типа дня:**
  - Переключатель "Тренировочный день" / "День отдыха"
  - Блокируется после первого сохранения (нельзя изменить тип дня для уже сохраненного дня)

- **Вес тела:**
  - Ввод текущего веса
  - Сохранение веса вместе с данными о питании

- **Уровень голода:**
  - Выбор из 5 уровней (emoji-кнопки)

- **Итоговая сводка:**
  - Автоматический расчет суммарных КБЖУ за день
  - Для Premium: сравнение с целевыми значениями
  - Прогресс-бары для визуализации выполнения плана

**Переходы:**
- → `/app/dashboard` (после сохранения или отмены)
- → `/login` (если пользователь не авторизован)

**Особенности:**
- Данные не перезаписывают существующие, а добавляются к ним
- Название приема пищи автоматически определяется по времени суток:
  - 6-10: Завтрак
  - 10-13: Второй завтрак
  - 13-16: Обед
  - 16-20: Полдник
  - 20-6: Ужин

---

### `/app/reports` — Отчеты и аналитика (только Premium)

**Функциональность:**
- **Доступ только для Premium пользователей**
- Для Free пользователей показывается Paywall (заглушка с предложением активировать Premium)

- **Для Premium:**
  - Сводка по дням и неделям
  - Группировка записей по неделям
  - Отображение веса, КБЖУ, заметок
  - Анализ прогресса

**Переходы:**
- → `/app/dashboard` (кнопка "Вернуться на дашборд" или навигация)
- → `/login` (если пользователь не авторизован)

**Особенности:**
- Защищено middleware (редирект на `/app/dashboard` для Free пользователей)
- Дополнительная проверка Premium статуса на уровне компонента

---

## Защищенные страницы для тренеров

### `/app/coach` — Кабинет тренера

**Функциональность:**
- **Список клиентов:**
  - Отображение всех клиентов, назначенных тренеру
  - Статус каждого клиента (система "светофор"):
    - 🟢 **Зеленый** — выполнил план питания за сегодня
    - 🔴 **Красный** — не выполнил план или превысил норму
    - ⚪ **Серый** — нет данных за сегодня

- **Фильтрация и сортировка:**
  - Фильтр по статусу (все / зеленые / красные / серые)
  - Сортировка по имени, дате последнего чекина, статусу
  - Поиск по имени клиента

- **Информация о клиентах:**
  - Имя и email
  - Статус подписки
  - Последний чекин
  - Текущий статус выполнения плана

**Переходы:**
- → `/app/coach/[clientId]` (клик по клиенту — просмотр детального дашборда)
- → `/login` (кнопка выхода)

**Особенности:**
- Доступ только для пользователей с ролью `coach`
- Автоматический редирект на `/app/dashboard` для не-тренеров

---

### `/app/coach/[clientId]` — Просмотр дашборда клиента (для тренера)

**Функциональность:**
- **Read-only просмотр дашборда клиента:**
  - Все данные, которые видит клиент на своем дашборде
  - Сводка за сегодня
  - Статистика за неделю
  - История записей

- **Редактирование целей по питанию:**
  - Возможность изменить целевые значения КБЖУ для тренировочных и дней отдыха
  - Переключение между типами дней

**Переходы:**
- → `/app/coach` (кнопка "Назад" — возврат к списку клиентов)
- → `/login` (если пользователь не авторизован)

**Особенности:**
- Тренер видит данные клиента в режиме только для чтения
- Может редактировать только цели по питанию (nutrition targets)

---

## Защищенные страницы для супер-администраторов

### `/admin` — Панель супер-администратора

**Функциональность:**
- **Управление пользователями:**
  - Просмотр всех пользователей системы
  - Поиск по email
  - Фильтрация по роли (client / coach / super_admin / all)
  - Фильтрация по статусу подписки (free / active / cancelled / past_due / all)

- **Редактирование пользователей:**
  - Изменение роли (client ↔ coach ↔ super_admin)
  - Изменение статуса подписки (free / active / cancelled / past_due)
  - Изменение уровня подписки (basic / premium)
  - Назначение тренера клиенту (выбор из списка тренеров)
  - Установка дат начала и окончания подписки
  - Редактирование имени пользователя

- **Информация о пользователях:**
  - Email, имя, роль
  - Статус и уровень подписки
  - Назначенный тренер (для клиентов)
  - Дата создания аккаунта

**Переходы:**
- → `/login` (кнопка выхода)
- → `/app/dashboard` (автоматический редирект для не-админов)

**Особенности:**
- Доступ только для пользователей с ролью `super_admin`
- Полный контроль над всеми пользователями и их подписками
- Используется для ручного управления подписками (до интеграции платежной системы)

---

## Система навигации и защиты маршрутов

### Middleware (`src/middleware.ts`)

**Функциональность:**
- Проверка авторизации для всех защищенных маршрутов
- Определение роли пользователя
- Автоматические редиректы на основе роли:
  - Авторизованные пользователи на `/` → редирект на соответствующую страницу
  - Неавторизованные пользователи на защищенные маршруты → редирект на `/login`

**Правила доступа:**
- **Публичные маршруты:** `/`, `/login`, `/register`
- **Защищенные маршруты:** `/app/*`, `/admin`
- **Ролевые ограничения:**
  - `/app/reports` — только Premium пользователи
  - `/app/coach` — только тренеры
  - `/admin` — только super_admin

---

## Особенности реализации

### Feature Gating (Ограничение доступа к функциям)

1. **Middleware уровень:**
   - Проверка Premium статуса для `/app/reports`
   - Проверка роли для `/app/coach` и `/admin`

2. **Компонентный уровень:**
   - Компонент `Paywall` для отображения заглушки для Free пользователей
   - Условный рендеринг на основе `subscription_status` и `subscription_tier`

### Типы дней (Day Types)

- **Тренировочный день (training):** более высокие целевые значения КБЖУ
- **День отдыха (rest):** более низкие целевые значения КБЖУ
- Тип дня устанавливается при первом сохранении данных за день и не может быть изменен позже

### Хранение данных о питании

- Данные о приемах пищи хранятся в формате JSONB в колонке `meals` таблицы `daily_logs`
- Каждый прием пищи содержит: id, title, weight, calories, protein, fats, carbs, mealDate
- При добавлении новых приемов пищи данные объединяются с существующими (не перезаписываются)

### Система статусов для тренеров

- **Зеленый:** клиент выполнил план питания (потребление в пределах целевых значений)
- **Красный:** клиент не выполнил план (недостаточно или превышение нормы)
- **Серый:** нет данных за сегодня

---

## Диаграмма переходов между страницами

```
┌─────────────┐
│      /      │ (Landing Page)
└──────┬──────┘
       │
       ├─→ /register
       │
       └─→ /login
            │
            ├─→ /app/dashboard (клиенты)
            │     │
            │     ├─→ /app/nutrition
            │     │     └─→ /app/dashboard (после сохранения)
            │     │
            │     └─→ /app/reports (только Premium)
            │
            ├─→ /app/coach (тренеры)
            │     │
            │     └─→ /app/coach/[clientId]
            │           └─→ /app/coach (назад)
            │
            └─→ /admin (super_admin)
```

---

## Технические детали

### Используемые технологии

- **Frontend:** Next.js 16 (App Router), React 19, Tailwind CSS v4
- **Backend:** Supabase (Authentication, Database, RLS)
- **State Management:** React Hooks (useState, useEffect, useMemo)
- **Routing:** Next.js App Router с middleware для защиты маршрутов

### База данных

- **Таблица `profiles`:**
  - Роли: `client`, `coach`, `super_admin`
  - Статусы подписки: `free`, `active`, `cancelled`, `past_due`
  - Уровни подписки: `basic`, `premium`
  - Связь тренер-клиент через `coach_id`

- **Таблица `daily_logs`:**
  - Хранение данных о питании за день
  - Колонка `meals` (JSONB) для детальных данных о приемах пищи
  - Колонка `weight` для веса тела
  - Колонка `target_type` для типа дня (training/rest)

- **Таблица `nutrition_targets`:**
  - Целевые значения КБЖУ для тренировочных и дней отдыха
  - Связь с пользователем через `user_id`

### Безопасность

- Row Level Security (RLS) в Supabase для защиты данных
- Middleware для проверки авторизации и ролей
- Компонентная проверка Premium статуса для дополнительной защиты

---

## Примечания

1. **Старые маршруты:** В коде присутствуют старые маршруты `/coach`, `/nutrition`, `/reports`, которые дублируют функциональность новых маршрутов `/app/coach`, `/app/nutrition`, `/app/reports`. Рекомендуется использовать только маршруты с префиксом `/app/`.

2. **Free клиенты и тренеры:** Free клиенты не могут иметь тренеров (защищено триггером в базе данных).

3. **Платежи:** В текущей версии подписки управляются вручную через админ-панель. Интеграция платежной системы планируется в будущем.

---

*Документ обновлен: 16 декабря 2025*

