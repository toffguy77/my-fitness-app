# PRD - Product Requirements Document
# Phase 2.6: Enhanced Client Dashboard & Nutrition Entry

**Версия:** 2.6.0  
**Дата:** Декабрь 2025  
**Статус:** Завершено  
**Предыдущая версия:** Phase 2.5 v2.5.0

---

## 1. Обзор продукта

### 1.1 Цель

Улучшить пользовательский опыт работы с данными о питании и весом, добавить контекст для Premium пользователей через отображение норм КБЖУ, и автоматизировать создание планов питания.

### 1.2 Ключевые улучшения

1. **Управление данными на дашборде:**
   - Редактирование и удаление отдельных приемов пищи
   - Добавление приемов пищи с выбором даты
   - Редактирование веса прямо на дашборде

2. **Улучшенный ввод питания:**
   - Динамические названия приемов пищи
   - Упрощенный интерфейс (5 уровней голода, упрощенный ввод веса)
   - Поля ввода доступны по умолчанию

3. **Контекст для Premium:**
   - Отображение норм КБЖУ рядом с фактическими значениями
   - Автоматическое создание плана питания

4. **Выбор типа дня:**
   - Переключатель тренировка/отдых
   - Блокировка после сохранения

---

## 2. Функциональные спецификации

### 2.1 Дашборд клиента (`/app/dashboard`)

#### 2.1.1 Секция "Сегодня"

**Компонент:** `ClientDashboard`

**Функциональность:**

1. **Отображение КБЖУ:**
   - 4 карточки: Калории, Белки, Жиры, Углеводы
   - Формат для Premium: `{факт} / {норма}` (например: `140 / 2100`)
   - Формат для Free: только `{факт}`
   - Нормы определяются на основе `todayLog.target_type`:
     - Если `target_type === 'rest'` → используем `targetsRest`
     - Иначе → используем `targetsTraining`
   - Если `target_type` не установлен → дефолт `targetsTraining`

2. **Управление весом:**
   - Кликабельный блок с пунктирной рамкой
   - При клике открывается поле ввода
   - Сохранение при потере фокуса или нажатии Enter
   - Если веса нет → показывается "Нажмите, чтобы добавить"
   - Логика получения веса:
     ```typescript
     const currentWeight = todayLog.weight || 
       weekLogs.find(log => log.date === today && log.weight)?.weight || 
       null
     ```

3. **Кнопка добавления приема пищи:**
   - Большая кнопка рядом с весом
   - При клике открывается модальное окно `AddMealModal`

4. **Список приемов пищи:**
   - Отображение всех приемов из `todayLog.meals`
   - Нумерация: `#1`, `#2`, и т.д.
   - Информация о приеме:
     - Название (жирным шрифтом)
     - Дата (если отличается от сегодняшней)
     - КБЖУ и вес порции
   - Кликабельность: клик по карточке → редактирование
   - Кнопки:
     - ✏️ Редактировать → `/app/nutrition?edit={mealId}`
     - 🗑️ Удалить → подтверждение → удаление → пересчет totals

**Технические детали:**

```typescript
// Определение норм для Premium
const currentTargets = todayLog.target_type === 'rest' 
  ? targetsRest 
  : targetsTraining
const showTargets = isPremium && currentTargets

// Отображение
<div className="text-lg font-bold text-gray-900">
  {todayLog.actual_calories || 0}
  {showTargets && ` / ${currentTargets.calories}`}
</div>
```

**Приоритет:** Критический

---

#### 2.1.2 Модальное окно добавления приема пищи

**Компонент:** `AddMealModal`

**Функциональность:**

1. **Поля формы:**
   - Дата приема пищи (date picker, максимум сегодня)
   - Название (автозаполнение по времени суток)
   - Вес (г)
   - Калории
   - Белки (г)
   - Жиры (г)
   - Углеводы (г)

2. **Логика сохранения:**
   - Получение существующих meals для выбранной даты
   - Добавление нового приема в массив
   - Пересчет totals для выбранной даты
   - Обновление или создание лога за дату

**Технические детали:**

```typescript
// Получение существующих meals
const { data: existingLog } = await supabase
  .from('daily_logs')
  .select('meals')
  .eq('user_id', user?.id)
  .eq('date', mealDate)
  .single()

const existingMeals: Meal[] = (existingLog?.meals as Meal[]) || []
const allMeals = [...existingMeals, newMeal]

// Пересчет totals
const dateMeals = allMeals.filter(m => (m.mealDate || mealDate) === mealDate)
const totals = dateMeals.reduce(/* ... */)
```

**Приоритет:** Критический

---

### 2.2 Страница ввода питания (`/app/nutrition`)

#### 2.2.1 Динамические названия приемов пищи

**Функция:** `getMealNameByTime(hour: number): string`

**Логика:**
```typescript
if (hour >= 6 && hour < 10) return 'Завтрак'
if (hour >= 10 && hour < 13) return 'Второй завтрак'
if (hour >= 13 && hour < 16) return 'Обед'
if (hour >= 16 && hour < 20) return 'Полдник'
if (hour >= 20 || hour < 6) return 'Ужин'
return 'Прием пищи'
```

**Применение:**
- При создании нового приема пищи в `useState` инициализаторе
- В модальном окне добавления приема
- Как placeholder в поле названия

**Приоритет:** Средний

---

#### 2.2.2 Уровень голода (5 уровней)

**Компонент:** Grid из 5 emoji кнопок

**Уровни:**
1. 😋 - Совсем нет голода
2. 🙂 - Легкий голод
3. 😊 - Умеренный голод (дефолт)
4. 😟 - Сильный голод
5. 🤯 - Зверский голод

**Реализация:**
```typescript
const getHungerEmoji = (level: number): string => {
  const emojis: Record<number, string> = {
    1: '😋', 2: '🙂', 3: '😊', 4: '😟', 5: '🤯'
  }
  return emojis[level] || '😊'
}

// Grid
<div className="grid grid-cols-5 gap-2">
  {[1, 2, 3, 4, 5].map((level) => (
    <button onClick={() => setLog({ ...log, hunger_level: level })}>
      {getHungerEmoji(level)}
    </button>
  ))}
</div>
```

**Приоритет:** Средний

---

#### 2.2.3 Поля ввода по умолчанию

**Инициализация state:**
```typescript
const [meals, setMeals] = useState<Meal[]>(() => {
  const now = new Date()
  const mealName = getMealNameByTime(now.getHours())
  return [{
    id: crypto.randomUUID(),
    title: mealName,
    weight: 100,
    calories: 0,
    protein: 0,
    fats: 0,
    carbs: 0,
    mealDate: new Date().toISOString().split('T')[0],
    createdAt: now.toISOString()
  }]
})
```

**Логика загрузки существующих данных:**
- Если есть лог за сегодня с meals → загружаем их
- Если лога нет или meals пустой → оставляем дефолтный прием

**Приоритет:** Высокий

---

#### 2.2.4 Логика сохранения (добавление, не перезапись)

**Алгоритм:**

1. Получаем существующие meals из базы:
   ```typescript
   const { data: existingLog } = await supabase
     .from('daily_logs')
     .select('meals, target_type')
     .eq('user_id', user.id)
     .eq('date', today)
     .single()
   ```

2. Объединяем meals:
   ```typescript
   const existingMeals: Meal[] = (existingLog?.meals as Meal[]) || []
   const newMeals = meals.map(meal => ({
     ...meal,
     mealDate: meal.mealDate || today,
     createdAt: meal.createdAt || new Date().toISOString()
   }))
   
   // Обновляем существующие по ID, добавляем новые
   const mealIds = new Set(newMeals.map(m => m.id))
   const allMeals = [
     ...existingMeals.filter(m => !mealIds.has(m.id)),
     ...newMeals
   ]
   ```

3. Пересчитываем totals:
   ```typescript
   const todayMeals = allMeals.filter(m => (m.mealDate || today) === today)
   const aggregatedTotals = todayMeals.reduce(/* ... */)
   ```

4. Сохраняем:
   ```typescript
   const payload = {
     user_id: user.id,
     date: today,
     target_type: targetTypeToSave,
     meals: allMeals,
     ...aggregatedTotals
   }
   await supabase.from('daily_logs').upsert(payload)
   ```

**Приоритет:** Критический

---

### 2.3 Нормы КБЖУ для Premium

#### 2.3.1 Отображение на дашборде

**Условия:**
- Пользователь должен быть Premium (`isPremium === true`)
- Должны быть загружены цели (`targetsTraining` или `targetsRest`)
- Должен быть лог за сегодня (`todayLog`)

**Определение норм:**
```typescript
const currentTargets = todayLog.target_type === 'rest' 
  ? targetsRest 
  : targetsTraining

const showTargets = isPremium && currentTargets
```

**Отображение:**
```tsx
<div className="text-lg font-bold text-gray-900">
  {todayLog.actual_calories || 0}
  {showTargets && ` / ${currentTargets.calories}`}
</div>
```

**Приоритет:** Высокий

---

#### 2.3.2 План питания по умолчанию

**SQL миграция:** `docs/migrations/create_default_nutrition_plan.sql`

**Логика:**

1. Находим всех Premium пользователей:
   ```sql
   SELECT id, email 
   FROM profiles 
   WHERE subscription_status = 'active' 
   AND subscription_tier = 'premium'
   ```

2. Для каждого пользователя проверяем наличие плана:
   ```sql
   IF NOT EXISTS (
     SELECT 1 FROM nutrition_targets 
     WHERE user_id = premium_user.id 
     AND is_active = true
   )
   ```

3. Создаем два плана:
   - **Тренировочный день:** 2100 ккал / 100г белка / 40г жиров / 500г углеводов
   - **День отдыха:** 1800 ккал / 100г белка / 30г жиров / 400г углеводов

**Значения:**
```sql
-- Training
INSERT INTO nutrition_targets (
  user_id, day_type, calories, protein, fats, carbs, is_active
) VALUES (
  premium_user.id, 'training', 2100, 100, 40, 500, true
)

-- Rest
INSERT INTO nutrition_targets (
  user_id, day_type, calories, protein, fats, carbs, is_active
) VALUES (
  premium_user.id, 'rest', 1800, 100, 30, 400, true
)
```

**Приоритет:** Высокий

---

### 2.4 Выбор и блокировка типа дня

#### 2.4.1 Переключатель типа дня

**Компонент:** `DayToggle`

**Обновления:**
- Добавлен проп `disabled?: boolean`
- Визуальная индикация блокировки (opacity, cursor)
- Подсказка при наведении

**Использование:**
```tsx
<DayToggle 
  value={dayType} 
  onChange={(newType) => {
    if (!dayTypeLocked) {
      setDayType(newType)
    }
  }}
  disabled={dayTypeLocked}
/>
```

**Приоритет:** Высокий

---

#### 2.4.2 Сохранение типа дня

**Поле в БД:** `daily_logs.target_type` (TEXT, значения: 'training', 'rest')

**Логика сохранения:**
```typescript
// Проверяем существующий target_type
const targetTypeToSave = existingLog?.target_type || dayType

// Блокируем после сохранения
setDayTypeLocked(true)

// Сохраняем в payload
const payload = {
  ...aggregatedLog,
  target_type: targetTypeToSave
}
```

**Логика загрузки:**
```typescript
if (logResult.data?.target_type) {
  setDayType(logResult.data.target_type as 'training' | 'rest')
  setDayTypeLocked(true) // Блокируем, если уже сохранен
}
```

**Приоритет:** Высокий

---

## 3. Технические спецификации

### 3.1 Обновление типов TypeScript

**DailyLog:**
```typescript
type DailyLog = {
  date: string
  actual_calories: number
  actual_protein: number
  actual_fats: number
  actual_carbs: number
  weight?: number | null
  meals?: Meal[]
  target_type?: 'training' | 'rest'  // НОВОЕ
}
```

**Meal:**
```typescript
type Meal = {
  id: string
  title: string
  weight: number
  calories: number
  protein: number
  fats: number
  carbs: number
  mealDate?: string  // Дата приема пищи
  createdAt?: string
}
```

**DayToggle Props:**
```typescript
interface DayToggleProps {
  value: DayType
  onChange: (value: DayType) => void
  disabled?: boolean  // НОВОЕ
}
```

---

### 3.2 Обновление компонентов

#### DayToggle.tsx

**Изменения:**
- Добавлен проп `disabled`
- Условное применение стилей блокировки
- Отключение обработчиков при `disabled === true`
- Добавлены title атрибуты для подсказок

**Код:**
```tsx
<button
  onClick={() => !disabled && onChange('training')}
  disabled={disabled}
  className={`... ${disabled ? 'opacity-50 cursor-not-allowed' : ''}`}
  title={disabled ? 'Тип дня уже сохранен и не может быть изменен' : ''}
>
```

---

#### Dashboard (page.tsx)

**Новые state:**
```typescript
const [showAddMealModal, setShowAddMealModal] = useState<boolean>(false)
```

**Логика определения норм:**
```typescript
const currentTargets = todayLog.target_type === 'rest' 
  ? targetsRest 
  : targetsTraining
const showTargets = isPremium && currentTargets
```

**Обработка meals:**
```typescript
let mealsArray: Meal[] = []
if (todayData.meals !== null && todayData.meals !== undefined) {
  if (Array.isArray(todayData.meals)) {
    mealsArray = todayData.meals
  } else if (typeof todayData.meals === 'string') {
    try {
      mealsArray = JSON.parse(todayData.meals)
    } catch (e) {
      mealsArray = []
    }
  }
}
```

---

#### Nutrition Page (page.tsx)

**Новые state:**
```typescript
const [dayTypeLocked, setDayTypeLocked] = useState<boolean>(false)
```

**Логика блокировки:**
```typescript
// При загрузке
if (logResult.data?.target_type) {
  setDayType(logResult.data.target_type)
  setDayTypeLocked(true)
} else {
  setDayTypeLocked(false)
}

// При сохранении
const targetTypeToSave = existingLog?.target_type || dayType
setDayTypeLocked(true)
```

---

### 3.3 Обработка ошибок

#### Обработка отсутствия колонки meals

**Проблема:** Старые данные могут не иметь колонки `meals`

**Решение:**
```typescript
if (errorMessage.includes('column daily_logs.meals does not exist')) {
  // Повторяем запрос без meals
  const { data: logsDataWithoutMeals } = await supabase
    .from('daily_logs')
    .select('date, actual_calories, actual_protein, actual_fats, actual_carbs, weight, ...')
    // без meals
  
  const logsWithEmptyMeals = logsDataWithoutMeals.map(log => ({ 
    ...log, 
    meals: [] 
  }))
}
```

**Приоритет:** Средний

---

#### Обработка ошибок Supabase

**Проблема:** Ошибки Supabase не являются экземплярами Error

**Решение:**
```typescript
const error = logsError instanceof Error 
  ? logsError 
  : new Error((logsError as any)?.message || 'Ошибка загрузки логов')
logger.error('Dashboard: ошибка загрузки логов', error, { 
  userId: user.id, 
  code: (logsError as any)?.code 
})
```

**Приоритет:** Низкий

---

## 4. База данных

### 4.1 Обновление схемы

**Таблица `daily_logs`:**
- ✅ Поле `target_type` (TEXT) - тип дня: 'training' или 'rest'
- ✅ Поле `meals` (JSONB) - массив приемов пищи

**Таблица `nutrition_targets`:**
- ✅ Автоматическое создание планов для Premium через миграцию

**Миграции:**
1. `add_meals_to_daily_logs.sql` - добавление колонки meals
2. `create_default_nutrition_plan.sql` - создание планов для Premium

---

### 4.2 Индексы

**Для meals:**
```sql
CREATE INDEX IF NOT EXISTS idx_daily_logs_meals 
ON daily_logs USING GIN (meals);
```

**Для target_type:**
```sql
CREATE INDEX IF NOT EXISTS idx_daily_logs_target_type 
ON daily_logs(target_type);
```

---

## 5. UX/UI спецификации

### 5.1 Визуальные улучшения

**Кнопки:**
- Редактировать: синий фон (`bg-blue-50`), иконка ✏️
- Удалить: красный фон (`bg-red-50`), иконка 🗑️
- Добавить: пунктирная рамка (`border-dashed`), большой размер

**Карточки приемов пищи:**
- Hover эффект (`hover:bg-gray-100`)
- Кликабельность (`cursor-pointer`)
- Нумерация в бейдже (`#1`, `#2`)

**Блок веса:**
- Пунктирная рамка для интерактивности
- Hover эффект
- Большой размер для удобства клика

---

### 5.2 Модальные окна

**AddMealModal:**
- Фиксированное позиционирование (`fixed inset-0`)
- Затемнение фона (`bg-black bg-opacity-50`)
- Центрирование (`flex items-center justify-center`)
- Максимальная ширина (`max-w-md`)
- Кнопка закрытия (×) в правом верхнем углу

---

## 6. Тестирование

### 6.1 Тестовые сценарии

1. **Добавление приема пищи:**
   - Добавить прием за сегодня
   - Добавить прием за вчера
   - Проверить пересчет totals

2. **Редактирование приема пищи:**
   - Открыть редактирование
   - Изменить данные
   - Сохранить
   - Проверить обновление

3. **Удаление приема пищи:**
   - Удалить прием
   - Подтвердить
   - Проверить пересчет totals

4. **Выбор типа дня:**
   - Выбрать тип дня
   - Сохранить
   - Проверить блокировку
   - Перезагрузить страницу
   - Проверить восстановление типа дня

5. **Premium нормы:**
   - Войти как Premium
   - Проверить отображение норм
   - Изменить тип дня
   - Проверить изменение норм

---

## 7. Известные проблемы и ограничения

1. **График веса:** Не реализован (будет в отчетах)
2. **Валидация:** Нет проверки на разумность значений КБЖУ
3. **Уведомления:** Нет уведомлений о превышении норм
4. **Старые данные:** Могут не иметь поля `meals` (обработано с предупреждением)

---

## 8. Будущие улучшения

1. **Визуализация прогресса:**
   - Прогресс-бары для КБЖУ
   - Цветовая индикация (зеленый/желтый/красный)

2. **Аналитика:**
   - График веса
   - Тренды по КБЖУ
   - Соответствие нормам за период

3. **Умные подсказки:**
   - Рекомендации по распределению КБЖУ
   - Предупреждения о превышении норм

---

**Конец документации Phase 2.6**

