# Структура приложения My Fitness App v1.1

## Обзор
Freemium SaaS платформа для управления питанием. Версия 1.1 включает расширенное управление профилем и улучшенную логику взаимодействия.

---

## Роли пользователей
1. **Клиент (Client):** Free (Дневник) / Premium (Работа с тренером).
2. **Тренер (Coach):** Управление пулом клиентов, аналитика.
3. **Супер-админ (Super Admin):** Биллинг, назначение ролей.

---

## Карта маршрутов (Sitemap)

### Публичная зона
- `/` — **Landing Page**: Маркетинг, вход, регистрация.
- `/login` — **Auth**: Вход в систему.
- `/register` — **Sign Up**: Создание аккаунта (default: Client Free).

### Зона Клиента (`/app`)
- `/app/dashboard` — **Главная**:
  - Сводка КБЖУ (План/Факт).
  - Ввод/Редактирование веса.
  - Лента приемов пищи.
  - *Изменение:* Кнопка "Настройки" в хедере.

- `/app/nutrition` — **Дневник**:
  - Форма ввода еды.
  - Переключатель "Тренировка/Отдых" (динамический, без блокировки).
  - Уровень голода.

- `/app/reports` — **Аналитика** (Premium only):
  - Графики веса и калорийности.
  - История по неделям.
  - *Paywall:* Для Free-юзеров заглушка.

- `/app/settings` — **Настройки** (New):
  - Данные профиля (Аватар, Имя).
  - Статус подписки.
  - Контакты тренера (для Premium).
  - Смена пароля.
  - Выход из системы.

### Зона Тренера (`/app/coach`)
- `/app/coach` — **Ростер (Список клиентов)**:
  - Сортировка по приоритету внимания (Traffic Light System).
  - Быстрые метрики: Дельта веса, % выполнения плана.

- `/app/coach/[clientId]` — **Карточка клиента**:
  - Режим "Read-only" дашборда клиента.
  - Редактирование целей (`nutrition_targets`).

### Зона Админа (`/admin`)
- `/admin` — **Управление пользователями**:
  - Поиск юзеров.
  - Ручное включение Premium.
  - Назначение тренера (`coach_id`).

---

## Ключевые изменения логики v1.1

### 1. Динамический тип дня
- **Было:** Тип дня (Тренировка/Отдых) блокировался после первого сохранения
- **Стало:** Пользователь может менять тип дня в любой момент
- **Механика:** При смене типа дня система динамически подтягивает соответствующие `nutrition_targets` из БД и пересчитывает прогресс-бары
- **UX:** Если таргеты не найдены — показывается предупреждение

### 2. Приоритезация в кабинете тренера
- **Сортировка по приоритету внимания (Traffic Light System):**
  - 🔴 **Красный (Red):** Нет отчета > 24 часов ИЛИ перебор ккал > 15% от нормы
  - 🟢 **Зеленый (Green):** Все в порядке, план выполнен
  - ⚪ **Серый (Grey):** Новый клиент / нет данных за сегодня
- **Метрики:**
  - Дельта веса (изменение за неделю)
  - % выполнения плана питания
  - Последний чекин

### 3. Централизация настроек
- **Новая страница `/app/settings`:**
  - Редактирование профиля (Имя, Телефон, Аватар)
  - Просмотр статуса подписки (Free/Premium, дата окончания)
  - Смена пароля
  - Кнопка "Выйти" (перенесена с дашборда)
  - Для Premium: блок "Мой тренер" с контактами

### 4. Улучшенная навигация
- Кнопка "Настройки" добавлена в хедер дашборда
- Более логичная структура меню для мобильных устройств

---

## Детальное описание страниц v1.1

### `/app/settings` — Настройки профиля (NEW)

**Функциональность:**
- **Секция "Профиль":**
  - Редактирование имени (`full_name`)
  - Редактирование телефона (новое поле в `profiles`)
  - Загрузка аватара (опционально, будущая функция)
  
- **Секция "Подписка":**
  - Отображение текущего тарифа (Free / Premium)
  - Дата начала подписки
  - Дата окончания подписки
  - Статус подписки (free / active / cancelled / past_due)
  - Для Premium: кнопка "Связаться с поддержкой" (для отмены)
  
- **Секция "Мой тренер" (только для Premium):**
  - Имя тренера
  - Email тренера
  - Кнопка "Написать тренеру" (будущая функция: чат)
  
- **Секция "Безопасность":**
  - Смена пароля (через Supabase Auth API)
  - Подтверждение текущего пароля перед сменой
  
- **Секция "Действия":**
  - Кнопка "Выйти из системы" (перенесена с дашборда)

**Переходы:**
- → `/app/dashboard` (кнопка "Назад" или после сохранения)
- → `/login` (после выхода)

---

### `/app/nutrition` — Ввод питания (Улучшено)

**Изменения в логике:**
- **Убрана блокировка типа дня:**
  - Переключатель "Тренировка/Отдых" всегда активен
  - При смене типа дня:
    1. Загружаются соответствующие `nutrition_targets` из БД
    2. Пересчитываются прогресс-бары в реальном времени
    3. Если таргеты не найдены — показывается предупреждение: "Цели для этого типа дня не установлены. Обратитесь к тренеру."
  
- **Сохранение типа дня:**
  - При сохранении сохраняется текущий выбранный тип дня
  - При следующем открытии страницы загружается последний сохраненный тип дня
  - Пользователь может изменить тип дня даже после сохранения

**Остальная функциональность без изменений:**
- Ввод нескольких приемов пищи
- Автоматическое определение названия приема по времени
- Ввод веса тела
- Уровень голода (5 emoji)
- Итоговая сводка с прогресс-барами

---

### `/app/coach` — Кабинет тренера (Улучшено)

**Новая логика сортировки:**
- **Приоритетная сортировка (по умолчанию):**
  1. 🔴 Красные (требуют внимания)
  2. ⚪ Серые (нет данных)
  3. 🟢 Зеленые (все ок)
  
- **Критерии для красного статуса:**
  - Нет отчета за последние 24 часа
  - ИЛИ перебор калорий > 15% от целевого значения
  - ИЛИ сильное отклонение по весу (> 2% за неделю)
  
- **Дополнительные метрики в списке:**
  - Дельта веса за неделю (↑/↓ с процентом)
  - % выполнения плана питания за сегодня
  - Индикатор последнего чекина (сегодня / вчера / > 2 дней)

**Фильтрация и сортировка:**
- Фильтр по статусу (все / красные / зеленые / серые)
- Сортировка по имени (A-Z / Z-A)
- Сортировка по дате последнего чекина
- Сортировка по статусу (приоритетная)

---

### `/app/dashboard` — Дашборд клиента (Изменения)

**Изменения:**
- Кнопка "Выйти" перенесена в `/app/settings`
- Добавлена кнопка "Настройки" в хедере (иконка шестеренки)
- Остальная функциональность без изменений

---

## Миграция с v1.0 на v1.1

### Изменения в базе данных
1. **Новое поле в `profiles`:**
   ```sql
   ALTER TABLE profiles ADD COLUMN IF NOT EXISTS phone TEXT;
   ```

2. **Обновление логики `daily_logs`:**
   - Поле `target_type` больше не блокируется после первого сохранения
   - Можно обновлять `target_type` в любой момент

### Изменения в коде
1. **Удаление `dayTypeLocked` state** в `/app/nutrition`
2. **Удаление проверки блокировки** при рендеринге `DayToggle`
3. **Добавление динамической загрузки таргетов** при смене типа дня
4. **Создание новой страницы** `/app/settings`
5. **Обновление сортировки** в `/app/coach` с приоритетной логикой

---

## Roadmap (Будущие улучшения)

### v1.2 (Планируется)
- **Onboarding flow:** Установка начального веса, роста, возраста при первой регистрации
- **Инвайт-коды:** Механизм для тренеров добавлять клиентов самостоятельно
- **Чат с тренером:** Встроенный мессенджер для Premium клиентов
- **Мобильная навигация:** Нижний таб-бар для частых операций

### v1.3 (Планируется)
- **Интеграция платежей:** Stripe/PayPal для автоматического управления подписками
- **Уведомления:** Push-уведомления о напоминаниях ввода данных
- **Экспорт данных:** CSV/PDF отчеты для клиентов