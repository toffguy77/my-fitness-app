# BRD - Business Requirements Document
# Phase 2: The Loop (Петля обратной связи)

**Версия:** 2.0.0  
**Дата:** Декабрь 2025  
**Статус:** Реализовано  
**Предыдущая версия:** MVP v1.0.0

---

## 1. Бизнес-контекст

### 1.1 Проблема MVP v1.0.0

После реализации MVP выявились две критические проблемы:

1. **Слепота тренера:** 
   - Данные клиентов уходят в "черную дыру" (базу данных)
   - Тренер не видит, что происходит с клиентами без SQL-запросов
   - Невозможно оперативно реагировать на проблемы клиентов
   - Нет инструмента для масштабирования бизнеса

2. **Статичность системы:**
   - Хардкод типа дня (`training`) убивает методологию циклирования
   - Невозможно реализовать подходы Fizikl/PHT, где калорийность в день отдыха и тренировки разная
   - Клиент не может корректно отмечать дни отдыха

### 1.2 Бизнес-цель Phase 2

**Превратить цифровой дневник в бизнес-инструмент для продажи услуг coaching.**

Ключевая метрика успеха: **Тренер может видеть статус всех клиентов за 30 секунд и понимать, кому нужна помощь.**

---

## 2. Функциональные требования

### 2.1 Ролевая модель и разделение зон видимости

**Требование:** Разделить приложение на две зоны видимости в зависимости от роли пользователя.

**Логика:**
- При входе проверяется `profiles.role`
- Если `role = 'client'` → редирект на `/` (клиентский дашборд)
- Если `role = 'coach'` → редирект на `/coach` (кабинет тренера)

**Бизнес-ценность:**
- Четкое разделение интерфейсов для разных ролей
- Защита от случайного доступа к чужим данным
- Основа для масштабирования (один тренер → много клиентов)

**Приоритет:** Критический

---

### 2.2 Кабинет тренера (Coach Dashboard)

**Требование:** Создать централизованный инструмент для управления клиентами.

#### 2.2.1 Список клиентов (Roster)

**Функциональность:**
- Вывод всех пользователей, где `profiles.coach_id` = ID текущего тренера
- Метрики в строке:
  - ФИО клиента
  - Последний чекин (дата)
  - Калории за сегодня (факт / план)

**Бизнес-ценность:**
- Быстрый обзор всех клиентов
- Понимание активности клиентов
- Выявление проблемных зон

#### 2.2.2 Система светофора (Traffic Light System)

**Функциональность:**
Визуальный индикатор рядом с каждым клиентом за **сегодня**:

- 🔴 **Red (Требует внимания):**
  - Не заполнил отчет за сегодня ИЛИ
  - Перебор/недобор калорий > 15% от плана
  
- 🟢 **Green (В норме):**
  - Отчет заполнен И
  - Калории в пределах ±15% от плана
  
- ⚪ **Grey (Нет данных):**
  - Данных пока нет (начало дня)

**Бизнес-ценность:**
- Мгновенная визуальная оценка состояния клиентов
- Приоритизация работы тренера
- Ответ на вопрос: "Кому из 50 клиентов сегодня нужна помощь?"

**Приоритет:** Критический

#### 2.2.3 Drill-down (Проваливание в клиента)

**Функциональность:**
- Клик по клиенту открывает его **Client View** (дашборд в режиме Read-only)
- Возможность редактировать `nutrition_targets` для этого клиента

**Бизнес-ценность:**
- Детальный анализ прогресса клиента
- Корректировка планов без необходимости SQL-запросов
- Полный контроль над процессом

**Приоритет:** Высокий

#### 2.2.4 Фильтры и сортировка

**Функциональность:**
- Фильтр по статусу: Все / Требуют внимания / В норме / Нет данных
- Сортировка:
  - По имени (A-Z / Z-A)
  - По дате последнего чекина (новые / старые)
  - По статусу (🔴 → 🟢 → ⚪)

**Бизнес-ценность:**
- Эффективная работа с большим количеством клиентов
- Быстрый поиск проблемных клиентов
- Удобная навигация

**Приоритет:** Средний

---

### 2.3 Динамические цели (Day Type Toggle)

**Требование:** Убрать хардкод типа дня и реализовать переключение между "Тренировка" и "Отдых".

#### 2.3.1 UI компонент

**Функциональность:**
- Переключатель (Toggle/Tabs) в верхней части дашборда: `[Тренировка] | [Отдых]`
- Визуальное выделение активного состояния
- Адаптивный дизайн для мобильных устройств

**Бизнес-ценность:**
- Интуитивный интерфейс
- Соответствие методологии циклирования

#### 2.3.2 Логика переключения

**Функциональность:**
- При загрузке фетчить **оба** таргета из `nutrition_targets` (where day_type = 'training' AND 'rest')
- При переключении таба мгновенно менять цифры "План" (Target) на экране
- При сохранении отчета писать в `daily_logs.target_type` актуальное состояние переключателя

**Бизнес-ценность:**
- Поддержка методологии Fizikl/PHT
- Корректное отслеживание дней отдыха
- Гибкость в планировании питания

**Приоритет:** Критический

#### 2.3.3 Автоматизация

**Функциональность:**
- Если клиент отметил день как "Отдых", система понимает, что тренировки сегодня не было
- Это важно для будущей аналитики и корреляции питания с тренировками

**Бизнес-ценность:**
- Подготовка к расширенной аналитике
- Корректные данные для принятия решений

---

## 3. Технические требования

### 3.1 Backend (Supabase)

#### 3.1.1 Row Level Security (RLS) Policies

**Требование:** Обновить RLS для таблиц `daily_logs` и `nutrition_targets`.

**Текущее состояние:**
- Пользователь видит только свои данные

**Новое состояние:**
- Пользователь видит свои данные **OR**
- Пользователь является тренером этого клиента (проверка через `profiles.coach_id`)

**SQL Policies:**
```sql
-- Тренер может видеть данные своих клиентов
CREATE POLICY "Coaches can view their clients' logs"
ON daily_logs FOR SELECT
USING (
  auth.uid() = user_id 
  OR EXISTS (
    SELECT 1 FROM profiles 
    WHERE profiles.id = daily_logs.user_id 
    AND profiles.coach_id = auth.uid()
  )
);

-- Тренер может редактировать цели своих клиентов
CREATE POLICY "Coaches can update their clients' targets"
ON nutrition_targets FOR UPDATE
USING (
  EXISTS (
    SELECT 1 FROM profiles 
    WHERE profiles.id = nutrition_targets.user_id 
    AND profiles.coach_id = auth.uid()
  )
);
```

**Приоритет:** Критический

#### 3.1.2 Структура данных

**Требование:** Убедиться, что у тестового пользователя созданы записи в `nutrition_targets` для обоих типов дней (`training` и `rest`).

---

### 3.2 Frontend (Next.js)

#### 3.2.1 Middleware для роутинга

**Требование:** Настроить автоматический роутинг по ролям.

**Функциональность:**
- Проверка роли пользователя при каждом запросе
- Редирект тренера на `/coach` при попытке доступа к клиентским страницам
- Редирект клиента на `/` при попытке доступа к `/coach`

**Файл:** `src/middleware.ts`

**Приоритет:** Критический

#### 3.2.2 Утилиты для работы с профилями

**Требование:** Создать переиспользуемые функции для работы с профилями пользователей.

**Функции:**
- `getUserProfile(user)` - получение профиля пользователя
- `getCoachClients(coachId)` - получение списка клиентов тренера

**Файл:** `src/utils/supabase/profile.ts`

**Приоритет:** Высокий

#### 3.2.3 Компонент переключения дня

**Требование:** Создать переиспользуемый компонент для переключения типа дня.

**Файл:** `src/components/DayToggle.tsx`

**Приоритет:** Критический

#### 3.2.4 Компонент просмотра клиента

**Требование:** Создать переиспользуемый компонент для просмотра дашборда клиента.

**Функциональность:**
- Работает в режиме read-only для тренера
- Отображение сводки по питанию
- Возможность редактирования целей (для тренера)

**Файл:** `src/components/ClientDashboardView.tsx`

**Приоритет:** Высокий

---

## 4. Пользовательские сценарии

### Сценарий 1: Тренер входит в систему
1. Тренер открывает `/login`
2. Вводит email и password
3. Нажимает "Войти"
4. **Автоматический редирект на `/coach`** (новое поведение)
5. Видит список всех своих клиентов со статусами
6. Видит, кому нужна помощь (🔴 индикаторы)

### Сценарий 2: Тренер просматривает клиента
1. Тренер на странице `/coach`
2. Видит клиента со статусом 🔴
3. Кликает на клиента
4. Переходит на `/coach/[clientId]`
5. Видит полный дашборд клиента (read-only)
6. Может редактировать цели питания
7. Сохраняет изменения
8. Возвращается к списку клиентов

### Сценарий 3: Клиент переключает тип дня
1. Клиент на дашборде `/`
2. Видит переключатель `[Тренировка] | [Отдых]`
3. Выбирает "Отдых"
4. Видит, что цели изменились (меньше калорий)
5. Переходит на `/nutrition`
6. Вводит данные о питании
7. Сохраняет отчет
8. В `daily_logs.target_type` записывается `'rest'`

### Сценарий 4: Тренер фильтрует клиентов
1. Тренер на `/coach`
2. Нажимает фильтр "🔴 Требуют внимания"
3. Видит только клиентов со статусом 🔴
4. Меняет сортировку на "По дате"
5. Видит клиентов, отсортированных по дате последнего чекина

---

## 5. Метрики успеха

### Количественные метрики:
- **Время до понимания статуса клиентов:** < 30 секунд (было: невозможно без SQL)
- **Количество клиентов, которых тренер может вести:** неограниченно (было: только через SQL)
- **Процент дней с корректным типом:** 100% (было: только 'training')

### Качественные метрики:
- Тренер может работать с приложением без знания SQL
- Клиент может корректно отмечать дни отдыха
- Система поддерживает методологию циклирования

---

## 6. Ограничения и зависимости

### Ограничения:
- Редактирование целей доступно только тренеру (не клиенту)
- Просмотр клиента тренером - только read-only (кроме целей)
- Фильтры и сортировка работают только на стороне клиента (не на сервере)

### Зависимости:
- Структура таблицы `profiles` с полями `role` и `coach_id`
- RLS policies в Supabase настроены корректно
- У клиентов есть записи в `nutrition_targets` для обоих типов дней

---

## 7. Риски и митигация

### Риск 1: Производительность при большом количестве клиентов
**Митигация:** Фильтрация и сортировка на клиенте, можно добавить пагинацию в будущем

### Риск 2: Ошибки в RLS policies
**Митигация:** Тщательное тестирование доступа, проверка прав в middleware

### Риск 3: Клиент забывает переключать тип дня
**Митигация:** UI подсказки, можно добавить автоматическое определение в будущем

---

## 8. Следующие шаги (Phase 3)

1. Автоматическое определение типа дня (на основе данных о тренировках)
2. Уведомления тренеру о проблемных клиентах
3. Массовые действия (назначение целей группе клиентов)
4. Расширенная аналитика для тренера

---

# PRD - Product Requirements Document
# Phase 2: The Loop (Петля обратной связи)

**Версия:** 2.0.0  
**Дата:** Декабрь 2025  
**Статус:** Реализовано

---

## 1. Обзор изменений

Phase 2 добавляет в приложение **петлю обратной связи** между тренером и клиентами, превращая цифровой дневник в полноценный бизнес-инструмент для coaching.

### Ключевые изменения:
1. Ролевая модель с разделением интерфейсов
2. Кабинет тренера с системой светофора
3. Динамические цели с переключателем типа дня
4. Просмотр и редактирование клиентов тренером

---

## 2. Архитектурные изменения

### 2.1 Новая структура маршрутов

```
/                    → Client Dashboard (только для role='client')
/login               → Страница авторизации (обновлена)
/nutrition           → Ввод питания (только для role='client')
/reports             → Отчеты (только для role='client')
/coach               → Кабинет тренера (только для role='coach')
/coach/[clientId]    → Просмотр клиента тренером (только для role='coach')
```

### 2.2 Новые компоненты

```
src/
├── middleware.ts                    (новый - роутинг по ролям)
├── components/
│   ├── DayToggle.tsx               (новый - переключатель дня)
│   └── ClientDashboardView.tsx     (новый - просмотр клиента)
└── utils/supabase/
    └── profile.ts                  (новый - утилиты профилей)
```

---

## 3. Детальные требования к функциям

### 3.1 Middleware (`src/middleware.ts`)

**Описание:** Middleware для автоматического роутинга по ролям.

**Логика:**
1. Проверка авторизации пользователя
2. Загрузка профиля из `profiles` по `id = auth.uid()`
3. Определение роли (`role` из профиля)
4. Редирект в зависимости от роли:
   - `coach` → `/coach` (если пытается зайти на клиентские страницы)
   - `client` → `/` (если пытается зайти на `/coach`)

**Технические детали:**
- Использует `createServerClient` из `@supabase/ssr`
- Работает на уровне Next.js middleware
- Применяется ко всем маршрутам кроме статических файлов

---

### 3.2 Кабинет тренера (`/coach`)

**Описание:** Главная страница для тренеров после авторизации.

#### 3.2.1 Загрузка данных

**Запросы к БД:**
1. Получение списка клиентов: `profiles` where `coach_id = current_user.id` AND `role = 'client'`
2. Для каждого клиента:
   - Отчет за сегодня: `daily_logs` where `user_id = client.id` AND `date = today`
   - Цель за сегодня: `nutrition_targets` where `user_id = client.id` AND `day_type = todayLog.target_type`
   - Последний чекин: `daily_logs` where `user_id = client.id` ORDER BY `date DESC` LIMIT 1

#### 3.2.2 Расчет статуса клиента

**Алгоритм:**
```typescript
if (!todayLog) {
  status = 'red' // Нет отчета
} else if (todayLog && target) {
  diff = abs((todayLog.actual_calories - target.calories) / target.calories)
  if (diff > 0.15) {
    status = 'red' // Перебор/недобор > 15%
  } else {
    status = 'green' // В норме
  }
} else {
  status = 'grey' // Нет данных
}
```

#### 3.2.3 Фильтрация

**Реализация:**
- Фильтр по статусу: `statusFilter ∈ {'all', 'red', 'green', 'grey'}`
- Применяется через `useMemo` для оптимизации

#### 3.2.4 Сортировка

**Реализация:**
- Поля сортировки: `name`, `lastCheckin`, `status`
- Порядок: `asc` / `desc`
- Переключение порядка при повторном клике на то же поле

---

### 3.3 Просмотр клиента тренером (`/coach/[clientId]`)

**Описание:** Страница детального просмотра клиента в режиме read-only.

**Компонент:** `ClientDashboardView` с пропсами:
- `clientId: string` - ID клиента
- `readOnly: boolean = true` - режим только просмотра
- `onTargetsUpdate?: () => void` - callback после обновления целей

**Функциональность:**
- Отображение сводки по питанию за неделю
- Переключатель типа дня (для просмотра разных целей)
- Отображение активных программ
- Редактирование целей питания (только для тренера)

**Проверка прав доступа:**
```typescript
const { data: clientProfile } = await supabase
  .from('profiles')
  .select('coach_id')
  .eq('id', clientId)
  .single()

if (clientProfile.coach_id !== currentUser.id) {
  router.push('/coach') // Нет доступа
}
```

---

### 3.4 Компонент DayToggle (`src/components/DayToggle.tsx`)

**Описание:** Переиспользуемый компонент для переключения типа дня.

**Props:**
```typescript
interface DayToggleProps {
  value: 'training' | 'rest'
  onChange: (value: 'training' | 'rest') => void
}
```

**UI:**
- Две кнопки в контейнере с фоном `bg-gray-100`
- Активная кнопка: белый фон, черный текст, тень
- Неактивная: серый текст, hover эффект

**Использование:**
- Client Dashboard (`/`)
- Nutrition Page (`/nutrition`)
- Client View для тренера (`/coach/[clientId]`)

---

### 3.5 Компонент ClientDashboardView (`src/components/ClientDashboardView.tsx`)

**Описание:** Переиспользуемый компонент для отображения дашборда клиента.

**Props:**
```typescript
interface ClientDashboardViewProps {
  clientId: string
  readOnly?: boolean
  onTargetsUpdate?: () => void
}
```

**Функциональность:**

1. **Загрузка данных:**
   - Цели для обоих типов дней (`training`, `rest`)
   - Логи за последние 7 дней
   - Автоматическое определение дефолтного типа дня

2. **Отображение сводки:**
   - Средние КБЖУ за день
   - Дефицит/профицит калорий
   - Количество дней с отчетами

3. **Редактирование целей (только для тренера):**
   - Кнопка "Редактировать" для каждого типа дня
   - Форма редактирования с полями КБЖУ
   - Сохранение через `nutrition_targets.update()`

**Состояния:**
- `loading` - загрузка данных
- `editingTargets` - редактирование целей (null или объект)
- `savingTargets` - процесс сохранения

---

### 3.6 Обновление Client Dashboard (`/`)

**Изменения:**
1. Добавлен импорт `DayToggle`
2. Состояние `dayType` для переключения типа дня
3. Загрузка целей для обоих типов дней
4. Computed `currentTargets` на основе `dayType`
5. Переключатель в UI после header
6. Использование `currentTargets` вместо `targets`

**Логика переключения:**
- При изменении `dayType` мгновенно меняются отображаемые цели
- При сохранении отчета используется текущий `dayType`

---

### 3.7 Обновление Nutrition Page (`/nutrition`)

**Изменения:**
1. Добавлен импорт `DayToggle`
2. Состояние `dayType` для переключения типа дня
3. Загрузка целей для обоих типов дней
4. Восстановление `dayType` из сохраненного лога (если есть)
5. Переключатель в UI
6. Использование `dayType` при сохранении в `target_type`

**Логика восстановления:**
```typescript
if (logData?.target_type) {
  setDayType(logData.target_type as 'training' | 'rest')
} else {
  // Дефолт на основе наличия целей
}
```

---

### 3.8 Обновление Login Page (`/login`)

**Изменения:**
1. После успешного входа загрузка профиля пользователя
2. Определение роли из профиля
3. Редирект в зависимости от роли:
   - `coach` → `/coach`
   - `client` → `/`

**Код:**
```typescript
const { data: profile } = await supabase
  .from('profiles')
  .select('role')
  .eq('id', data.user.id)
  .single()

const role = profile?.role || 'client'
const redirectPath = role === 'coach' ? '/coach' : '/'
router.push(redirectPath)
```

---

## 4. Модель данных

### 4.1 Обновленные таблицы

#### `profiles`
- `id` (UUID, PK) - ссылается на `auth.users.id`
- `role` (enum: 'client' | 'coach')
- `coach_id` (UUID, FK → `profiles.id`)
- `full_name` (text)
- `email` (text)

#### `nutrition_targets`
- `user_id` (UUID, FK → `profiles.id`)
- `day_type` ('training' | 'rest')
- `calories`, `protein`, `fats`, `carbs` (integer)
- `is_active` (boolean)

#### `daily_logs`
- `user_id` (UUID, FK → `profiles.id`)
- `date` (date)
- `target_type` ('training' | 'rest') - **новое поле используется**
- `actual_calories`, `actual_protein`, `actual_fats`, `actual_carbs` (integer)

### 4.2 RLS Policies

**Для `daily_logs`:**
- Пользователь видит свои данные
- Тренер видит данные своих клиентов (через `profiles.coach_id`)

**Для `nutrition_targets`:**
- Пользователь видит свои данные
- Тренер видит и может редактировать данные своих клиентов

---

## 5. Пользовательские потоки

### Поток 1: Тренер работает с клиентами

```
1. Вход → /coach
2. Просмотр списка клиентов
3. Фильтрация по статусу 🔴
4. Клик на клиента → /coach/[clientId]
5. Просмотр дашборда клиента
6. Редактирование целей питания
7. Сохранение
8. Возврат к списку
```

### Поток 2: Клиент использует переключатель дня

```
1. Вход → /
2. Видит переключатель [Тренировка] | [Отдых]
3. Выбирает "Отдых"
4. Видит измененные цели (меньше калорий)
5. Переходит на /nutrition
6. Вводит данные
7. Сохраняет отчет
8. target_type = 'rest' сохраняется в БД
```

---

## 6. Технические детали реализации

### 6.1 Оптимизация производительности

- Использование `useMemo` для фильтрации и сортировки клиентов
- Параллельная загрузка данных через `Promise.all`
- Мемоизация вычислений сводки

### 6.2 Обработка ошибок

- Try-catch блоки вокруг всех async операций
- Проверка прав доступа перед загрузкой данных
- Валидация данных перед сохранением

### 6.3 Безопасность

- RLS policies на уровне БД
- Проверка прав в middleware
- Проверка прав в компонентах перед отображением данных

---

## 7. Известные ограничения

1. **Фильтрация на клиенте:** При большом количестве клиентов (>100) может быть медленно
2. **Нет пагинации:** Все клиенты загружаются сразу
3. **Нет поиска:** Только фильтры и сортировка
4. **Редактирование целей:** Только по одному типу дня за раз

---

## 8. Метрики и аналитика

### Метрики для отслеживания:
- Количество клиентов у тренера
- Среднее время до просмотра проблемного клиента
- Процент дней с корректным `target_type`
- Количество редактирований целей тренером

---

## 9. Тестирование

### Тестовые сценарии:
1. Тренер входит → редирект на `/coach`
2. Клиент входит → редирект на `/`
3. Тренер пытается зайти на `/nutrition` → редирект на `/coach`
4. Клиент пытается зайти на `/coach` → редирект на `/`
5. Переключение типа дня меняет цели
6. Сохранение отчета с правильным `target_type`
7. Фильтрация клиентов работает корректно
8. Сортировка клиентов работает корректно
9. Редактирование целей тренером сохраняется
10. Проверка прав доступа к клиенту работает

---

## 10. Документация для разработчиков

### Новые файлы:
- `src/middleware.ts` - роутинг по ролям
- `src/components/DayToggle.tsx` - компонент переключения
- `src/components/ClientDashboardView.tsx` - просмотр клиента
- `src/utils/supabase/profile.ts` - утилиты профилей

### Обновленные файлы:
- `src/app/login/page.tsx` - роутинг по ролям
- `src/app/page.tsx` - добавлен DayToggle
- `src/app/nutrition/page.tsx` - добавлен DayToggle
- `src/app/coach/page.tsx` - новый кабинет тренера
- `src/app/coach/[clientId]/page.tsx` - просмотр клиента

---

**Конец документации Phase 2**

