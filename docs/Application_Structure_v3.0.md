# Структура приложения My Fitness App v3.0

## Обзор

My Fitness App — это Freemium SaaS платформа для управления питанием и отслеживания прогресса. Документация v3.0 отражает **текущую реализацию** в коде (по состоянию на 17 декабря 2025).

---

## Роли пользователей

### 1. Клиент (Client)
- **Роль по умолчанию** при регистрации
- Может быть **Free** (бесплатный) или **Premium** (платный)
- Free клиенты имеют доступ к базовому функционалу дневника питания
- Premium клиенты получают доступ к отчетам, аналитике и работе с тренером
- **Ограничение:** Free клиенты не могут иметь тренеров (защищено триггером в БД)

### 2. Тренер (Coach)
- Может просматривать данные своих клиентов
- Видит статус выполнения планов питания (Traffic Light System)
- Может редактировать цели по питанию для клиентов
- Имеет доступ к кабинету тренера с приоритетной сортировкой клиентов

### 3. Супер-администратор (Super Admin)
- Полный доступ ко всем функциям
- Управление пользователями, ролями и подписками
- Назначение тренеров клиентам
- Ручное управление подписками (до интеграции платежной системы)

---

## Публичные страницы (не требуют авторизации)

### `/` — Landing Page (Главная страница)

**Файл:** `src/app/page.tsx`

**Функциональность:**
- Маркетинговая страница с описанием приложения
- Секции:
  - Hero Section: заголовок, подзаголовок, CTA кнопка "Начать бесплатно"
  - The Problem: 3 блока (Иллюзия контроля, Эмоциональный шум, Одиночество)
  - The Solution: список преимуществ
  - The Hook: сравнение Free vs Pro в виде таблицы
  - Footer CTA: финальный призыв к регистрации
  - Footer: копирайт

**Переходы:**
- → `/register` (кнопка "Начать бесплатно" в Hero)
- → `/register` (кнопка "Регистрация" в Footer CTA)
- Автоматический редирект на `/app/dashboard` (для авторизованных клиентов)
- Автоматический редирект на `/admin` (для super_admin)
- Автоматический редирект на `/app/coach` (для тренеров)

**Особенности:**
- Адаптивный дизайн (mobile first)
- Минималистичный черно-белый стиль с акцентным цветом

---

### `/login` — Страница входа

**Файл:** `src/app/login/page.tsx`

**Функциональность:**
- Авторизация существующих пользователей (email + password)
- Проверка существующей сессии при загрузке (автоматический редирект, если уже авторизован)
- Определение роли пользователя и редирект на соответствующую страницу:
  - `client` → `/app/dashboard`
  - `coach` → `/app/coach`
  - `super_admin` → `/admin`

**Переходы:**
- → `/app/dashboard` (после успешного входа для клиентов)
- → `/app/coach` (после успешного входа для тренеров)
- → `/admin` (после успешного входа для super_admin)
- → `/register` (ссылка "Нет аккаунта? Зарегистрируйтесь")

**Особенности:**
- Автоматический редирект при наличии активной сессии
- Определение роли из таблицы `profiles`

---

### `/register` — Страница регистрации

**Файл:** `src/app/register/page.tsx`

**Функциональность:**
- Регистрация новых пользователей
- Ввод данных: email, password, full_name (опционально)
- Автоматическое создание профиля с:
  - Ролью: `client`
  - Статусом подписки: `free`
  - Уровнем подписки: `basic`

**Переходы:**
- → `/app/dashboard` (после успешной регистрации)
- → `/login` (ссылка "Уже есть аккаунт? Войдите")

**Особенности:**
- Все новые пользователи регистрируются как Free клиенты
- Профиль создается автоматически после успешной регистрации в Auth

---

## Защищенные страницы для клиентов

### `/app/dashboard` — Дашборд клиента

**Файл:** `src/app/app/dashboard/page.tsx`

**Функциональность:**

#### 1. Заголовок
- Название страницы "Дашборд"
- Текущая дата (формат: день недели, число, месяц)
- Кнопка "Настройки" (иконка шестеренки) → переход на `/app/settings`

#### 2. Переключатель типа дня (DayToggle)
- Отображается только если есть цели для тренировочных или дней отдыха
- Позволяет переключаться между "Тренировка" и "Отдых"
- Используется для определения текущих целевых значений КБЖУ

#### 3. Сводка за сегодня
- **Отображается только если есть данные за сегодня (`todayLog` существует)**
- **КБЖУ за сегодня:**
  - Калории: `actual_calories` / `target_calories` (для Premium)
  - Белки: `actual_protein` г / `target_protein`г (для Premium)
  - Жиры: `actual_fats` г / `target_fats`г (для Premium)
  - Углеводы: `actual_carbs` г / `target_carbs`г (для Premium)
  - Для Premium пользователей отображаются целевые значения в формате "140 / 100"
  - Кнопка "Редактировать" → переход на `/app/nutrition`

- **Быстрые действия:**
  - **Вес тела:** Кликабельный блок с пунктирной рамкой
    - При клике открывается поле ввода
    - Отображается последний введенный вес за сегодня
    - Если веса нет, показывается "Нажмите, чтобы добавить"
    - Сохранение происходит при потере фокуса или нажатии Enter
    - Вес сохраняется в `daily_logs.weight` за текущую дату
  
  - **Добавить прием пищи:** Большая кнопка с пунктирной рамкой
    - Открывает модальное окно `AddMealModal`
    - Позволяет добавить прием пищи с выбором даты (для поправок)

- **Приемы пищи за сегодня:**
  - Список всех приемов пищи из `todayLog.meals` (массив)
  - Каждый прием пищи отображается как карточка с:
    - Номером приема (#1, #2...)
    - Названием приема пищи
    - КБЖУ приема
    - Иконками редактирования (✏️) и удаления (🗑️)
  - Клик по карточке → переход на `/app/nutrition?edit={meal.id}`
  - Кнопка "+ Добавить еще один прием пищи" внизу списка
  - **Предупреждение:** Если есть данные КБЖУ, но нет детализации по приемам, показывается желтое предупреждение

#### 4. Вес (история)
- Отображается только если есть записи веса за последние 7 дней
- Показывает последние 7 записей веса
- Рассчитывает изменение веса (первая запись vs последняя)
- Визуальная индикация: зеленый для потери веса, красный для набора

#### 5. Питание за неделю
- Статистика за последние 7 дней
- Средние значения КБЖУ за неделю
- Количество дней с записями (из 7)
- Индикатор профицита/дефицита калорий
- Кнопка "Ввести данные" → переход на `/app/nutrition`

#### 6. Тренировки (заглушка)
- Информационное сообщение: "Функция в разработке"
- Заглушка для будущей функциональности

#### 7. Активные программы
- Отображает текущий план питания
- Показывает тип дня (Тренировка/Отдых)
- Отображает целевые значения КБЖУ
- Статус "Активна" (зеленый бейдж)

#### 8. Быстрые действия
- Кнопка "Ввести питание" → переход на `/app/nutrition`
- Для Premium: кнопка "Отчеты и аналитика" → переход на `/app/reports`
- Для Free: заглушка с текстом "Доступно с Premium подпиской"

**Переходы:**
- → `/app/settings` (кнопка "Настройки" в хедере)
- → `/app/nutrition` (кнопка "Ввести данные", "Редактировать", добавление приема пищи)
- → `/app/nutrition?edit={mealId}` (клик по приему пищи или иконка редактирования)
- → `/app/reports` (кнопка "Отчеты и аналитика" — только для Premium)
- → `/login` (если пользователь не авторизован)

**Особенности:**
- Для Premium пользователей отображаются целевые значения КБЖУ
- Тип дня определяется из `todayLog.target_type` или из наличия целей
- Вес можно редактировать прямо на дашборде
- Модальное окно `AddMealModal` для добавления приемов пищи с выбором даты

---

### `/app/nutrition` — Ввод данных о питании

**Файл:** `src/app/app/nutrition/page.tsx`

**Функциональность:**

#### 1. Заголовок
- Название страницы "Ввод питания"
- Кнопка "Назад" → переход на `/app/dashboard`

#### 2. Переключатель типа дня (DayToggle)
- **Динамический переключатель (без блокировки):**
  - Пользователь может менять тип дня в любой момент
  - При смене типа дня автоматически пересчитываются прогресс-бары
  - Если таргеты для выбранного типа дня не найдены, показывается предупреждение: "Цели для этого типа дня не установлены. Обратитесь к тренеру."
- Отображается только если есть цели для тренировочных или дней отдыха

#### 3. Вес тела
- Поле ввода веса (кг) с шагом 0.1
- Сохранение веса вместе с данными о питании

#### 4. Приемы пищи
- **По умолчанию:** Создается один прием пищи при загрузке страницы
- **Название приема:** Автоматически определяется по времени суток:
  - 6-10: "Завтрак"
  - 10-13: "Второй завтрак"
  - 13-16: "Обед"
  - 16-20: "Полдник"
  - 20-6: "Ужин"
- **Для каждого приема:**
  - Название (редактируемое)
  - Дата приема пищи (выбор даты для поправок)
  - Вес (г)
  - Калории
  - Белки (г)
  - Жиры (г)
  - Углеводы (г)
  - Кнопка "Удалить" (✕) — отключена, если приемов пищи <= 1
- **Кнопка "+ Добавить прием пищи"** — добавляет новый прием пищи в список

#### 5. Уровень голода
- 5 emoji-кнопок для выбора уровня:
  - 1: 😋 (Совсем нет голода)
  - 2: 🙂 (Легкий голод)
  - 3: 😊 (Умеренный голод)
  - 4: 😟 (Сильный голод)
  - 5: 🤯 (Зверский голод)

#### 6. Итоговая сводка "Всего за день"
- Автоматический расчет суммарных КБЖУ из всех приемов пищи
- Для Premium: сравнение с целевыми значениями
- Прогресс-бары для визуализации выполнения плана:
  - Калории: цвет зависит от процента выполнения
  - Белки, Жиры, Углеводы: отдельные прогресс-бары

#### 7. Кнопка "Сохранить отчет"
- Сохранение данных в `daily_logs`
- Валидация: проверка, что введены данные хотя бы об одном приеме пищи

**Логика сохранения:**
- При сохранении:
  1. Получается существующий лог за сегодня (`meals` и `target_type`)
  2. Новые/обновленные приемы пищи объединяются с существующими
  3. Сохраняется текущий выбранный тип дня (`dayType`)
  4. Пересчитываются итоговые КБЖУ из всех приемов пищи за день
  5. Выполняется upsert в `daily_logs`
- После успешного сохранения: редирект на `/app/dashboard` с обновлением данных

**Переходы:**
- → `/app/dashboard` (после сохранения или нажатия "Назад")
- → `/login` (если пользователь не авторизован)

**Особенности:**
- Данные не перезаписывают существующие, а добавляются к ним (merge по `id` приема пищи)
- Тип дня можно менять в любой момент (динамический, без блокировки)
- При смене типа дня прогресс-бары пересчитываются автоматически
- Минимум 1 прием пищи должен остаться (нельзя удалить последний)

---

### `/app/reports` — Отчеты и аналитика (только Premium)

**Файл:** `src/app/app/reports/page.tsx`

**Функциональность:**

#### Для Free пользователей:
- Показывается компонент `Paywall` (заглушка с предложением активировать Premium)
- Кнопка "← Назад" → переход на `/app/dashboard`

#### Для Premium пользователей:
- **Заголовок:**
  - Название "Сводка"
  - Подзаголовок "Отчеты по дням и неделям"
  - Кнопка "← Назад" → переход на `/app/dashboard`
  - Кнопка "Выйти" (иконка LogOut) → выход из системы

- **Сегодня:**
  - Отображается лог за сегодня (если есть)
  - Показывает: дату, КБЖУ, вес (если есть), заметки (если есть)

- **Группировка по неделям:**
  - Все логи группируются по неделям (номер недели в году)
  - Для каждой недели показывается диапазон дат
  - Внутри недели логи отсортированы по дате (по убыванию)
  - Для каждого лога отображается:
    - Дата и день недели
    - КБЖУ
    - Вес (если есть)

**Переходы:**
- → `/app/dashboard` (кнопка "← Назад")
- → `/login` (после выхода из системы)

**Особенности:**
- Защищено middleware (редирект на `/app/dashboard` для Free пользователей)
- Дополнительная проверка Premium статуса на уровне компонента
- Группировка по неделям с автоматическим определением номера недели

---

### `/app/settings` — Настройки профиля

**Файл:** `src/app/app/settings/page.tsx`

**Функциональность:**

#### 1. Заголовок
- Название "Настройки"
- Подзаголовок "Управление профилем"
- Кнопка "Назад" (стрелка влево) → переход на `/app/dashboard`

#### 2. Секция "Профиль"
- Отображение email пользователя
- Редактирование имени (`full_name`)
- Редактирование телефона (`phone`)
- Кнопка "Сохранить изменения"
- Сообщения об успехе/ошибке

#### 3. Секция "Подписка"
- Отображение текущего тарифа (Free / Premium)
- Дата начала подписки (если есть)
- Дата окончания подписки (если есть)
- Для Free пользователей: предупреждение "Для активации Premium подписки обратитесь к администратору или вашему тренеру."

#### 4. Секция "Мой тренер" (только для Premium)
- Отображается только если:
  - Пользователь Premium (`subscription_status === 'active' && subscription_tier === 'premium'`)
  - У пользователя назначен тренер (`coach_id` не null)
- Показывает:
  - Имя тренера
  - Email тренера
  - Кнопка "Написать тренеру" (открывает `mailto:` ссылку)

#### 5. Секция "Безопасность"
- Смена пароля:
  - Поле "Текущий пароль"
  - Поле "Новый пароль" (минимум 6 символов)
  - Поле "Подтвердите новый пароль"
  - Валидация: проверка совпадения паролей
  - Кнопка "Изменить пароль"
  - Сообщения об успехе/ошибке

#### 6. Секция "Выход из системы"
- Кнопка "Выйти из системы" (красная)
- Выполняет `supabase.auth.signOut()`
- Редирект на `/login`

**Переходы:**
- → `/app/dashboard` (кнопка "Назад" или после сохранения)
- → `/login` (после выхода из системы)
- → `/login` (если пользователь не авторизован)

**Особенности:**
- Все данные профиля загружаются при открытии страницы
- Данные тренера загружаются только для Premium пользователей с назначенным тренером
- Смена пароля выполняется через Supabase Auth API (`updateUser`)

---

## Защищенные страницы для тренеров

### `/app/coach` — Кабинет тренера

**Файл:** `src/app/app/coach/page.tsx`

**Функциональность:**

#### 1. Заголовок
- Название "Кабинет тренера"
- Подзаголовок "Управление клиентами"
- Кнопка "Выйти" (иконка LogOut) → выход из системы

#### 2. Список клиентов
- **Фильтрация по статусу:**
  - Все
  - 🔴 Требуют внимания (red)
  - 🟢 В норме (green)
  - ⚪ Нет данных (grey)

- **Сортировка:**
  - По имени (A-Z / Z-A)
  - По дате последнего чекина
  - По статусу (приоритетная сортировка: Red > Grey > Green)
  - **По умолчанию:** Сортировка по статусу (Red сверху)

- **Для каждого клиента отображается:**
  - Аватар (иконка пользователя)
  - Имя и email
  - Статус подписки (Free / Premium)
  - Статус выполнения плана (🔴 / 🟢 / ⚪)
  - Последний чекин (дата)
  - Текущие калории vs целевые (если есть данные)

#### 3. Логика определения статуса (Traffic Light System)
- **🔴 Красный (Red):**
  - Нет отчета за последние 24 часа ИЛИ
  - Перебор/недобор калорий > 15% от целевого значения
  
- **🟢 Зеленый (Green):**
  - Есть данные за сегодня
  - Отклонение калорий <= 15% от целевого значения
  
- **⚪ Серый (Grey):**
  - Нет данных за сегодня
  - Нет данных вообще (новый клиент)

#### 4. Приоритетная сортировка
- По умолчанию сортировка по статусу
- Порядок приоритета: Red (1) > Grey (2) > Green (3)
- При сортировке по статусу всегда по возрастанию (Red сверху)

**Переходы:**
- → `/app/coach/[clientId]` (клик по клиенту — просмотр детального дашборда)
- → `/login` (кнопка выхода)
- → `/` (если пользователь не тренер)

**Особенности:**
- Доступ только для пользователей с ролью `coach`
- Автоматический редирект на `/` для не-тренеров
- Загружаются только клиенты, у которых `coach_id` равен ID текущего тренера
- Для каждого клиента загружаются данные за сегодня и последний чекин

---

### `/app/coach/[clientId]` — Просмотр дашборда клиента (для тренера)

**Файл:** `src/app/app/coach/[clientId]/page.tsx`

**Функциональность:**
- **Read-only просмотр дашборда клиента:**
  - Использует компонент `ClientDashboardView` с параметром `readOnly={true}`
  - Отображает все данные, которые видит клиент на своем дашборде:
    - Сводка за сегодня
    - Статистика за неделю
    - История веса
    - Активные программы питания
  - **Редактирование целей по питанию:**
    - Тренер может редактировать целевые значения КБЖУ для тренировочных и дней отдыха
    - Переключение между типами дней
    - Сохранение изменений в таблицу `nutrition_targets`

**Переходы:**
- → `/app/coach` (кнопка "Назад" — возврат к списку клиентов)
- → `/login` (если пользователь не авторизован)
- → `/app/coach` (если текущий пользователь не является тренером этого клиента)

**Особенности:**
- Проверка прав: тренер может просматривать только своих клиентов
- Тренер видит данные клиента в режиме только для чтения
- Может редактировать только цели по питанию (nutrition targets)
- Использует компонент `ClientDashboardView` для переиспользования логики

---

## Защищенные страницы для супер-администраторов

### `/admin` — Панель супер-администратора

**Файл:** `src/app/admin/page.tsx`

**Функциональность:**

#### 1. Заголовок
- Название "Панель администратора"
- Подзаголовок "Управление пользователями и подписками"
- Кнопка "Выйти" (иконка LogOut) → выход из системы

#### 2. Статистика
- Всего пользователей
- Клиенты
- Тренеры
- Premium подписки

#### 3. Фильтры и поиск
- **Поиск:** По email или имени пользователя
- **Фильтр по роли:** Все / Клиенты / Тренеры / Супер-админы
- **Фильтр по подписке:** Все / Бесплатные / Активные / Отмененные / Просроченные

#### 4. Таблица пользователей
- Колонки:
  - Пользователь (имя, email)
  - Роль (бейдж с цветом)
  - Подписка (статус и уровень)
  - Тренер (имя тренера или "-")
  - Дата регистрации
  - Действия (кнопка "Редактировать")

#### 5. Модальное окно редактирования пользователя
- **Редактируемые поля:**
  - Имя (`full_name`)
  - Роль (`role`): client / coach / super_admin
  - Статус подписки (`subscription_status`): free / active / cancelled / past_due
  - Уровень подписки (`subscription_tier`): basic / premium
  - Тренер (`coach_id`): Dropdown со списком всех тренеров (только для клиентов)
  - Дата начала подписки (`subscription_start_date`)
  - Дата окончания подписки (`subscription_end_date`)

- **Кнопки:**
  - "Отмена" — закрывает модальное окно
  - "Сохранить" — сохраняет изменения в БД

**Переходы:**
- → `/login` (кнопка выхода)
- → `/app/dashboard` (автоматический редирект для не-админов)

**Особенности:**
- Доступ только для пользователей с ролью `super_admin`
- Полный контроль над всеми пользователями и их подписками
- Используется для ручного управления подписками (до интеграции платежной системы)
- При редактировании тренера для клиента загружается список всех пользователей с ролью `coach`

---

## Система навигации и защиты маршрутов

### Middleware (`src/middleware.ts`)

**Функциональность:**
- Проверка авторизации для всех защищенных маршрутов
- Определение роли пользователя из таблицы `profiles`
- Автоматические редиректы на основе роли:
  - Авторизованные пользователи на `/` → редирект на соответствующую страницу
  - Неавторизованные пользователи на защищенные маршруты → редирект на `/login`

**Правила доступа:**
- **Публичные маршруты:** `/`, `/login`, `/register`
- **Защищенные маршруты:** `/app/*`, `/admin`
- **Ролевые ограничения:**
  - `/app/reports` — только Premium пользователи (проверка `subscription_status === 'active' && subscription_tier === 'premium'`)
  - `/app/coach` — только тренеры (проверка `role === 'coach'`)
  - `/admin` — только super_admin (проверка `role === 'super_admin'`)

**Логика редиректов:**
- Авторизованный пользователь на `/`:
  - `super_admin` → `/admin`
  - `coach` → `/app/coach`
  - `client` → `/app/dashboard`
- Неавторизованный пользователь на защищенный маршрут → `/login`
- Premium проверка для `/app/reports` → редирект на `/app/dashboard` если не Premium
- Ролевая проверка для `/app/coach` → редирект на `/app/dashboard` если не тренер
- Ролевая проверка для `/admin` → редирект на `/app/dashboard` если не super_admin

---

## Особенности реализации

### Feature Gating (Ограничение доступа к функциям)

1. **Middleware уровень:**
   - Проверка Premium статуса для `/app/reports`
   - Проверка роли для `/app/coach` и `/admin`

2. **Компонентный уровень:**
   - Компонент `Paywall` для отображения заглушки для Free пользователей
   - Условный рендеринг на основе `subscription_status` и `subscription_tier`
   - Проверка `hasActiveSubscription()` для отображения Premium функций

### Типы дней (Day Types)

- **Тренировочный день (training):** более высокие целевые значения КБЖУ
- **День отдыха (rest):** более низкие целевые значения КБЖУ
- **Динамическое переключение:** Тип дня можно менять в любой момент (без блокировки)
- **Сохранение:** При сохранении сохраняется текущий выбранный тип дня
- **Автоматический пересчет:** При смене типа дня прогресс-бары пересчитываются автоматически через `useMemo`

### Хранение данных о питании

- Данные о приемах пищи хранятся в формате JSONB в колонке `meals` таблицы `daily_logs`
- Каждый прием пищи содержит:
  - `id`: уникальный идентификатор
  - `title`: название приема пищи
  - `weight`: вес (г)
  - `calories`: калории
  - `protein`: белки (г)
  - `fats`: жиры (г)
  - `carbs`: углеводы (г)
  - `mealDate`: дата приема пищи (для поправок)
  - `createdAt`: время создания (для сортировки)
- При добавлении новых приемов пищи данные объединяются с существующими (merge по `id`)
- При сохранении пересчитываются итоговые КБЖУ из всех приемов пищи за день

### Система статусов для тренеров (Traffic Light System)

- **🔴 Красный (Red):**
  - Нет отчета за последние 24 часа ИЛИ
  - Перебор/недобор калорий > 15% от целевого значения
  
- **🟢 Зеленый (Green):**
  - Есть данные за сегодня
  - Отклонение калорий <= 15% от целевого значения
  
- **⚪ Серый (Grey):**
  - Нет данных за сегодня
  - Нет данных вообще (новый клиент)

- **Приоритетная сортировка:**
  - По умолчанию сортировка по статусу
  - Порядок: Red (1) > Grey (2) > Green (3)
  - Red клиенты всегда сверху списка

### Уровень голода

- 5 уровней с emoji-кнопками:
  - 1: 😋 (Совсем нет голода)
  - 2: 🙂 (Легкий голод)
  - 3: 😊 (Умеренный голод)
  - 4: 😟 (Сильный голод)
  - 5: 🤯 (Зверский голод)
- Сохраняется в `daily_logs.hunger_level`

### Автоматическое определение названия приема пищи

- Функция `getMealNameByTime(hour)` определяет название по времени суток:
  - 6-10: "Завтрак"
  - 10-13: "Второй завтрак"
  - 13-16: "Обед"
  - 16-20: "Полдник"
  - 20-6: "Ужин"
- Используется при создании нового приема пищи по умолчанию

---

## Технические детали

### Используемые технологии

- **Frontend:** Next.js 16 (App Router), React 19, Tailwind CSS v4
- **Backend:** Supabase (Authentication, Database, RLS)
- **State Management:** React Hooks (useState, useEffect, useMemo)
- **Routing:** Next.js App Router с middleware для защиты маршрутов
- **Icons:** Lucide React

### База данных

#### Таблица `profiles`
- `id`: UUID (PK, FK к `auth.users.id`)
- `email`: TEXT
- `full_name`: TEXT (nullable)
- `phone`: TEXT (nullable) — добавлено в v1.1
- `role`: ENUM ('client', 'coach', 'super_admin')
- `coach_id`: UUID (nullable, FK к `profiles.id`)
- `avatar_url`: TEXT (nullable)
- `subscription_status`: TEXT ('free', 'active', 'cancelled', 'past_due')
- `subscription_tier`: TEXT ('basic', 'premium')
- `subscription_start_date`: TIMESTAMPTZ (nullable)
- `subscription_end_date`: TIMESTAMPTZ (nullable)
- `created_at`: TIMESTAMPTZ
- `updated_at`: TIMESTAMPTZ

#### Таблица `daily_logs`
- `id`: UUID (PK)
- `user_id`: UUID (FK к `profiles.id`)
- `date`: DATE
- `actual_calories`: INTEGER
- `actual_protein`: INTEGER
- `actual_fats`: INTEGER
- `actual_carbs`: INTEGER
- `weight`: DECIMAL(5,2) (nullable)
- `hunger_level`: INTEGER (nullable)
- `energy_level`: INTEGER (nullable)
- `notes`: TEXT (nullable)
- `target_type`: TEXT ('training', 'rest') (nullable)
- `meals`: JSONB (массив объектов Meal)
- `created_at`: TIMESTAMPTZ
- `updated_at`: TIMESTAMPTZ
- **Unique constraint:** `(user_id, date)`

#### Таблица `nutrition_targets`
- `id`: UUID (PK)
- `user_id`: UUID (FK к `profiles.id`)
- `day_type`: TEXT ('training', 'rest')
- `calories`: INTEGER
- `protein`: INTEGER
- `fats`: INTEGER
- `carbs`: INTEGER
- `is_active`: BOOLEAN
- `created_at`: TIMESTAMPTZ
- `updated_at`: TIMESTAMPTZ

### Безопасность

- **Row Level Security (RLS)** в Supabase для защиты данных
- **Middleware** для проверки авторизации и ролей
- **Компонентная проверка** Premium статуса для дополнительной защиты
- **PostgreSQL триггеры:**
  - `prevent_coach_for_free_clients()` — предотвращает назначение тренеров Free клиентам
- **SECURITY DEFINER функции:**
  - `is_super_admin(user_id)` — проверка прав super_admin без рекурсии RLS
  - `is_coach(user_id)` — проверка роли coach без рекурсии RLS
  - `is_client_coach(client_id, potential_coach_id)` — проверка связи тренер-клиент

### Адаптивность

- **Mobile First подход:**
  - Все страницы используют `w-full` на мобильных устройствах
  - Ограничение ширины (`max-w-md`, `max-w-4xl`, `max-w-7xl`) применяется только на больших экранах через `md:` breakpoint
  - Адаптивные отступы: `p-4 sm:p-6`
  - Адаптивные grid-колонки: `grid-cols-1 sm:grid-cols-2`
  - Адаптивные размеры текста: `text-4xl sm:text-5xl md:text-6xl`

---

## Компоненты

### `DayToggle` (`src/components/DayToggle.tsx`)
- Переключатель между "Тренировка" и "Отдых"
- Props: `value`, `onChange`, `disabled?` (опционально)
- Визуальное выделение выбранного варианта

### `Paywall` (`src/components/Paywall.tsx`)
- Заглушка для Free пользователей
- Показывает размытый контент и сообщение о необходимости Premium подписки
- Кнопка "Вернуться на дашборд"

### `ClientDashboardView` (`src/components/ClientDashboardView.tsx`)
- Переиспользуемый компонент дашборда клиента
- Используется на `/app/dashboard` и `/app/coach/[clientId]`
- Props: `clientId`, `readOnly?`, `onTargetsUpdate?`
- Поддерживает режим только для чтения (для тренеров)
- Позволяет тренерам редактировать цели по питанию

---

## Примечания

1. **Старые маршруты:** В коде присутствуют старые маршруты `/coach`, `/nutrition`, `/reports`, которые дублируют функциональность новых маршрутов `/app/coach`, `/app/nutrition`, `/app/reports`. Рекомендуется использовать только маршруты с префиксом `/app/`.

2. **Free клиенты и тренеры:** Free клиенты не могут иметь тренеров (защищено триггером в базе данных `prevent_coach_for_free_clients`).

3. **Платежи:** В текущей версии подписки управляются вручную через админ-панель. Интеграция платежной системы планируется в будущем.

4. **Динамический тип дня:** В отличие от предыдущих версий, тип дня можно менять в любой момент. Блокировка была удалена в v1.1.

5. **Модальное окно добавления приема пищи:** Реализовано на дашборде (`AddMealModal`), позволяет добавлять приемы пищи с выбором даты для поправок.

---

*Документ обновлен: 17 декабря 2025 (на основе текущей реализации в коде)*

