# Миграции базы данных

Все SQL-скрипты для настройки базы данных проекта My Fitness App.

---

## 🚀 Быстрый старт

### Для новой базы данных

Если вы создаете базу данных с нуля, используйте единый скрипт:

- **[setup_database.sql](./setup_database.sql)** — полный сетап базы данных с нуля

Этот скрипт включает:
- ✅ Создание всех типов (ENUM)
- ✅ Создание всех таблиц
- ✅ Создание всех индексов
- ✅ Создание всех функций
- ✅ Создание всех триггеров
- ✅ Настройку RLS политик
- ✅ Вставку начальных данных (достижения)

### Выполнение скрипта

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое файла `setup_database.sql`
3. Вставьте в SQL Editor
4. Нажмите "Run"

> ⚠️ **ВАЖНО:** Этот скрипт создает базу данных с нуля. Используйте только для новых проектов или полного пересоздания базы.

---

## 📋 Структура базы данных

Скрипт создает следующие таблицы:

### Основные таблицы
- `profiles` — профили пользователей (клиенты, кураторы, админы)
- `nutrition_targets` — цели питания (тренировочные дни и дни отдыха)
- `daily_logs` — дневные логи питания и веса

### Функциональные таблицы
- `curator_notes` — заметки куратора для клиентов
- `messages` — сообщения чата между куратором и клиентом
- `products` — кэш продуктов из внешних API
- `user_products` — пользовательские продукты
- `product_usage_history` — история использования продуктов
- `favorite_products` — избранные продукты
- `invite_codes` — инвайт-коды для регистрации
- `invite_code_usage` — история использования инвайт-кодов
- `ocr_scans` — история OCR сканирований
- `achievements` — справочник достижений
- `user_achievements` — достижения пользователей
- `notification_settings` — настройки уведомлений
- `pending_notifications` — очередь уведомлений

### Функции
- `is_super_admin()` — проверка роли super_admin
- `is_curator()` — проверка роли curator
- `is_client_curator()` — проверка связи куратор-клиент
- `validate_calories_match_macros()` — валидация КБЖУ
- `validate_nutrition_limits()` — проверка лимитов
- `get_expired_subscriptions()` — получение истекших подписок
- `get_expiring_subscriptions()` — получение подписок, истекающих через N дней
- `deactivate_expired_subscriptions()` — деактивация истекших подписок
- `use_invite_code()` — использование инвайт-кода
- `check_achievements()` — проверка и присвоение достижений
- `update_product_usage_count()` — обновление счетчика использования продукта
- `update_updated_at_column()` — автоматическое обновление updated_at
- `check_and_update_subscription_status()` — автоматическое обновление статуса подписки
- `check_message_rate_limit()` — проверка rate limit для сообщений

### Триггеры
- `nutrition_validation_trigger` — валидация данных о питании
- `update_subscription_status_on_expiry` — автоматическое обновление статуса подписки
- `trigger_update_product_usage_count` — обновление счетчика использования продукта
- `trigger_update_products_updated_at` — обновление updated_at для products
- `trigger_update_user_products_updated_at` — обновление updated_at для user_products
- `trigger_check_message_rate_limit` — проверка rate limit для сообщений

---

## 📁 Дополнительные скрипты

### Утилиты

- **create_default_nutrition_plan.sql** — создание плана питания по умолчанию для premium пользователей
- **create_test_users_v3.sql** — создание тестовых пользователей (v3)
- **create_test_users_v3_manual.md** — инструкция по созданию тестовых пользователей вручную
- **check_migrations_format.sh** — скрипт для проверки формата миграций

---

## 🔍 Проверка состояния базы данных

Для проверки, что база данных создана правильно, можно использовать следующий запрос:

```sql
-- Проверка основных таблиц
SELECT 
    CASE WHEN EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'profiles') 
         THEN 'profiles ✓' ELSE 'profiles ✗' END as profiles,
    CASE WHEN EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'nutrition_targets') 
         THEN 'nutrition_targets ✓' ELSE 'nutrition_targets ✗' END as nutrition_targets,
    CASE WHEN EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'daily_logs') 
         THEN 'daily_logs ✓' ELSE 'daily_logs ✗' END as daily_logs,
    CASE WHEN EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'coordinator_notes') 
         THEN 'coordinator_notes ✓' ELSE 'coordinator_notes ✗' END as coordinator_notes,
    CASE WHEN EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'messages') 
         THEN 'messages ✓' ELSE 'messages ✗' END as messages,
    CASE WHEN EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'products') 
         THEN 'products ✓' ELSE 'products ✗' END as products,
    CASE WHEN EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'achievements') 
         THEN 'achievements ✓' ELSE 'achievements ✗' END as achievements;

-- Проверка функций
SELECT 
    CASE WHEN EXISTS (SELECT 1 FROM pg_proc WHERE proname = 'is_super_admin') 
         THEN 'is_super_admin ✓' ELSE 'is_super_admin ✗' END as is_super_admin,
    CASE WHEN EXISTS (SELECT 1 FROM pg_proc WHERE proname = 'check_achievements') 
         THEN 'check_achievements ✓' ELSE 'check_achievements ✗' END as check_achievements,
    CASE WHEN EXISTS (SELECT 1 FROM pg_proc WHERE proname = 'use_invite_code') 
         THEN 'use_invite_code ✓' ELSE 'use_invite_code ✗' END as use_invite_code;
```

---

## 🔄 Realtime

Для работы чата необходимо включить Realtime для таблицы `messages`:

1. Перейдите в Supabase Dashboard → Database → Replication
2. Найдите таблицу `messages`
3. Включите Realtime

Или выполните SQL команду (если у вас есть права):

```sql
ALTER PUBLICATION supabase_realtime ADD TABLE messages;
```

---

## 📝 Примечания

- Все таблицы защищены Row Level Security (RLS)
- Все функции используют `SECURITY DEFINER` где необходимо
- Все триггеры идемпотентны (можно запускать несколько раз)
- Скрипт использует `IF NOT EXISTS` для предотвращения ошибок при повторном запуске

---

---

## 📦 Дополнительные миграции

### v8.1_add_public_profiles.sql
**Дата:** 2025-01-20  
**Описание:** Добавление поддержки публичных профилей и лидербордов  
**Изменения:**
- Добавлено поле `profile_visibility` в таблицу `profiles`
- Добавлен индекс для публичных профилей
- Обновлена RLS политика для чтения публичных профилей

> **Примечание:** Эти изменения уже включены в `setup_database.sql` для новых установок.

### v8.2_fix_profile_creation.sql
**Дата:** 2025-01-20  
**Описание:** Исправление проблемы создания профиля при регистрации  
**Проблема:** Ошибка "new row violates row-level security policy for table 'profiles'" при регистрации нового пользователя  
**Изменения:**
- Создана функция `create_user_profile()` с `SECURITY DEFINER` для безопасного создания профиля
- Функция обходит RLS и проверяет, что профиль еще не существует
- Обновлена RLS политика для INSERT в profiles
- Код регистрации обновлен для использования новой функции

> **Примечание:** Эта миграция критична для работы регистрации. Необходимо применить для существующих баз данных.

---

**Последнее обновление:** Январь 2025
