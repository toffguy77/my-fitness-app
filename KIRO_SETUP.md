# Настройка Kiro для работы с проектом

## Проблема

Kiro не наследует переменные окружения из вашей оболочки (zsh/bash), что приводит к ошибкам при работе с Go и другими инструментами.

## Быстрое решение

### 1. Для разовых команд

Перед запуском Go команд в терминале Kiro выполните:

```bash
source scripts/setup-env.sh
```

Это настроит переменные окружения для текущей сессии терминала.

### 2. Для постоянной настройки

Создан файл `.kiro/settings/env.json` с правильными путями:

```json
{
  "environment": {
    "GOROOT": "/opt/homebrew/opt/go/libexec",
    "GOPATH": "$HOME/src/go",
    "PATH": "/opt/homebrew/bin:/opt/homebrew/opt/go/libexec/bin:$HOME/src/go/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
  }
}
```

**Примечание:** Kiro может не поддерживать автоматическую загрузку этого файла. В таком случае используйте метод 1 или 3.

### 3. Использование Makefile (рекомендуется)

Makefile в `apps/api/` уже настроен с правильными переменными окружения:

```bash
# Вместо прямых команд Go
cd apps/api
make test
make build
make dev
```

Makefile автоматически установит GOROOT и PATH.

## Проверка настройки

```bash
# 1. Загрузить окружение
source scripts/setup-env.sh

# 2. Проверить Go
go version
# Должно вывести: go version go1.25.5 darwin/arm64

# 3. Запустить тесты
cd apps/api
make test
```

## Для разных операционных систем

### macOS (Homebrew)

```bash
# Установка Go
brew install go

# Проверка пути
brew --prefix go
# Выведет: /opt/homebrew/opt/go

# Переменные окружения
export GOROOT=/opt/homebrew/opt/go/libexec
export PATH=/opt/homebrew/bin:$GOROOT/bin:$PATH
```

### macOS (официальный установщик)

```bash
# После установки с https://go.dev/dl/
export GOROOT=/usr/local/go
export PATH=$GOROOT/bin:$PATH
```

### Linux

```bash
# После установки
export GOROOT=/usr/local/go
export PATH=$GOROOT/bin:$PATH
```

## Автоматизация для вашей оболочки

Добавьте в `~/.zshrc` или `~/.bashrc`:

```bash
# Go environment
if command -v brew &> /dev/null; then
    export GOROOT="$(brew --prefix go)/libexec"
else
    export GOROOT="/usr/local/go"
fi
export GOPATH="$HOME/src/go"
export PATH="$GOROOT/bin:$GOPATH/bin:$PATH"
```

Затем перезагрузите оболочку:
```bash
source ~/.zshrc  # или source ~/.bashrc
```

## Pre-commit хуки

Pre-commit хуки уже настроены с правильными переменными окружения в `.husky/pre-commit`.

Если Go не найден, хуки пропустят проверки backend с предупреждением.

## CI/CD

GitHub Actions автоматически настраивает окружение через `setup-go` action. Никаких дополнительных настроек не требуется.

## Troubleshooting

### Ошибка: "go: cannot find GOROOT directory: /libexec"

**Решение:**
```bash
source scripts/setup-env.sh
```

### Ошибка: "command not found: go"

**Проверьте установку:**
```bash
which go
# Должно вывести путь к Go

# Если не установлен
brew install go  # macOS
```

### Kiro не видит переменные окружения

**Временное решение:**
```bash
# В каждой новой сессии терминала Kiro
source scripts/setup-env.sh
```

**Постоянное решение:**
Используйте Makefile команды вместо прямых Go команд.

## Полезные команды

```bash
# Проверить текущее окружение
echo $GOROOT
echo $GOPATH
echo $PATH

# Проверить Go
go version
go env

# Запустить тесты через Makefile
cd apps/api
make test
make test-coverage

# Запустить dev сервер
make dev

# Собрать проект
make build
```

## Дополнительная информация

- Подробная документация: `.kiro/ENVIRONMENT_SETUP.md`
- Документация по тестированию: `docs/TESTING.md`
- Быстрый старт: `TESTING_QUICK_START.md`

## Для команды разработчиков

При клонировании проекта:

1. Установите Go (если не установлен)
2. Выполните `source scripts/setup-env.sh`
3. Проверьте: `go version`
4. Установите зависимости: `cd apps/api && go mod download`
5. Запустите тесты: `make test`

Готово! 🎉
