# Правила разработки My Fitness App

Этот документ описывает обязательные правила и процессы разработки для проекта My Fitness App. Все изменения должны соответствовать этим правилам.

---

## 📋 Обязательные правила

### 1. Документирование функциональности

**Правило:** Все изменения функциональности должны быть задокументированы **до** начала реализации.

**Процесс:**
- Изменения функциональности ведутся в документации:
  - `docs/Application_Navigation_Diagrams_vN.md` — диаграммы навигации и пользовательских потоков
  - `docs/Application_Structure_vN.md` — структура приложения, компоненты, API, состояние данных
- После каждого изменения функциональности обновляется соответствующая документация
- При значимых изменениях (breaking changes, новые фичи, рефакторинг архитектуры) создается следующая версия документации (vN+1)
- Версионирование документации должно быть синхронизировано с версионированием миграций БД

**Именование файлов документации:**
- Все файлы документации должны использовать формат версионирования с нулями для основных версий, чтобы обеспечить хронологическую сортировку при алфавитной сортировке
- Формат: `Application_Structure_v{Major}.{Minor}.md` или `Application_Structure_v{Major}.0.md` для основных версий
- Примеры правильного именования:
  - `Application_Structure_v1.0.md` (первая версия)
  - `Application_Structure_v2.0.md` (вторая основная версия)
  - `Application_Structure_v3.0.md` (третья основная версия)
  - `Application_Structure_v3.1.md` (первая минорная версия третьей основной)
  - `Application_Structure_v3.2.md` (вторая минорная версия третьей основной)
- Это правило применяется ко всем версионированным файлам документации в папке `docs/`
- При переименовании старых файлов без версии в названии, добавлять `.0` к номеру основной версии (например, `Application_Structure.md` → `Application_Structure_v1.0.md`)

**Критерии значимых изменений:**
- Добавление новых страниц/роутов
- Изменение структуры данных в БД
- Изменение бизнес-логики, влияющее на пользовательские потоки
- Изменение API или контрактов между компонентами
- Рефакторинг, меняющий архитектуру приложения

---

### 2. Миграции базы данных

**Правило:** Все изменения схемы БД выполняются через версионированные SQL-скрипты.

**Процесс:**
- Все SQL-скрипты находятся в `docs/migrations/`
- Инкрементальные миграции версионированы и выполняются последовательно
- **Формат именования миграций:** `v{Major}.{Minor}_{description}.sql` (например: `v3.2_add_feedback_loop.sql`)
  - ⚠️ **ВАЖНО:** Все миграции должны быть переименованы в этот формат
  - Старые миграции с другими форматами именования (например: `phase2.5_step1_add_enum.sql`, `add_phone_to_profiles.sql`) должны быть переименованы
  - Формат версии должен соответствовать версионированию документации (v3.1, v3.2 и т.д.)
- Каждая миграция должна быть:
  - Идемпотентной (можно запускать несколько раз без ошибок)
  - Обратимой (должна быть возможность отката, если требуется)
  - Содержать комментарии о назначении и зависимостях
- Также должен существовать скрипт для полного сетапа базы с нуля (например: `setup_database_from_scratch.sql`)
- **Обновление setup_database_from_scratch.sql:**
  - ⚠️ **ОБЯЗАТЕЛЬНО:** При создании новой миграции необходимо обновить файл `docs/migrations/setup_database_from_scratch.sql`
  - Добавить новую миграцию в список миграций в разделе "ПРИМЕНЕНИЕ ВСЕХ МИГРАЦИЙ"
  - Список должен быть полным и актуальным, включая все миграции в хронологическом порядке
  - Это гарантирует, что при создании новой базы с нуля будут применены все необходимые миграции
- Миграции выполняются в Supabase SQL Editor в порядке версий

**Структура миграции:**
```sql
-- Migration: v3.2_add_feedback_loop
-- Description: Добавление таблицы coach_notes для обратной связи тренера
-- Dependencies: v3.1_add_onboarding_fields.sql
-- Date: YYYY-MM-DD

-- Ваш SQL код здесь
```

---

### 3. Тестирование

**Правило:** Каждое изменение кода должно сопровождаться созданием или обновлением тестов.

**Типы тестов:**

1. **Unit Tests** — тестирование отдельных функций, утилит, хелперов
   - Расположение: рядом с тестируемым файлом в `__tests__/` или `*.test.ts`
   - Пример: `src/utils/__tests__/logger.test.ts`

2. **Functional Tests** — тестирование функциональности компонентов и страниц
   - Расположение: рядом с компонентом или в `__tests__/`
   - Пример: `src/components/__tests__/DayToggle.test.tsx`

3. **Integration Tests** — тестирование взаимодействия компонентов, API, БД
   - Расположение: `src/__tests__/integration/`
   - Тестируют полные пользовательские сценарии

4. **Business Logic Tests** — тестирование бизнес-правил и расчетов
   - Расположение: `src/__tests__/business/`
   - Примеры: расчет BMR/TDEE, валидация целей питания, логика статусов клиентов

**Требования к тестам:**
- Покрытие критичной бизнес-логики должно быть не менее 80%
- Общее покрытие кода должно стремиться к 80%+
- Тесты должны быть независимыми и изолированными
- Использовать моки для внешних зависимостей (Supabase, API)
- Тесты должны быть быстрыми и детерминированными

**Типы тестов:**
1. **Unit Tests** — тестирование отдельных функций и компонентов
2. **Functional Tests** — тестирование функциональности компонентов
3. **Integration Tests** — тестирование взаимодействия компонентов и сервисов
4. **Business Logic Tests** — тестирование бизнес-правил и расчетов
5. **E2E Tests** — тестирование полных пользовательских сценариев (Playwright)
6. **Performance Tests** — тестирование производительности и метрик

**Запуск тестов:**
```bash
npm test                    # Запуск всех unit/integration тестов (Jest)
npm test -- --watch        # Режим watch
npm test -- --coverage     # С покрытием кода
npm run test:e2e           # Запуск E2E тестов (Playwright)
npm run test:e2e:ui        # E2E тесты с UI режимом
npm run test:all           # Запуск всех тестов (Jest + E2E)
```

**E2E тестирование:**
- E2E тесты находятся в директории `e2e/`
- Используется Playwright для тестирования в браузере
- Тесты должны покрывать критические пользовательские сценарии:
  - Регистрация и авторизация
  - Онбординг
  - Ввод питания
  - Dashboard и навигация
  - Coach функционал
  - Производительность страниц

**Отчеты по тестам:**
- Все отчеты по тестам, найденные баги и исправления ведутся в едином файле: `BUGS_FOUND_AND_FIXED.md` в корне проекта
- При обнаружении бага через тесты:
  1. Добавить описание бага в раздел "Исправленные баги" файла `BUGS_FOUND_AND_FIXED.md`
  2. Исправить баг в коде
  3. Обновить статистику тестов в файле
  4. Добавить ссылки на исправленные файлы
- При добавлении новых тестов обновлять раздел "Созданные тесты" в `BUGS_FOUND_AND_FIXED.md`
- Файл `BUGS_FOUND_AND_FIXED.md` является единым источником правды о состоянии тестирования проекта

---

### 4. Процесс разработки фичи

**Правило:** После реализации функциональности выполняется полный цикл проверки перед коммитом.

**Чеклист перед коммитом:**

1. ✅ Документация обновлена (Application_Navigation_Diagrams и Application_Structure)
2. ✅ Миграции БД созданы и протестированы (если требуется)
3. ✅ Файл `setup_database_from_scratch.sql` обновлен со всеми миграциями (если создана новая миграция)
4. ✅ Тесты написаны/обновлены для всех изменений
5. ✅ Все тесты проходят (`npm test`)
6. ✅ Линтинг проходит без ошибок (`npm run lint`)
7. ✅ TypeScript компилируется без ошибок (`npm run type-check`)
8. ✅ Код проверен вручную в dev-режиме
9. ✅ Изменения закоммичены с понятным сообщением
10. ✅ Изменения запушены в репозиторий

**Формат commit message:**
```
type(scope): краткое описание

Подробное описание изменений (если необходимо)

- Что изменено
- Почему изменено
- Связанные задачи/документы
```

**Типы коммитов:**
- `feat` — новая функциональность
- `fix` — исправление бага
- `docs` — изменения в документации
- `test` — добавление/изменение тестов
- `refactor` — рефакторинг кода
- `migration` — миграции БД
- `style` — форматирование кода
- `chore` — обновление зависимостей, конфигов

**Пример:**
```
feat(nutrition): добавление возможности редактирования завершенных дней

- Добавлен параметр ?date= в URL для выбора даты
- Добавлена проверка is_completed перед редактированием
- Обновлена документация Application_Structure_v3.2.md
- Добавлены тесты для проверки блокировки редактирования

Closes #123
```

---

## 🔧 Дополнительные правила и рекомендации

### 5. Версионирование

**Правило:** Версионирование документации, миграций и кода должно быть синхронизировано.

**Схема версионирования:**
- **Major (v3.x)** — значительные изменения архитектуры, breaking changes
- **Minor (v3.2)** — новые фичи, изменения функциональности
- **Patch (v3.2.1)** — исправления багов, мелкие улучшения

**Синхронизация:**
- Версия документации должна соответствовать версии миграций
- При создании новой версии документации создается соответствующая миграция (если требуется)
- Версия в `package.json` может отличаться (семантическое версионирование npm)

---

### 6. Code Style и качество кода

**Правило:** Код должен следовать установленным стандартам и быть читаемым.

**Требования:**
- Использовать TypeScript строго (избегать `any`, использовать типы)
- Следовать правилам ESLint (конфигурация в `eslint.config.mjs`)
- Использовать Prettier для форматирования (если настроен)
- Компоненты должны быть функциональными и использовать React hooks
- Избегать дублирования кода (DRY принцип)
- Использовать осмысленные имена переменных и функций
- Добавлять комментарии для сложной бизнес-логики

**Проверка:**
```bash
npm run lint          # Проверка ESLint
npm run type-check    # Проверка TypeScript
```

---

### 7. Работа с Supabase

**Правило:** Все взаимодействие с БД через Supabase должно быть типизировано и безопасно.

**Требования:**
- Использовать типы из `@/utils/supabase/profile.ts` и других утилит
- Проверять ошибки при запросах к БД
- Использовать RLS (Row Level Security) политики для безопасности
- Логировать ошибки БД через `@/utils/logger`
- Использовать транзакции для связанных операций (если возможно)

**Пример:**
```typescript
const { data, error } = await supabase
  .from('daily_logs')
  .select('*')
  .eq('user_id', userId)
  .single()

if (error) {
  logger.error('Ошибка загрузки лога', error, { userId })
  throw error
}
```

---

### 8. Логирование

**Правило:** Использовать централизованный логгер для всех операций.

**Требования:**
- Использовать `@/utils/logger` для всех логов
- Логировать важные события: создание/обновление данных, ошибки, действия пользователей
- Использовать правильные уровни логирования:
  - `debug` — отладочная информация
  - `info` — информационные сообщения
  - `warn` — предупреждения
  - `error` — ошибки с контекстом
- Включать контекст в логи (userId, action, metadata)

**Пример:**
```typescript
logger.info('Nutrition: лог сохранен', { userId: user.id, date: selectedDate })
logger.error('Nutrition: ошибка сохранения', error, { userId: user.id })
```

---

### 9. Безопасность

**Правило:** Все изменения должны учитывать безопасность данных и доступ.

**Требования:**
- Проверять авторизацию пользователя перед операциями
- Использовать RLS политики в Supabase
- Не передавать чувствительные данные в клиентский код
- Валидировать входные данные на сервере (middleware)
- Использовать параметризованные запросы (Supabase делает это автоматически)
- Проверять права доступа (роли) перед операциями

---

### 10. Производительность

**Правило:** Код должен быть оптимизирован для производительности.

**Рекомендации:**
- Использовать React.memo для тяжелых компонентов
- Использовать useMemo и useCallback где необходимо
- Оптимизировать запросы к БД (select только нужные поля, использовать индексы)
- Избегать лишних ре-рендеров
- Использовать Next.js оптимизации (Image, Link, динамические импорты)

---

### 11. Обработка ошибок

**Правило:** Все ошибки должны обрабатываться корректно и логироваться.

**Требования:**
- Использовать try-catch для асинхронных операций
- Показывать понятные сообщения об ошибках пользователю
- Логировать ошибки с контекстом
- Не показывать технические детали ошибок в UI
- Использовать fallback UI для критичных ошибок

---

### 12. Git Workflow

**Правило:** Использовать понятную структуру веток и процесс слияния.

**Рекомендации:**
- `main` — стабильная ветка, всегда в рабочем состоянии
- `develop` — ветка разработки (опционально)
- Feature ветки: `feature/description` или `feat/description`
- Fix ветки: `fix/description`
- Названия веток должны быть описательными
- Перед мерджем в main: все тесты проходят, код ревью (если команда)

---

## 📝 Чеклист для нового разработчика

При начале работы над задачей:

1. ✅ Прочитать актуальную документацию (`Application_Structure_vN.md`, `Application_Navigation_Diagrams_vN.md`)
2. ✅ Понять текущую архитектуру и структуру данных
3. ✅ Проверить существующие миграции БД
4. ✅ Запустить проект локально и убедиться, что все работает
5. ✅ Изучить существующие тесты для понимания паттернов
6. ✅ Создать feature-ветку для работы

При завершении задачи:

1. ✅ Обновить документацию (если требуется)
2. ✅ Создать миграции БД (если требуется)
3. ✅ Обновить `setup_database_from_scratch.sql` со всеми миграциями (если создана новая миграция)
4. ✅ Написать/обновить тесты
5. ✅ Запустить все тесты и убедиться, что они проходят
6. ✅ Проверить линтинг и типы
7. ✅ Протестировать вручную
8. ✅ Закоммитить с понятным сообщением
9. ✅ Запушить изменения

---

## 🚨 Исключения и особые случаи

**Hotfixes:**
- Критичные баги могут быть исправлены без предварительного обновления документации
- Документация должна быть обновлена в том же коммите или сразу после

**Экспериментальные фичи:**
- Экспериментальные изменения могут быть сделаны в отдельной ветке
- Перед мерджем в main должны быть задокументированы и протестированы

**Рефакторинг:**
- Рефакторинг без изменения функциональности не требует обновления Application_Navigation_Diagrams
- Может потребовать обновления Application_Structure, если меняется архитектура

---

## 📚 Связанные документы

- [README.md](../README.md) — общая информация о проекте
- [Application_Structure_v3.2.md](../docs/Application_Structure_v3.2.md) — текущая структура приложения
- [Application_Navigation_Diagrams_v3.2.md](../docs/Application_Navigation_Diagrams_v3.2.md) — текущие диаграммы навигации
- [Phase2.6_BRD.md](../docs/Phase2.6_BRD.md) — бизнес-требования текущей фазы

---

**Последнее обновление:** 2025-01-XX  
**Версия правил:** 1.0

