# CORS Fix Applied ✅

## Проблема
Регистрация не работала из-за CORS ошибки:
```
Access to fetch at 'http://localhost:4000/api/v1/auth/register' from origin 'http://localhost:3000' 
has been blocked by CORS policy
```

## Причины
1. **Неправильный порт фронтенда**: Запускался на 3000 вместо 3069
2. **CORS настроен только на 3069**: Бэкенд разрешал только `http://localhost:3069`

## Исправления

### 1. Исправлен порт фронтенда
**Файл**: `apps/web/package.json`

Изменено:
```json
"dev": "next dev -p 3069",
"start": "next start -p 3069"
```

Теперь фронтенд запускается на правильном порту 3069.

### 2. CORS теперь разрешает оба порта
**Файл**: `apps/api/cmd/server/main.go`

Добавлена поддержка обоих портов (3000 и 3069) для удобства разработки:
```go
corsOrigins := []string{cfg.CORSOrigin}
if cfg.CORSOrigin == "http://localhost:3069" {
    corsOrigins = append(corsOrigins, "http://localhost:3000")
}
```

## Как проверить

1. **Перезапустите серверы**:
   ```bash
   # Остановите текущие процессы (Ctrl+C)
   make dev
   ```

2. **Проверьте порты**:
   - Frontend: http://localhost:3069 ✅
   - Backend: http://localhost:4000 ✅

3. **Тестируйте регистрацию**:
   - Откройте http://localhost:3069/auth
   - Заполните email и пароль
   - Отметьте 3 обязательных согласия
   - Нажмите "Зарегистрироваться"
   - Должно работать без CORS ошибок! ✅

## Что должно работать

### ✅ Регистрация
- POST /api/v1/auth/register
- С email, password, и consents
- Редирект на /onboarding после успеха

### ✅ Логин
- POST /api/v1/auth/login
- С email и password
- Редирект на /dashboard после успеха

### ✅ CORS
- Разрешены запросы с localhost:3069
- Разрешены запросы с localhost:3000 (для совместимости)
- Поддержка OPTIONS preflight запросов

## Логи бэкенда

Теперь вместо:
```
WARN HTTP request client error {"status_code": 403, "method": "OPTIONS"}
```

Должны видеть:
```
INFO HTTP request success {"status_code": 201, "method": "POST", "path": "/api/v1/auth/register"}
```

## Дополнительная информация

### Переменные окружения
Убедитесь что в `.env.local`:
```bash
CORS_ORIGIN=http://localhost:3069
WEB_PORT=3069
WEB_URL=http://localhost:3069
NEXT_PUBLIC_API_URL=http://localhost:4000
```

### Если всё ещё не работает

1. **Очистите кэш браузера** (Cmd+Shift+R на Mac)
2. **Проверьте что бэкенд запущен**: `curl http://localhost:4000/health`
3. **Проверьте логи бэкенда** на наличие ошибок
4. **Проверьте консоль браузера** на наличие других ошибок

## Готово! 🎉

Регистрация и логин теперь должны работать корректно.
